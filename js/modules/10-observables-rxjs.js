window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "observables-rxjs",
  "title": "Observables & RxJS",
  "icon": "bi bi-broadcast",
  "questions": [
    {
      "id": "what-are-observables",
      "title": "What are observables?",
      "explanation": "<p>An <strong>Observable</strong> is a stream — a sequence of values delivered over time. Unlike a regular function call that returns one value immediately, an Observable can emit zero, one, or many values across any span of time, and then either complete normally or error out.</p><p>Angular uses Observables everywhere: HTTP responses, form <code>valueChanges</code>, router events, <code>@Output</code> EventEmitters, and more. The RxJS library provides the Observable implementation.</p><h3>Key concepts</h3><ul><li><strong>Producer</strong> — the Observable itself; defines what values to emit and when</li><li><strong>Consumer</strong> — the subscriber; reacts to each emitted value</li><li><strong>Lazy</strong> — nothing happens until something subscribes. Each <code>subscribe()</code> call starts a fresh execution of the Observable.</li><li><strong>Unicast by default</strong> — each subscriber gets its own independent execution (cold Observable)</li></ul><h3>The three notification types</h3><ul><li><code>next(value)</code> — emits a value to the subscriber</li><li><code>error(err)</code> — terminates the stream with an error</li><li><code>complete()</code> — terminates the stream successfully (no more values)</li></ul>",
      "code": "import { Observable } from 'rxjs';\n\n// Creating an Observable manually\nconst numbers$ = new Observable<number>(observer => {\n  observer.next(1);     // emit 1\n  observer.next(2);     // emit 2\n  observer.next(3);     // emit 3\n  observer.complete();  // stream ends — no more values\n});\n\n// Subscribe to start receiving values\nnumbers$.subscribe({\n  next:     (value) => console.log('Got:', value),     // 1, 2, 3\n  error:    (err)   => console.error('Error:', err),\n  complete: ()      => console.log('Stream done!')\n});\n\n// Real-world: Angular HttpClient returns an Observable\n// this.http.get<User[]>('/api/users')  — emits once (the response), then completes\n// this.route.paramMap                 — emits every time the URL changes, never completes",
      "language": "typescript"
    },
    {
      "id": "observable-vs-promise",
      "title": "Difference between Observable and Promise",
      "explanation": "<p>Both handle async operations, but they are fundamentally different tools.</p><h3>Promise</h3><ul><li>Emits exactly <strong>one value</strong> (or one error), then it is done</li><li><strong>Eager</strong> — starts executing immediately when created</li><li>Cannot be cancelled</li><li>No built-in operators for transformation</li><li>Always async (microtask queue)</li></ul><h3>Observable</h3><ul><li>Can emit <strong>zero, one, or many values</strong> over time</li><li><strong>Lazy</strong> — only starts when subscribed</li><li>Can be <strong>cancelled</strong> by unsubscribing</li><li>Hundreds of operators for transforming, filtering, combining streams</li><li>Can be synchronous or asynchronous</li></ul><h3>When to use which?</h3><p>Use <strong>Observable</strong> for anything inside Angular (HTTP, forms, router). Use <strong>Promise</strong> only when interfacing with third-party libraries that return promises — and even then you can convert with <code>from(promise)</code>.</p>",
      "code": "import { Observable, from } from 'rxjs';\n\n// ─── Promise — one value, eager ────────────────────────────────\nconst promise = fetch('/api/users').then(res => res.json());\n// Starts fetching immediately. Cannot be cancelled.\n// Resolves with one value.\n\npromise.then(users => console.log(users));\n\n// ─── Observable — lazy, cancellable, many values ───────────────\nconst users$ = this.http.get<User[]>('/api/users');\n// Does nothing yet — no HTTP request has been made.\n\nconst sub = users$.subscribe(users => console.log(users));\n// Request starts NOW.\n\n// Cancel it before it finishes (e.g. user navigated away):\nsub.unsubscribe();\n// The pending HTTP request is actually cancelled!\n\n// ─── Convert a Promise to an Observable ────────────────────────\nconst obs$ = from(fetch('/api/users').then(r => r.json()));\n// Now you can use all RxJS operators on it\n\n// ─── Key difference: multiple values ───────────────────────────\n// A WebSocket stream emits a new message every few seconds — this\n// is impossible to model with a Promise but trivial with an Observable.\nconst messages$ = new Observable<string>(observer => {\n  const ws = new WebSocket('wss://example.com');\n  ws.onmessage = e => observer.next(e.data);     // many values\n  ws.onerror   = e => observer.error(e);\n  ws.onclose   = ()  => observer.complete();\n  return () => ws.close();   // cleanup on unsubscribe\n});",
      "language": "typescript"
    },
    {
      "id": "what-is-rxjs",
      "title": "What is RxJS?",
      "explanation": "<p><strong>RxJS (Reactive Extensions for JavaScript)</strong> is the library that Angular uses to work with Observables. It provides:</p><ul><li>The <code>Observable</code> class itself</li><li><strong>Creation functions</strong> — shortcuts for creating Observables from common sources (<code>of</code>, <code>from</code>, <code>interval</code>, <code>timer</code>, <code>fromEvent</code>)</li><li><strong>Operators</strong> — pure functions that transform, filter, combine, or control a stream (<code>map</code>, <code>filter</code>, <code>switchMap</code>, <code>debounceTime</code>, etc.)</li><li><strong>Subjects</strong> — special Observables that act as both producer and consumer</li></ul><h3>The pipe() method</h3><p>Operators are applied via the <code>.pipe()</code> method. You chain operators inside <code>pipe()</code>, and the output of each becomes the input of the next. The original Observable is never mutated.</p><h3>Why does Angular use RxJS?</h3><p>Async data in a UI app is complex — network requests can race, users can type faster than the server responds, multiple data sources need combining. RxJS gives you a composable, declarative toolkit to handle all of this cleanly.</p>",
      "code": "import { of, from, interval, fromEvent } from 'rxjs';\nimport { map, filter, take, debounceTime } from 'rxjs/operators';\n\n// ─── Creation functions ────────────────────────────────────────\n\n// of() — emits the arguments as a sequence, then completes\nof(1, 2, 3).subscribe(console.log);   // 1, 2, 3\n\n// from() — converts an array, Promise, or iterable to an Observable\nfrom([10, 20, 30]).subscribe(console.log);   // 10, 20, 30\n\n// interval() — emits an incrementing number every N milliseconds\ninterval(1000).pipe(take(5)).subscribe(console.log);  // 0, 1, 2, 3, 4 (one per second)\n\n// fromEvent() — wraps a DOM event as an Observable\nconst clicks$ = fromEvent(document, 'click');\nclicks$.subscribe(e => console.log('Clicked at:', (e as MouseEvent).x));\n\n// ─── Operators via pipe() ──────────────────────────────────────\nof(1, 2, 3, 4, 5)\n  .pipe(\n    filter(n => n % 2 === 0),   // keep only even numbers\n    map(n => n * 10)            // multiply each by 10\n  )\n  .subscribe(console.log);     // 20, 40",
      "language": "typescript"
    },
    {
      "id": "what-are-rxjs-operators",
      "title": "What are operators in RxJS?",
      "explanation": "<p><strong>Operators</strong> are pure functions that take an Observable as input, apply a transformation, and return a new Observable. They never modify the original stream — they create a new one.</p><p>Operators are grouped by what they do:</p><h3>Transformation operators</h3><ul><li><code>map(fn)</code> — transform each value (like Array.map)</li><li><code>switchMap(fn)</code> — map to an inner Observable, cancel previous if a new one starts</li><li><code>mergeMap(fn)</code> — map to an inner Observable, run all concurrently</li><li><code>concatMap(fn)</code> — map to an inner Observable, run one at a time in order</li></ul><h3>Filtering operators</h3><ul><li><code>filter(fn)</code> — only let values through that pass the predicate</li><li><code>debounceTime(ms)</code> — wait ms after the last emission before letting the value through</li><li><code>distinctUntilChanged()</code> — skip if value is same as previous</li><li><code>take(n)</code> — take the first n values then complete</li></ul><h3>Combination operators</h3><ul><li><code>combineLatest([a$, b$])</code> — emit latest from all streams whenever any emits</li><li><code>forkJoin([a$, b$])</code> — wait for all streams to complete, emit last values (like Promise.all)</li><li><code>merge(a$, b$)</code> — interleave emissions from multiple streams</li></ul><h3>Error handling</h3><ul><li><code>catchError(fn)</code> — catch an error and return a fallback Observable</li><li><code>retry(n)</code> — re-subscribe up to n times on error</li></ul>",
      "code": "import { of, interval } from 'rxjs';\nimport { map, filter, debounceTime, distinctUntilChanged,\n         switchMap, catchError, take } from 'rxjs/operators';\n\n// ─── map: transform each value ────────────────────────────────\nof(1, 2, 3).pipe(\n  map(n => n * n)   // square each number\n).subscribe(console.log);  // 1, 4, 9\n\n// ─── filter: keep only matching values ────────────────────────\nof(1, 2, 3, 4, 5).pipe(\n  filter(n => n > 3)\n).subscribe(console.log);  // 4, 5\n\n// ─── debounceTime + distinctUntilChanged: live search ─────────\n// Classic pattern for a search input\nsearchControl.valueChanges.pipe(\n  debounceTime(400),            // wait 400ms after user stops typing\n  distinctUntilChanged(),       // ignore if same value typed twice\n  filter(term => (term ?? '').length > 1),\n  switchMap(term =>             // cancel previous HTTP call\n    this.http.get<any[]>('/api/search?q=' + term).pipe(\n      catchError(() => of([]))  // on error, return empty array\n    )\n  )\n).subscribe(results => this.results = results);\n\n// ─── take: limit how many values you receive ───────────────────\ninterval(500).pipe(\n  take(3)   // only get the first 3 values (0, 1, 2) then auto-complete\n).subscribe(console.log);",
      "language": "typescript"
    },
    {
      "id": "what-is-subscribe-method",
      "title": "What is the subscribe() method?",
      "explanation": "<p>Calling <strong><code>subscribe()</code></strong> is what starts an Observable's execution. Nothing happens — no HTTP request, no timer, no computation — until you subscribe.</p><p><code>subscribe()</code> accepts an observer object with up to three callback properties:</p><ul><li><code>next</code> — called for each emitted value</li><li><code>error</code> — called if the stream encounters an error (stream terminates)</li><li><code>complete</code> — called when the stream ends normally (no more values)</li></ul><h3>The Subscription object</h3><p><code>subscribe()</code> returns a <strong>Subscription</strong>. Call <code>subscription.unsubscribe()</code> to stop listening and free resources. Forgetting to unsubscribe is the most common cause of memory leaks in Angular.</p><h3>When does Angular unsubscribe for you?</h3><p>The <strong>async pipe</strong> in templates handles subscribe and unsubscribe automatically. For manual subscriptions in component classes, you must unsubscribe yourself in <code>ngOnDestroy()</code>.</p>",
      "code": "import { interval, Subject } from 'rxjs';\nimport { takeUntil } from 'rxjs/operators';\n\n// ─── Basic subscribe ───────────────────────────────────────────\nconst sub = this.http.get<User[]>('/api/users').subscribe({\n  next:     (users)   => { this.users = users; this.loading = false; },\n  error:    (err)     => { this.error = true;  this.loading = false; },\n  complete: ()        => console.log('HTTP stream completed')\n});\n\n// ─── Unsubscribing ─────────────────────────────────────────────\nconst counter$ = interval(1000);\nconst sub2 = counter$.subscribe(n => console.log(n)); // 0, 1, 2, 3...\nsetTimeout(() => sub2.unsubscribe(), 5000);           // stops after 5 seconds\n\n// ─── Best practice in components: takeUntil pattern ───────────\nexport class LiveFeedComponent implements OnInit, OnDestroy {\n  private destroy$ = new Subject<void>();\n\n  ngOnInit() {\n    interval(1000)\n      .pipe(takeUntil(this.destroy$))  // auto-unsubscribes when destroy$ emits\n      .subscribe(n => console.log('tick', n));\n  }\n\n  ngOnDestroy() {\n    this.destroy$.next();\n    this.destroy$.complete();\n  }\n}\n\n// ─── Even better: use the async pipe in templates ──────────────\n// No manual subscribe/unsubscribe needed:\n// <div *ngFor=\"let user of users$ | async\">{{ user.name }}</div>",
      "language": "typescript"
    },
    {
      "id": "what-are-subjects",
      "title": "What are Subjects?",
      "explanation": "<p>A <strong>Subject</strong> is both an Observable and an Observer at the same time. This means you can:</p><ul><li>Call <code>subject.next(value)</code> to <em>push</em> a new value into the stream (act as producer)</li><li>Call <code>subject.subscribe()</code> to <em>listen</em> to the stream (act as consumer)</li><li>Share the same stream among multiple subscribers (multicast)</li></ul><h3>Key difference from a plain Observable</h3><p>A plain Observable is unicast — each subscriber gets its own independent execution. A Subject is multicast — all subscribers receive the same emissions. Think of a Subject like an event bus.</p><h3>Common use cases in Angular</h3><ul><li>Service-to-component communication (replacing EventEmitter for non-parent-child)</li><li>Triggering a side effect (like the <code>destroy$</code> pattern for unsubscribing)</li><li>Manual control over when a stream emits (e.g. triggering a data refresh)</li></ul>",
      "code": "import { Subject } from 'rxjs';\n\n// ─── Basic Subject ─────────────────────────────────────────────\nconst events$ = new Subject<string>();\n\n// Two subscribers share the same stream\nevents$.subscribe(e => console.log('Subscriber A:', e));\nevents$.subscribe(e => console.log('Subscriber B:', e));\n\nevents$.next('login');   // both get it: 'login'\nevents$.next('logout');  // both get it: 'logout'\n\n// ─── Real-world: notification service ─────────────────────────\nimport { Injectable } from '@angular/core';\n\n@Injectable({ providedIn: 'root' })\nexport class NotificationService {\n  private _toast$ = new Subject<{ message: string; type: 'success' | 'error' }>();\n\n  // Public read-only Observable — components subscribe to this\n  readonly toast$ = this._toast$.asObservable();\n\n  show(message: string, type: 'success' | 'error' = 'success') {\n    this._toast$.next({ message, type });\n  }\n}\n\n// Component: trigger a notification\n// this.notifications.show('Saved successfully!', 'success');\n\n// Toast component: listen and display\n// this.notifications.toast$.subscribe(toast => this.toasts.push(toast));",
      "language": "typescript"
    },
    {
      "id": "types-of-subjects",
      "title": "Types of Subjects (Subject, BehaviorSubject, ReplaySubject)",
      "explanation": "<p>RxJS provides three commonly used Subject variants. The difference is in what happens when a <em>new subscriber</em> joins after values have already been emitted.</p><h3>Subject — no memory</h3><p>New subscribers receive only values emitted <em>after</em> they subscribe. Missed values are gone forever. Use when you want a pure event bus and don't need late subscribers to catch up.</p><h3>BehaviorSubject — remembers the last value</h3><p>Requires an initial value. New subscribers immediately receive the <em>current (last emitted)</em> value, then continue receiving future emissions. This is the most common Subject in Angular apps — ideal for state (e.g. logged-in user, theme, cart count).</p><h3>ReplaySubject — remembers N values</h3><p>Buffers the last N values. New subscribers immediately receive those buffered values then continue live. Use when late subscribers need to catch up on recent history (e.g. last few chat messages, last few log entries).</p>",
      "code": "import { Subject, BehaviorSubject, ReplaySubject } from 'rxjs';\n\n// ─── Subject — new subscribers miss past values ────────────────\nconst clicks$ = new Subject<string>();\nclicks$.next('click 1');   // nobody listening yet — lost\n\nclicks$.subscribe(e => console.log('A:', e));\nclicks$.next('click 2');   // A: click 2\nclicks$.next('click 3');   // A: click 3\n\n// ─── BehaviorSubject — new subscribers get current value ───────\nconst theme$ = new BehaviorSubject<'light' | 'dark'>('light');\n\n// Late subscriber immediately gets 'light'\ntheme$.subscribe(t => console.log('Theme:', t));  // Theme: light (immediately)\n\ntheme$.next('dark');   // Theme: dark\n\n// Even later subscriber gets the current value 'dark'\ntheme$.subscribe(t => console.log('Late:', t));   // Late: dark (immediately)\n\nconsole.log('Current:', theme$.value);   // 'dark'  — read synchronously\n\n// ─── ReplaySubject — last N values buffered ────────────────────\nconst log$ = new ReplaySubject<string>(3);  // remember last 3\nlog$.next('msg 1');\nlog$.next('msg 2');\nlog$.next('msg 3');\nlog$.next('msg 4');\n\n// New subscriber gets last 3: 'msg 2', 'msg 3', 'msg 4'\nlog$.subscribe(msg => console.log('Log:', msg));\n\n// ─── Real-world: BehaviorSubject for auth state ────────────────\n@Injectable({ providedIn: 'root' })\nexport class AuthService {\n  private _user$ = new BehaviorSubject<User | null>(null);\n  readonly user$ = this._user$.asObservable();\n\n  login(user: User)  { this._user$.next(user); }\n  logout()           { this._user$.next(null); }\n  get currentUser()  { return this._user$.value; }  // synchronous read\n}",
      "language": "typescript"
    },
    {
      "id": "hot-vs-cold-observables",
      "title": "Hot vs Cold Observables",
      "explanation": "<p>This distinction affects whether each subscriber gets its own independent execution or shares a single running stream.</p><h3>Cold Observable</h3><p>The data source is created <em>inside</em> the Observable. Each subscriber gets its own fresh execution — like streaming a movie: each viewer starts from the beginning independently.</p><ul><li>Every <code>subscribe()</code> call triggers a new execution</li><li>HTTP requests via HttpClient are cold — each subscription makes a new network request</li><li>Subscribers don't share data</li></ul><h3>Hot Observable</h3><p>The data source exists <em>outside</em> the Observable. All subscribers share the same stream — like a live TV broadcast: you can tune in but you can't rewind.</p><ul><li>New subscribers join an already-running stream</li><li>Mouse events, WebSocket messages, Subjects are all hot</li><li>Subscribers share the same emissions; they miss anything emitted before they subscribed</li></ul><h3>Making a cold Observable hot — shareReplay</h3><p>Use <code>shareReplay(1)</code> to turn a cold Observable hot and cache the last value. This is very useful for HTTP calls you want to share across components without re-fetching.</p>",
      "code": "import { Observable, Subject } from 'rxjs';\nimport { shareReplay } from 'rxjs/operators';\n\n// ─── Cold Observable ───────────────────────────────────────────\nconst cold$ = this.http.get<User[]>('/api/users');\n// Each subscribe() sends a SEPARATE HTTP request:\ncold$.subscribe(u => console.log('A:', u));  // makes request 1\ncold$.subscribe(u => console.log('B:', u));  // makes request 2 (same URL!)\n\n// ─── Hot Observable ────────────────────────────────────────────\nconst hot$ = new Subject<string>();\nhot$.next('before anyone subscribes');  // lost — no subscribers yet\n\nhot$.subscribe(v => console.log('A:', v));\nhot$.next('both A and B get this');\nhot$.subscribe(v => console.log('B:', v));\nhot$.next('both get this too');        // A and B both receive it\n\n// ─── Making a cold Observable hot with shareReplay ─────────────\n// Good for: caching an API response shared across components\n\n@Injectable({ providedIn: 'root' })\nexport class ConfigService {\n  // Without shareReplay: every component that subscribes makes a NEW HTTP call\n  // With shareReplay(1): HTTP called once, result cached and shared\n  readonly config$ = this.http.get<AppConfig>('/api/config').pipe(\n    shareReplay(1)   // share among all subscribers, replay last value to late ones\n  );\n\n  constructor(private http: HttpClient) {}\n}",
      "language": "typescript"
    },
    {
      "id": "what-are-higher-order-observables",
      "title": "What are higher-order observables?",
      "explanation": "<p>A <strong>higher-order Observable</strong> is an Observable whose values are themselves Observables — an Observable of Observables. This pattern arises whenever you need to trigger an async operation in response to each emission of another stream.</p><p>The classic example: a user types in a search box. Each keystroke emits a search term. For each term, you need to make an HTTP request. The result is an Observable (the keystrokes) that produces another Observable (the HTTP request) for each value — a higher-order Observable.</p><h3>The challenge</h3><p>You need a way to 'flatten' the nested Observables — subscribe to each inner Observable and pass its values through to a single output stream. Different flattening strategies give you different behaviours, which is why there are four different operators for this.</p><h3>The four flattening operators</h3><ul><li><strong>switchMap</strong> — cancels the previous inner Observable when a new one starts. Best for: search, autocomplete — you only care about the latest request.</li><li><strong>mergeMap</strong> — runs all inner Observables concurrently. Best for: parallel uploads where you want all results.</li><li><strong>concatMap</strong> — queues inner Observables, runs one at a time in order. Best for: sequential operations that must not overlap (e.g. sequential API calls).</li><li><strong>exhaustMap</strong> — ignores new inner Observables while one is still active. Best for: a 'submit' button — ignore extra clicks while a form submission is in progress.</li></ul>",
      "code": "import { switchMap, mergeMap, concatMap, exhaustMap } from 'rxjs/operators';\n\n// ─── switchMap: cancel previous, use latest ────────────────────\n// Live search: if user types 'an', then 'ang', then 'angu' quickly,\n// only the last HTTP request for 'angu' matters.\nthis.searchCtrl.valueChanges.pipe(\n  debounceTime(300),\n  switchMap(term => this.http.get<any[]>('/api/search?q=' + term))\n).subscribe(results => this.results = results);\n\n// ─── mergeMap: run all concurrently ───────────────────────────\n// Upload multiple files at the same time:\nfrom(this.selectedFiles).pipe(\n  mergeMap(file => this.uploadService.upload(file))\n).subscribe(result => console.log('Uploaded:', result.name));\n\n// ─── concatMap: sequential, in order ─────────────────────────\n// Process orders one at a time, in the order they arrived:\nfrom(this.orderQueue).pipe(\n  concatMap(order => this.api.processOrder(order))\n).subscribe(result => console.log('Processed:', result.orderId));\n\n// ─── exhaustMap: ignore while busy ───────────────────────────\n// Login button: ignore extra clicks while a login request is pending\nfromEvent(loginBtn, 'click').pipe(\n  exhaustMap(() => this.authService.login(this.credentials))\n).subscribe({\n  next: user => this.router.navigate(['/dashboard']),\n  error: err => this.loginError = err.message\n});",
      "language": "typescript"
    },
    {
      "id": "common-rxjs-operators",
      "title": "Common RxJS operators in Angular",
      "explanation": "<p>These are the operators you will encounter in almost every Angular codebase. Understanding each one makes reading and writing RxJS pipelines intuitive.</p><h3>debounceTime(ms)</h3><p>Waits for a pause in emissions. If a new value arrives before the delay expires, the timer resets. The classic use is a search box — you don't want to fire an API call on every keystroke, only after the user pauses.</p><h3>distinctUntilChanged()</h3><p>Filters out consecutive duplicate values. Prevents unnecessary re-processing if the same value is emitted twice in a row.</p><h3>map(fn)</h3><p>Transforms each value — exactly like <code>Array.map()</code>. Use it to reshape, extract fields, or convert types.</p><h3>filter(fn)</h3><p>Only lets values through that satisfy the predicate — like <code>Array.filter()</code>.</p><h3>switchMap(fn)</h3><p>Maps to an inner Observable and cancels the previous inner Observable when a new one starts. The most important higher-order operator in Angular.</p><h3>catchError(fn)</h3><p>Intercepts errors in the stream. You return a fallback Observable from it — for example an empty array so the UI doesn't break, or a re-throw with a user-friendly message.</p><h3>takeUntil(notifier$)</h3><p>Completes the stream as soon as the <code>notifier$</code> Observable emits. The standard Angular pattern for auto-unsubscribing in <code>ngOnDestroy()</code>.</p><h3>tap(fn)</h3><p>Performs a side effect (like logging) without changing the stream. Useful for debugging pipelines.</p>",
      "code": "import { combineLatest, forkJoin, of } from 'rxjs';\nimport { map, filter, switchMap, debounceTime, distinctUntilChanged,\n         catchError, takeUntil, tap, finalize } from 'rxjs/operators';\n\n// ─── Full search pipeline (combines many operators) ────────────\nthis.searchCtrl.valueChanges.pipe(\n  debounceTime(400),              // wait 400ms after last keystroke\n  distinctUntilChanged(),         // skip if value hasn't changed\n  filter(term => (term ?? '').length >= 2), // only 2+ chars\n  tap(term => console.log('Searching for:', term)),  // debug log\n  switchMap(term =>\n    this.http.get<Product[]>('/api/products?q=' + term).pipe(\n      catchError(err => {\n        console.error('Search failed:', err);\n        return of([]);              // return empty array on error\n      })\n    )\n  ),\n  takeUntil(this.destroy$)        // auto-unsubscribe on component destroy\n).subscribe(products => this.products = products);\n\n// ─── forkJoin: like Promise.all — wait for multiple streams ────\n// Load user + their orders at the same time, render only when both are done\nforkJoin({\n  user:   this.http.get<User>('/api/users/' + this.userId),\n  orders: this.http.get<Order[]>('/api/users/' + this.userId + '/orders')\n}).subscribe(({ user, orders }) => {\n  this.user   = user;\n  this.orders = orders;\n});\n\n// ─── combineLatest: re-emit whenever any source emits ──────────\n// Filter products by both category AND search term reactively:\ncombineLatest([\n  this.categoryCtrl.valueChanges,\n  this.searchCtrl.valueChanges\n]).pipe(\n  debounceTime(200),\n  switchMap(([category, search]) =>\n    this.api.getProducts({ category, search })\n  )\n).subscribe(products => this.products = products);",
      "language": "typescript"
    }
  ]
});
