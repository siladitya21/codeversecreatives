window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "error-handling",
  "title": "Error Handling",
  "icon": "bi bi-bug",
  "questions": [
    {
      "id": "what-is-errorhandler",
      "title": "What is ErrorHandler and when does it fire?",
      "explanation": `
          <p>Every Angular application has a single global <strong>ErrorHandler</strong> that serves as the last line of defense for uncaught exceptions. When an error propagates up through the Angular component tree, service call stack, or RxJS pipeline without being caught, Angular routes it to the registered <code>ErrorHandler</code>. The default implementation does one thing: it calls <code>console.error(error)</code>, which logs the error to the browser console but otherwise lets the application continue running.</p>

          <p>The default behavior is useful during development but completely inadequate in production. A real production application needs to: capture errors with full stack traces, send them to an error tracking service (Sentry, Datadog, Rollbar), show a user-friendly message, and potentially redirect to a safe state. All of this is done by providing a custom <code>ErrorHandler</code>.</p>

          <h3>What ErrorHandler Does Not Catch</h3>
          <p>Angular's <code>ErrorHandler</code> catches synchronous errors thrown inside Angular's zone, errors thrown in component lifecycle hooks, and errors thrown in event handlers within Angular templates. It does <em>not</em> automatically catch HTTP errors (those come from RxJS observable errors that your code must handle), or errors in code running outside Angular's zone.</p>

          <h3>Zone Context</h3>
          <p>When you replace the default ErrorHandler, Angular calls your <code>handleError()</code> outside the Angular zone to prevent the error handler itself from triggering another change detection cycle. This means if you update component state inside <code>handleError()</code>, you need to use <code>NgZone.run()</code> to re-enter the zone and trigger rendering.</p>
        `,
      "code": "// ---- How ErrorHandler fits in the application ----\n// Normal flow:\n//   Component throws → Angular catches → ErrorHandler.handleError()\n\n// ---- Default ErrorHandler (what Angular provides out of the box) ----\nclass DefaultErrorHandler implements ErrorHandler {\n  handleError(error: unknown): void {\n    console.error(error);  // that's it\n  }\n}\n\n// ---- Production ErrorHandler with Sentry integration ----\nimport { ErrorHandler, Injectable, NgZone, inject } from '@angular/core';\nimport * as Sentry from '@sentry/angular';\n\n@Injectable()\nexport class GlobalErrorHandler implements ErrorHandler {\n  private ngZone = inject(NgZone);\n\n  handleError(error: unknown): void {\n    // Log to browser console for developer visibility\n    console.error('[GlobalErrorHandler]', error);\n\n    // Normalize error object — Angular sometimes wraps errors\n    const unwrapped = (error as { ngOriginalError?: Error }).ngOriginalError ?? error;\n\n    // Send to Sentry (or any error tracking service)\n    Sentry.captureException(unwrapped);\n\n    // Re-enter the zone to update UI (e.g., show an error toast)\n    this.ngZone.run(() => {\n      // inject NotificationService here and show a snackbar\n    });\n  }\n}\n\n// Register in bootstrapApplication (standalone) or AppModule:\n// providers: [{ provide: ErrorHandler, useClass: GlobalErrorHandler }]",
      "language": "typescript"
    },
    {
      "id": "http-error-interceptor",
      "title": "HTTP error interceptor — centralized HTTP error handling",
      "explanation": `
          <p>HTTP errors are different from runtime exceptions — they arrive as RxJS observable errors from Angular's <code>HttpClient</code>. The <code>ErrorHandler</code> does not catch them automatically; they propagate through the RxJS pipe as <code>HttpErrorResponse</code> objects. If your component does not handle the error (e.g., it just calls <code>.subscribe(data => ...)</code> without an error callback), the unhandled error eventually reaches <code>ErrorHandler</code>.</p>

          <p>The right place to handle HTTP errors centrally is in an <strong>HTTP interceptor</strong>. A functional interceptor (the modern Angular 15+ approach) is a plain function that wraps <code>next(request)</code> in a <code>catchError</code> pipe. Every HTTP call in the entire application flows through this function, giving you one place to map error codes to user-friendly messages, redirect on 401, log 5xx errors to your monitoring service, and decide whether to rethrow or swallow.</p>

          <h3>Functional Interceptors (Angular 15+)</h3>
          <p>The functional interceptor approach replaces the class-based <code>HttpInterceptor</code> interface. A functional interceptor is just a function that takes <code>(req, next)</code> and returns an observable. It is registered in <code>provideHttpClient(withInterceptors([myInterceptor]))</code>, and all injected services are accessed via <code>inject()</code> inside the function.</p>
        `,
      "code": "// ---- error.interceptor.ts (functional interceptor — Angular 15+) ----\nimport { HttpInterceptorFn, HttpErrorResponse } from '@angular/common/http';\nimport { inject } from '@angular/core';\nimport { Router } from '@angular/router';\nimport { catchError, throwError } from 'rxjs';\nimport { NotificationService } from './notification.service';\n\nexport const errorInterceptor: HttpInterceptorFn = (req, next) => {\n  const router = inject(Router);\n  const notify = inject(NotificationService);\n\n  return next(req).pipe(\n    catchError((error: HttpErrorResponse) => {\n      // Network error (no HTTP status — failed before reaching server)\n      if (error.status === 0) {\n        notify.error('No internet connection. Please check your network.');\n        return throwError(() => error);\n      }\n\n      switch (error.status) {\n        case 400:\n          // Validation error — server returned field-level errors\n          // Return the error so the component can display them\n          break;\n\n        case 401:\n          // Session expired — redirect to login\n          notify.warn('Your session has expired. Please sign in again.');\n          router.navigate(['/login'], {\n            queryParams: { returnUrl: router.url }\n          });\n          break;\n\n        case 403:\n          notify.error('You do not have permission to perform this action.');\n          router.navigate(['/unauthorized']);\n          break;\n\n        case 404:\n          // 404s are often expected (e.g., checking if a resource exists)\n          // Don't show a notification — let the component decide\n          break;\n\n        case 429:\n          notify.warn('Too many requests. Please wait a moment and try again.');\n          break;\n\n        default:\n          if (error.status >= 500) {\n            notify.error('A server error occurred. Our team has been notified.');\n            // Log server errors to monitoring\n            console.error('[HTTP 5xx]', req.url, error.status, error.message);\n          }\n      }\n\n      // Rethrow so components that need the error can still access it\n      return throwError(() => error);\n    })\n  );\n};\n\n// Register in main.ts:\n// bootstrapApplication(AppComponent, {\n//   providers: [\n//     provideHttpClient(withInterceptors([errorInterceptor]))\n//   ]\n// });",
      "language": "typescript"
    },
    {
      "id": "retry-logic-http",
      "title": "Retry strategies — retry(), retryWhen(), and exponential backoff",
      "explanation": `
          <p>Transient network failures are a reality in production — a mobile user briefly loses signal, a server reboots, or a load balancer times out under peak load. These failures are temporary: if you retry the request a moment later, it succeeds. Adding automatic retry logic prevents a transient failure from surfacing as a user-visible error.</p>

          <p>RxJS provides <code>retry()</code> for simple cases. It resubscribes to the source observable (re-sends the HTTP request) up to <code>count</code> times. If all retries fail, the error propagates to the subscriber. This basic form retries immediately and is only suitable when the failure reason is truly transient and benefits from an instant retry — like a single dropped packet.</p>

          <h3>Exponential Backoff</h3>
          <p>For most real-world retry scenarios, immediate retries are counterproductive. If the server is overloaded, hammering it with rapid retries makes the problem worse. <strong>Exponential backoff</strong> waits progressively longer between retries (1s, 2s, 4s, 8s...) and optionally adds random jitter to prevent the "thundering herd" problem where many clients retry simultaneously. RxJS's <code>retry({ delay })</code> configuration introduced in RxJS 7 makes this clean to express.</p>

          <h3>Which Errors to Retry</h3>
          <p>Only retry transient errors: network failures (<code>status === 0</code>) and server errors (<code>status >= 500</code>). Never retry client errors (4xx) — they indicate a permanent problem with the request that a retry will not fix. Retrying a 401 just sends the same bad credentials again; retrying a 400 sends the same malformed data.</p>
        `,
      "code": "import { HttpClient, HttpErrorResponse } from '@angular/common/http';\nimport { Injectable, inject } from '@angular/core';\nimport { Observable, throwError, timer } from 'rxjs';\nimport { retry, catchError } from 'rxjs/operators';\n\n@Injectable({ providedIn: 'root' })\nexport class ProductApiService {\n  private http = inject(HttpClient);\n\n  getProducts(): Observable<Product[]> {\n    return this.http.get<Product[]>('/api/products').pipe(\n      // Retry with exponential backoff — only for transient errors\n      retry({\n        count: 3,\n        delay: (error: HttpErrorResponse, retryCount: number) => {\n          // Only retry network errors and 5xx server errors\n          if (error.status === 0 || error.status >= 500) {\n            const delayMs = Math.pow(2, retryCount) * 1000;  // 2s, 4s, 8s\n            // Add jitter (±500ms) to prevent thundering herd\n            const jitter = Math.random() * 1000 - 500;\n            console.log(`Retry ${retryCount} in ${delayMs + jitter}ms`);\n            return timer(delayMs + jitter);\n          }\n          // For 4xx and other errors, do not retry — throw immediately\n          return throwError(() => error);\n        }\n      }),\n      catchError((error: HttpErrorResponse) => {\n        // All retries exhausted — transform to a user-facing error\n        if (error.status === 0) {\n          return throwError(() =>\n            new Error('Could not reach the server. Check your connection.')\n          );\n        }\n        return throwError(() =>\n          new Error(`Failed to load products (${error.status})`)\n        );\n      })\n    );\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "error-boundaries-in-angular",
      "title": "Route-level error pages and error boundaries",
      "explanation": `
          <p>Angular does not have React-style error boundary components, but you can achieve similar isolation using Angular's <strong>route-level error handling</strong>. When a route fails — whether because a lazy-loaded chunk fails to download, a route guard throws, or a resolve guard rejects — the router emits a <code>NavigationError</code> event. Listening to this event in your app shell lets you redirect to a friendly error page.</p>

          <h3>Route Error Pages</h3>
          <p>Define a wildcard <code>**</code> route that renders a 404 component, and a dedicated <code>/error</code> route for unexpected runtime errors. When your <code>GlobalErrorHandler</code> catches a critical error, it can navigate the user to <code>/error</code> with state describing the problem, rather than leaving the user on a broken page.</p>

          <h3>Resolver Errors</h3>
          <p>Route resolvers that fetch data before navigating should handle HTTP errors gracefully. If a resolver throws, the navigation is cancelled and the user stays on the current page — often a confusing outcome. The better pattern is to catch the error inside the resolver, return a default value or an empty result, and let the route component show an empty state instead of blocking navigation.</p>
        `,
      "code": "// ---- app.routes.ts ----\nimport { Routes } from '@angular/router';\n\nexport const routes: Routes = [\n  { path: '', loadComponent: () => import('./home/home.component').then(m => m.HomeComponent) },\n  { path: 'products', loadComponent: () => import('./products/product-list.component').then(m => m.ProductListComponent) },\n  { path: 'error', loadComponent: () => import('./error/error-page.component').then(m => m.ErrorPageComponent) },\n  { path: '404', loadComponent: () => import('./not-found/not-found.component').then(m => m.NotFoundComponent) },\n  { path: '**', redirectTo: '404' }  // Catch-all for unknown routes\n];\n\n// ---- app.component.ts — listen for router NavigationError ----\nimport { Component, OnInit, inject } from '@angular/core';\nimport { Router, NavigationError, Event } from '@angular/router';\nimport { filter } from 'rxjs/operators';\n\n@Component({\n  selector: 'app-root',\n  standalone: true,\n  template: '<router-outlet />'\n})\nexport class AppComponent implements OnInit {\n  private router = inject(Router);\n\n  ngOnInit(): void {\n    // Intercept router-level errors (failed lazy loads, guard errors)\n    this.router.events\n      .pipe(filter((e): e is NavigationError => e instanceof NavigationError))\n      .subscribe(event => {\n        console.error('Navigation error:', event.error);\n        this.router.navigate(['/error'], {\n          state: { message: 'Failed to load the requested page.' }\n        });\n      });\n  }\n}\n\n// ---- product.resolver.ts — graceful resolver error handling ----\nimport { inject } from '@angular/core';\nimport { ResolveFn } from '@angular/router';\nimport { catchError, of } from 'rxjs';\nimport { ProductService } from './product.service';\nimport type { Product } from './product.model';\n\nexport const productListResolver: ResolveFn<Product[]> =\n  (route, state) => {\n    return inject(ProductService).getAll().pipe(\n      catchError(err => {\n        // Don't block navigation — return empty array and let component show empty state\n        console.error('Failed to preload products:', err);\n        return of([]);\n      })\n    );\n  };",
      "language": "typescript"
    },
    {
      "id": "rxjs-error-handling",
      "title": "RxJS error handling — catchError, finalize, and EMPTY",
      "explanation": `
          <p>Angular applications are built heavily on RxJS, and understanding how errors propagate in observable pipelines is essential. When an observable emits an error, it terminates the stream — no more values will be emitted, and any operators downstream are skipped. This is different from a thrown exception: in a promise, you catch with <code>.catch()</code>; in an observable, the stream itself ends.</p>

          <h3>catchError</h3>
          <p><code>catchError()</code> intercepts the error and returns a new observable to continue with. If you return a fallback value observable (<code>of(defaultValue)</code>), the stream continues with that value and then completes normally. If you return <code>EMPTY</code>, the stream completes without emitting anything. If you return <code>throwError(() => newError)</code>, you re-throw (possibly a transformed error). The choice depends on whether you want the component to receive a fallback or nothing at all.</p>

          <h3>finalize</h3>
          <p><code>finalize()</code> is the observable equivalent of a <code>finally</code> block. It runs when the observable completes, errors, or is unsubscribed — regardless of outcome. This is the right place to hide loading spinners: <code>finalize(() => this.loading = false)</code> runs whether the HTTP request succeeds or fails.</p>

          <h3>Error Recovery vs Error Propagation</h3>
          <p>Not all errors should be caught at the service level. HTTP services that throw validation errors (<code>400</code> responses) should let those errors propagate to the component, because the component is the one that knows how to display field-level validation feedback. The interceptor handles cross-cutting concerns (401 redirect, 5xx notification), but per-request domain errors belong at the call site.</p>
        `,
      "code": "import { Component, OnInit, signal, inject } from '@angular/core';\nimport { HttpClient } from '@angular/common/http';\nimport { EMPTY, catchError, finalize, tap } from 'rxjs';\n\ninterface UserProfile { id: number; name: string; email: string; }\n\n@Component({\n  selector: 'app-profile',\n  standalone: true,\n  template: `\n    <div *ngIf=\"loading()\">Loading...</div>\n    <div *ngIf=\"error()\" class=\"error-banner\">{{ error() }}</div>\n    <div *ngIf=\"profile()\">\n      <h2>{{ profile()!.name }}</h2>\n      <p>{{ profile()!.email }}</p>\n    </div>\n  `\n})\nexport class ProfileComponent implements OnInit {\n  profile = signal<UserProfile | null>(null);\n  loading = signal(false);\n  error = signal<string | null>(null);\n\n  private http = inject(HttpClient);\n\n  ngOnInit(): void {\n    this.loading.set(true);\n    this.error.set(null);\n\n    this.http.get<UserProfile>('/api/me').pipe(\n      tap(user => this.profile.set(user)),\n\n      catchError(err => {\n        // Handle the error locally — this component needs to show specific feedback\n        if (err.status === 404) {\n          this.error.set('Profile not found. Please contact support.');\n        } else {\n          this.error.set('Could not load your profile. Please try again.');\n        }\n        // Return EMPTY to complete the stream gracefully without emitting a value\n        return EMPTY;\n      }),\n\n      // finalize always runs — perfect for hiding the loading indicator\n      finalize(() => this.loading.set(false))\n    ).subscribe();\n  }\n}",
      "language": "typescript"
    }
  ]
});
