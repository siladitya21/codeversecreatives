window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "accessibility",
  "title": "Accessibility (a11y)",
  "icon": "bi bi-universal-access",
  "questions": [
    {
      id: "angular-22-standard-accessibility-upgrade",
      title: "Angular 22 standard for accessibility",
      explanation: `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">Wheelchair ramps built into a building's original blueprint versus bolted on after an inspection fails. A ramp poured with the foundation is smooth, load-bearing, and barely noticeable. A ramp bolted on afterward is steep, wobbly, and obviously an afterthought. Accessibility works the same way &mdash; designed in from the first component, it's invisible and solid; retrofitted at the end, it's a rickety patch job.</p>
          </div>
        </div>
        <p>Angular 22-ready accessibility is built into component design from the start, not audited in at the end. Prefer semantic HTML, bind ARIA attributes correctly, manage focus after route changes and dialogs, announce async state changes, and verify with real keyboard and screen-reader testing rather than relying only on visual checks.</p>
        <h3>Modern a11y checklist</h3>
        <ul>
          <li>Use native controls before reaching for ARIA-heavy custom controls.</li>
          <li>Bind ARIA with <code>[attr.aria-*]</code>, not property binding.</li>
          <li>Use <code>@angular/cdk/a11y</code> for focus traps, live announcements, and keyboard helpers.</li>
          <li>Keep heading order and landmarks meaningful after router navigation.</li>
          <li>Support keyboard interaction for every interactive workflow.</li>
          <li>Respect reduced motion and keep focus states visible.</li>
        </ul>
      `,
      code: `@Component({
  selector: 'app-save-status',
  template: '<button type="button" (click)="save()">Save</button>'
})
export class SaveStatusComponent {
  private readonly live = inject(LiveAnnouncer);

  save(): void {
    this.live.announce('Saving changes');
    // save work...
    this.live.announce('Changes saved');
  }
}

// Dynamic ARIA:
// <button [attr.aria-expanded]="isOpen()" [attr.aria-controls]="panelId">
//   Filters
// </button>`,
      language: "typescript",
      diagram: `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">a11y — Built In, Not Bolted On</p><div class="grid grid-cols-2 md:grid-cols-3 gap-3 max-w-2xl mx-auto text-xs"><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3 text-center"><p class="font-bold text-indigo-700">Semantic HTML</p><p class="text-slate-500 mt-1">native first</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3 text-center"><p class="font-bold text-emerald-700">[attr.aria-*]</p><p class="text-slate-500 mt-1">correct binding</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3 text-center"><p class="font-bold text-amber-700">CDK a11y</p><p class="text-slate-500 mt-1">focus, announce</p></div><div class="bg-rose-50 border border-rose-200 rounded-lg p-3 text-center"><p class="font-bold text-rose-700">Route focus</p><p class="text-slate-500 mt-1">after navigation</p></div><div class="bg-purple-50 border border-purple-200 rounded-lg p-3 text-center"><p class="font-bold text-purple-700">Keyboard-first</p><p class="text-slate-500 mt-1">every workflow</p></div><div class="bg-cyan-50 border border-cyan-200 rounded-lg p-3 text-center"><p class="font-bold text-cyan-700">Real testing</p><p class="text-slate-500 mt-1">not just visual</p></div></div></div>`
    },
    {
      "id": "what-is-accessibility",
      "title": "What is accessibility in Angular?",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A <strong>museum audio guide next to the exhibit</strong>. The painting on the wall is the DOM &mdash; what a sighted visitor sees. The audio guide's script is the accessibility tree &mdash; a parallel description built from tags, attributes, and text that a blind visitor relies on entirely. If the museum only labels half the paintings, the audio guide falls silent at exactly the wrong moments. Angular's job is to make sure that script stays accurate as the exhibit rearranges itself in real time.</p>
          </div>
        </div>
        <p><strong>Accessibility (a11y)</strong> is the practice of building web applications that work for every user, including people with visual, auditory, motor, or cognitive disabilities. The "a11y" abbreviation comes from there being 11 letters between the "a" and "y" in "accessibility." Globally, roughly 15% of people live with some form of disability &mdash; skipping accessibility excludes a meaningful share of your users, not an edge case.</p>
        <p>In Angular, accessibility is not one feature you add at the end &mdash; it's a set of ongoing decisions made throughout development: choosing semantic HTML elements, managing keyboard focus correctly as the DOM changes, communicating dynamic updates to assistive technology, and making sure interactive controls work without a mouse.</p>
        <h3>Why browsers and screen readers work together</h3>
        <p>The browser exposes the DOM to assistive technologies through an <strong>accessibility tree</strong> &mdash; a parallel tree of accessibility objects derived from the DOM. Each element's tag, attributes, text content, and ARIA annotations determine what a screen reader announces. Angular's job is to keep this tree accurate as app state changes: adding ARIA attributes dynamically, managing focus after navigation, and announcing loading or error states that appear asynchronously.</p>
        <h3>The core rule</h3>
        <p>Use native HTML elements whenever possible. A <code>&lt;button&gt;</code> is keyboard-focusable, activatable with Enter and Space, announces its role as "button" to screen readers, and disables cleanly with the <code>disabled</code> attribute &mdash; all for free. A <code>&lt;div (click)="..."&gt;</code> has none of that built in; you'd have to add <code>tabindex</code>, <code>role="button"</code>, a <code>keydown</code> handler for Enter/Space, and ARIA state management by hand.</p>
      `,
      "code": "// ---- Semantic HTML: native elements carry behavior and meaning for free ----\n\n// GOOD: native button — keyboard-accessible, announces role, supports disabled\n// <button (click)=\"submit()\" [disabled]=\"form.invalid\">Place Order</button>\n\n// BAD: div requires you to add ALL of this manually:\n// <div\n//   role=\"button\"\n//   tabindex=\"0\"\n//   [attr.aria-disabled]=\"form.invalid\"\n//   (click)=\"form.invalid ? null : submit()\"\n//   (keydown.enter)=\"form.invalid ? null : submit()\"\n//   (keydown.space)=\"form.invalid ? null : submit()\">\n//   Place Order\n// </div>\n\n// ---- Choose the right element for the right job ----\n// Navigation links:       <a href>  or  <a routerLink>  (not <div>)\n// Toggle/expand:          <button>  (not <span (click)>)\n// Form controls:          <input>, <select>, <textarea>  (not <div contenteditable>)\n// Page sections:          <main>, <nav>, <aside>, <section>, <article>\n// Headings:               <h1>–<h6> in proper hierarchy (not <p class=\"heading\">)\n\n@Component({\n  selector: 'app-product-page',\n  template: `\n    <main>\n      <h1>Products</h1>\n      <nav aria-label=\"Product categories\">\n        <a routerLink=\"/electronics\">Electronics</a>\n        <a routerLink=\"/books\">Books</a>\n      </nav>\n      <section aria-labelledby=\"featured-heading\">\n        <h2 id=\"featured-heading\">Featured Items</h2>\n        <!-- product list -->\n      </section>\n    </main>\n  `\n})\nexport class ProductPageComponent {}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">DOM vs Accessibility Tree</p><div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs"><div class="bg-slate-50 border-2 border-slate-300 rounded-xl p-3"><p class="font-bold text-slate-700 text-center mb-2">What a sighted user sees</p><div class="flex flex-col items-center gap-1"><div class="bg-indigo-50 border border-indigo-200 rounded px-2 py-1 w-full text-center">&lt;h1&gt; Products</div><div class="bg-indigo-50 border border-indigo-200 rounded px-2 py-1 w-full text-center">&lt;button&gt; Add to Cart</div></div></div><div class="bg-slate-50 border-2 border-dashed border-slate-300 rounded-xl p-3"><p class="font-bold text-slate-700 text-center mb-2">What a screen reader announces</p><div class="flex flex-col items-center gap-1"><div class="bg-emerald-50 border border-emerald-200 rounded px-2 py-1 w-full text-center">"Products, heading level 1"</div><div class="bg-emerald-50 border border-emerald-200 rounded px-2 py-1 w-full text-center">"Add to Cart, button"</div></div></div></div></div>`
    },
    {
      "id": "aria-attributes",
      "title": "ARIA attributes in Angular",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">Sticky notes on a filing cabinet that has no printed labels. If the cabinet already says "Invoices" on the drawer (native HTML semantics), you don't need a sticky note. But if you built a custom drawer with no label at all (a <code>&lt;div&gt;</code> styled to look like a dropdown), a sticky note reading "aria-expanded: true/false" is the only way anyone finds out whether it's open. Stick notes only where the cabinet itself stays silent &mdash; two conflicting labels on the same drawer just confuse whoever's reading them.</p>
          </div>
        </div>
        <p><strong>ARIA (Accessible Rich Internet Applications)</strong> is a set of HTML attributes that augment the accessibility tree when native HTML semantics aren't sufficient. The most important rule: <em>use native HTML semantics first; add ARIA only when native elements cannot provide the needed role, state, or property.</em> Adding ARIA incorrectly is worse than not adding it &mdash; conflicting signals confuse screen readers rather than helping them.</p>
        <h3>ARIA roles</h3>
        <p>The <code>role</code> attribute tells assistive technology what kind of control an element is. Common roles in Angular apps: <code>role="dialog"</code> for modals, <code>role="alert"</code> for error messages that must be announced immediately, <code>role="status"</code> for updates like "Saving...", <code>role="tablist"</code>/<code>role="tab"</code> for tab navigation, and <code>role="progressbar"</code> for upload indicators.</p>
        <h3>ARIA states and properties</h3>
        <p><code>aria-expanded</code> &mdash; whether a panel, menu, or accordion is open or collapsed. <code>aria-selected</code> &mdash; whether a tab, option, or tree item is selected. <code>aria-disabled</code> &mdash; whether a control is disabled (use alongside native <code>disabled</code> where possible). <code>aria-label</code> &mdash; a screen-reader-only label for controls with no visible text (icon buttons). <code>aria-labelledby</code> &mdash; points to another element whose text labels this control. <code>aria-describedby</code> &mdash; points to supplementary text like a hint or error message.</p>
        <h3>Dynamic ARIA in Angular</h3>
        <p>Angular requires the <code>[attr.aria-*]</code> prefix for binding non-Angular attributes. Property binding syntax like <code>[aria-expanded]</code> does not work &mdash; Angular would look for a component input named <code>aria-expanded</code>, find none, and throw a template error. The correct syntax is always <code>[attr.aria-expanded]="isOpen"</code>.</p>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text">Writing <code>[aria-expanded]="isOpen"</code> instead of <code>[attr.aria-expanded]="isOpen"</code> compiles fine at first glance but fails at runtime with <code>Can't bind to 'aria-expanded' since it isn't a known property</code>, because Angular treats bracket-only syntax as a property binding, not an attribute binding. ARIA attributes almost always need the explicit <code>attr.</code> prefix.</p>
          </div>
        </div>
      `,
      "code": "@Component({\n  selector: 'app-accordion',\n  template: `\n    <!-- Icon button with no visible text: needs aria-label -->\n    <button (click)=\"closeDialog()\" aria-label=\"Close dialog\">\n      <svg aria-hidden=\"true\"><!-- decorative icon --></svg>\n    </button>\n\n    <!-- Accordion: aria-expanded communicates open/closed state -->\n    <div *ngFor=\"let section of sections\">\n      <button\n        [attr.aria-expanded]=\"section.isOpen\"\n        [attr.aria-controls]=\"'section-' + section.id\"\n        (click)=\"toggle(section)\"\n      >\n        {{ section.title }}\n      </button>\n      <div\n        [id]=\"'section-' + section.id\"\n        [attr.aria-hidden]=\"!section.isOpen\"\n      >\n        {{ section.content }}\n      </div>\n    </div>\n\n    <!-- Form field: aria-describedby links input to its error message -->\n    <div>\n      <label for=\"email\">Email</label>\n      <input\n        id=\"email\"\n        type=\"email\"\n        [(ngModel)]=\"email\"\n        [attr.aria-invalid]=\"emailTouched && !emailValid\"\n        aria-describedby=\"email-error\"\n      />\n      <span id=\"email-error\" role=\"alert\" *ngIf=\"emailTouched && !emailValid\">\n        Please enter a valid email address.\n      </span>\n    </div>\n\n    <!-- Alert role: screen readers announce this immediately -->\n    <div role=\"alert\" *ngIf=\"saveError\" class=\"error-banner\">\n      {{ saveError }}\n    </div>\n  `\n})\nexport class AccordionComponent {\n  sections = [\n    { id: 1, title: 'Shipping', content: '...', isOpen: false },\n    { id: 2, title: 'Returns', content: '...', isOpen: false }\n  ];\n  email = '';\n  emailTouched = false;\n  emailValid = true;\n  saveError = '';\n\n  toggle(section: any) { section.isOpen = !section.isOpen; }\n  closeDialog() { /* ... */ }\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Binding Syntax — Property vs Attribute</p><div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs"><div class="bg-rose-50 border-2 border-rose-200 rounded-xl p-3"><p class="font-bold text-rose-700 text-center mb-2">Wrong</p><div class="bg-white border border-rose-200 rounded px-2 py-1.5 font-mono text-center">[aria-expanded]="isOpen"</div><p class="text-slate-500 text-center mt-2">runtime error — not a known property</p></div><div class="bg-emerald-50 border-2 border-emerald-200 rounded-xl p-3"><p class="font-bold text-emerald-700 text-center mb-2">Right</p><div class="bg-white border border-emerald-200 rounded px-2 py-1.5 font-mono text-center">[attr.aria-expanded]="isOpen"</div><p class="text-slate-500 text-center mt-2">binds as an HTML attribute</p></div></div></div>`
    },
    {
      "id": "making-apps-accessible",
      "title": "How to make Angular apps accessible?",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A <strong>flight attendant's announcements</strong> during a flight nobody can see the seatbelt sign from every seat. "The seatbelt sign is now on" is spoken aloud precisely because a visual-only signal misses people who aren't looking at it. Angular CDK's <code>LiveAnnouncer</code> is that voice for your app &mdash; it speaks state changes that would otherwise only be visible pixels changing on screen.</p>
          </div>
        </div>
        <p>Making an Angular app accessible requires attention at several levels: structural (semantic HTML), behavioral (keyboard and focus management), communicative (ARIA live regions for dynamic updates), and perceptual (color contrast and font size). The Angular CDK's <code>a11y</code> module provides utilities that solve the hardest of these problems for you.</p>
        <h3>LiveAnnouncer — communicating dynamic changes</h3>
        <p>When content changes dynamically without a page load &mdash; a search result count updating, a file upload completing, a toast notification appearing &mdash; screen reader users have no way to know unless you explicitly announce it. Angular CDK's <code>LiveAnnouncer</code> injects text into an <code>aria-live</code> region, causing screen readers to read it aloud. Use <code>'polite'</code> for non-urgent announcements (reads after the user finishes their current interaction) and <code>'assertive'</code> for urgent ones (interrupts immediately).</p>
        <h3>Page titles — Angular's Title service</h3>
        <p>Screen reader users navigate between pages and need to hear the page title announced immediately. Angular's <code>Title</code> service sets the browser tab title, which screen readers announce on navigation. Without it, every route sounds identical.</p>
        <h3>Skip links</h3>
        <p>A skip link is the first focusable element on the page &mdash; an anchor that jumps the user directly to the main content, bypassing the navigation bar. Without one, keyboard and screen reader users must tab through every nav link on every page before reaching the content. It's typically visually hidden until focused.</p>
      `,
      "code": "import { Component, inject, OnInit } from '@angular/core';\nimport { LiveAnnouncer } from '@angular/cdk/a11y';\nimport { Title } from '@angular/platform-browser';\nimport { Router, NavigationEnd } from '@angular/router';\nimport { filter } from 'rxjs/operators';\n\n// ---- Announcing dynamic changes to screen readers ----\n@Component({\n  selector: 'app-search',\n  template: `\n    <input (input)=\"search($event)\" placeholder=\"Search products\" />\n    <p aria-live=\"polite\" class=\"sr-only\">{{ resultAnnouncement }}</p>\n  `\n})\nexport class SearchComponent {\n  private announcer = inject(LiveAnnouncer);\n  resultAnnouncement = '';\n\n  async search(event: Event): Promise<void> {\n    const query = (event.target as HTMLInputElement).value;\n    // ... fetch results ...\n    const count = 12; // from API\n\n    // Politely announce result count without interrupting user\n    await this.announcer.announce(`${count} results found for \"${query}\"`, 'polite');\n    this.resultAnnouncement = `${count} results found for \"${query}\"`;\n  }\n}\n\n// ---- Setting page title on route navigation ----\n@Component({ selector: 'app-root', template: `\n  <!-- Skip link: visually hidden but first in tab order -->\n  <a href=\"#main-content\" class=\"skip-link\">Skip to main content</a>\n  <app-nav></app-nav>\n  <main id=\"main-content\">\n    <router-outlet></router-outlet>\n  </main>\n`})\nexport class AppComponent implements OnInit {\n  private router = inject(Router);\n  private title = inject(Title);\n\n  ngOnInit(): void {\n    this.router.events\n      .pipe(filter(e => e instanceof NavigationEnd))\n      .subscribe(() => {\n        // Each route should define its own title via Route.title\n        // Title service ensures screen readers announce the new page\n      });\n  }\n}\n\n// ---- Route-level titles ----\n// const routes: Routes = [\n//   { path: 'products', title: 'Products — My Shop', component: ProductListComponent },\n//   { path: 'cart',     title: 'Shopping Cart — My Shop', component: CartComponent }\n// ];",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">CDK a11y — Four Building Blocks</p><div class="grid grid-cols-2 gap-3 max-w-md mx-auto text-xs"><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3 text-center"><p class="font-bold text-indigo-700">LiveAnnouncer</p><p class="text-slate-500 mt-1">speaks changes</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3 text-center"><p class="font-bold text-emerald-700">Title service</p><p class="text-slate-500 mt-1">announces routes</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3 text-center"><p class="font-bold text-amber-700">Skip link</p><p class="text-slate-500 mt-1">bypass nav</p></div><div class="bg-rose-50 border border-rose-200 rounded-lg p-3 text-center"><p class="font-bold text-rose-700">FocusTrap</p><p class="text-slate-500 mt-1">modal focus</p></div></div></div>`
    },
    {
      "id": "screen-readers-and-semantics",
      "title": "Working with screen readers",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A <strong>table of contents versus a pile of loose pages</strong>. Headings that go <code>h1 &rarr; h2 &rarr; h3</code> in order let a screen reader user jump straight to "Shipping Details" the way you'd flip to a chapter using a table of contents. Skip a level, or use an <code>&lt;h4&gt;</code> purely because it happens to render at the right font size, and the table of contents lies &mdash; the reader lands somewhere unexpected with no idea how they got there.</p>
          </div>
        </div>
        <p>Screen readers &mdash; NVDA, JAWS, VoiceOver, TalkBack &mdash; convert on-screen content to speech or Braille output. They navigate the browser's accessibility tree, not the visual layout. A beautiful design that relies on position, color, or visual hierarchy to convey meaning communicates nothing to a screen reader user.</p>
        <h3>Heading hierarchy</h3>
        <p>Screen reader users frequently navigate by headings, jumping from <code>&lt;h1&gt;</code> to <code>&lt;h2&gt;</code> to <code>&lt;h3&gt;</code> to understand page structure. A page should have exactly one <code>&lt;h1&gt;</code> (the main topic), and headings should not skip levels. Using a heading tag for its visual size rather than its semantic level breaks navigation for these users &mdash; control visual size with CSS, and let headings communicate structure.</p>
        <h3>Images and media</h3>
        <p>Every <code>&lt;img&gt;</code> needs an <code>alt</code> attribute. If the image is informative, the alt text should describe what it communicates. If it's decorative, use an empty string <code>alt=""</code> &mdash; this signals screen readers to skip the image entirely. Missing <code>alt</code> attributes cause screen readers to read out the filename, which is meaningless noise.</p>
        <h3>Form accessibility</h3>
        <p>Every form input must have a label. The most robust way is a visible <code>&lt;label for="inputId"&gt;</code> paired with <code>&lt;input id="inputId"&gt;</code>. When a visible label isn't possible, use <code>aria-label</code>. Error messages must be programmatically associated with their input via <code>aria-describedby</code> &mdash; placing an error message visually near the input isn't enough; the connection must exist in the accessibility tree.</p>
      `,
      "code": "@Component({\n  selector: 'app-checkout-form',\n  imports: [ReactiveFormsModule, NgIf],\n  template: `\n    <!-- Skip link at top of page -->\n    <a href=\"#checkout-form\" class=\"skip-link\">Skip to checkout form</a>\n\n    <main>\n      <h1>Checkout</h1>  <!-- One h1 per page -->\n\n      <section aria-labelledby=\"shipping-heading\">\n        <h2 id=\"shipping-heading\">Shipping Details</h2>\n\n        <form [formGroup]=\"form\" (ngSubmit)=\"submit()\" id=\"checkout-form\">\n\n          <!-- Proper label association -->\n          <div class=\"field\">\n            <label for=\"full-name\">Full Name</label>\n            <input\n              id=\"full-name\"\n              formControlName=\"fullName\"\n              type=\"text\"\n              autocomplete=\"name\"\n              [attr.aria-invalid]=\"isInvalid('fullName')\"\n              aria-describedby=\"full-name-error\"\n            />\n            <!-- Error associated via aria-describedby -->\n            <span\n              id=\"full-name-error\"\n              role=\"alert\"\n              *ngIf=\"isInvalid('fullName')\"\n            >\n              Full name is required.\n            </span>\n          </div>\n\n          <!-- Decorative image: empty alt -->\n          <img src=\"secure-badge.svg\" alt=\"\" /> Secure checkout\n\n          <button type=\"submit\" [disabled]=\"form.invalid\">\n            Place Order\n          </button>\n\n        </form>\n      </section>\n    </main>\n  `\n})\nexport class CheckoutFormComponent {\n  form = inject(FormBuilder).group({\n    fullName: ['', Validators.required]\n  });\n\n  isInvalid(field: string): boolean {\n    const ctrl = this.form.get(field);\n    return !!(ctrl?.invalid && ctrl?.touched);\n  }\n\n  submit() { /* ... */ }\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Heading Hierarchy — Right vs Wrong</p><div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs"><div class="bg-emerald-50 border-2 border-emerald-200 rounded-xl p-3"><p class="font-bold text-emerald-700 text-center mb-2">Correct</p><div class="space-y-1 font-mono"><div class="bg-white border border-emerald-200 rounded px-2 py-1">h1 Checkout</div><div class="bg-white border border-emerald-200 rounded px-2 py-1 ml-3">h2 Shipping Details</div><div class="bg-white border border-emerald-200 rounded px-2 py-1 ml-6">h3 Address</div></div></div><div class="bg-rose-50 border-2 border-rose-200 rounded-xl p-3"><p class="font-bold text-rose-700 text-center mb-2">Broken</p><div class="space-y-1 font-mono"><div class="bg-white border border-rose-200 rounded px-2 py-1">h1 Checkout</div><div class="bg-white border border-rose-200 rounded px-2 py-1 ml-6">h4 Shipping Details</div><div class="bg-white border border-rose-200 rounded px-2 py-1 ml-3">h2 Address</div></div></div></div></div>`
    },
    {
      "id": "keyboard-and-focus-management",
      "title": "Keyboard navigation and focus management",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A <strong>revolving door at a bank</strong> when a teller pulls you aside for a private conversation. While that conversation (the modal) is happening, you shouldn't be able to wander back out through the revolving door into the main lobby (the page behind the dialog) &mdash; you're kept inside until the conversation ends, then escorted back to exactly where you were standing. That's a focus trap: Tab and Shift+Tab cycle only within the dialog until it closes, then focus returns to the button that opened it.</p>
          </div>
        </div>
        <p>Keyboard accessibility is required for users with motor disabilities who cannot use a mouse, power users who prefer keyboards, and anyone using a switch device or other alternative input. Every interactive element in your Angular app must be reachable and operable via keyboard alone.</p>
        <h3>Focus trap — modals and dialogs</h3>
        <p>When a modal dialog opens, focus must move into the dialog and stay there until it closes. If a user can Tab out onto the underlying page content, they lose their place in the dialog and may interact with content that should be inert. Angular CDK's <code>FocusTrap</code> solves this &mdash; place <code>cdkTrapFocus</code> on the dialog container and Angular ensures Tab and Shift+Tab cycle only through the dialog's focusable elements. When the dialog closes, focus must return to whatever triggered it (typically the button that opened it).</p>
        <h3>FocusMonitor — keyboard vs mouse focus styles</h3>
        <p>The <code>:focus-visible</code> CSS pseudo-class (and Angular CDK's <code>FocusMonitor</code>) let you show focus outlines only when focus arrives via keyboard, not mouse click. This satisfies both groups: mouse users don't see distracting outlines on click, keyboard users get clear visual indicators. Never use <code>outline: none</code> globally without a replacement &mdash; that makes keyboard navigation invisible.</p>
        <h3>Programmatic focus after navigation</h3>
        <p>In single-page applications, route changes don't move focus automatically &mdash; the browser only shifts focus to the top of the page on hard reloads. After a route change, Angular should move focus to the main content heading or the page's <code>&lt;h1&gt;</code> so screen reader users know a navigation happened.</p>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text">Adding <code>cdkTrapFocus</code> without <code>cdkTrapFocusAutoCapture</code> traps Tab cycling correctly but never actually moves focus into the dialog on open &mdash; keyboard users still land on whatever was focused before, tab past invisible trapped content, and get stuck. The trap and the initial focus move are two separate jobs; you almost always want both.</p>
          </div>
        </div>
      `,
      "code": "import { Component, ViewChild, ElementRef, OnDestroy, inject, AfterViewInit } from '@angular/core';\nimport { A11yModule, FocusTrap, FocusTrapFactory, FocusMonitor } from '@angular/cdk/a11y';\n\n// ---- Modal with focus trap ----\n@Component({\n  selector: 'app-confirm-modal',\n  imports: [A11yModule, NgIf],\n  template: `\n    <div\n      *ngIf=\"isOpen\"\n      class=\"modal-overlay\"\n      role=\"dialog\"\n      aria-modal=\"true\"\n      [attr.aria-labelledby]=\"'modal-title-' + id\"\n      cdkTrapFocus                    <!-- keeps Tab cycling inside the dialog -->\n      cdkTrapFocusAutoCapture         <!-- moves focus into the dialog on open -->\n    >\n      <h2 [id]=\"'modal-title-' + id\">Confirm Delete</h2>\n      <p>This action cannot be undone.</p>\n      <button (click)=\"confirm()\">Delete</button>\n      <button (click)=\"cancel()\">Cancel</button>\n    </div>\n  `\n})\nexport class ConfirmModalComponent {\n  @ViewChild('triggerBtn') triggerBtn!: ElementRef;\n  isOpen = false;\n  id = Math.random().toString(36).slice(2);\n\n  open(triggerElement: HTMLElement): void {\n    this.isOpen = true;\n    // Store trigger so we can return focus when dialog closes\n    (this as any)._trigger = triggerElement;\n  }\n\n  cancel(): void {\n    this.isOpen = false;\n    // Return focus to the element that opened the dialog\n    (this as any)._trigger?.focus();\n  }\n\n  confirm(): void {\n    this.isOpen = false;\n    (this as any)._trigger?.focus();\n  }\n}\n\n// ---- FocusMonitor: style differently for keyboard vs mouse ----\n@Component({\n  selector: 'app-nav-button',\n  template: '<button #btn><ng-content></ng-content></button>'\n})\nexport class NavButtonComponent implements AfterViewInit, OnDestroy {\n  @ViewChild('btn') btn!: ElementRef;\n  private focusMonitor = inject(FocusMonitor);\n\n  ngAfterViewInit(): void {\n    this.focusMonitor.monitor(this.btn, true).subscribe(origin => {\n      const el = this.btn.nativeElement;\n      // Show outline only for keyboard focus, not mouse/touch\n      el.classList.toggle('keyboard-focused', origin === 'keyboard');\n    });\n  }\n\n  ngOnDestroy(): void {\n    this.focusMonitor.stopMonitoring(this.btn);\n  }\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Focus Trap Lifecycle</p><div class="flex flex-wrap items-center justify-center gap-2 text-xs"><div class="bg-indigo-50 border-2 border-indigo-200 rounded-lg px-3 py-2 text-center font-semibold text-indigo-700">Trigger button focused</div><span class="text-slate-300">&rarr;</span><div class="bg-emerald-50 border-2 border-emerald-200 rounded-lg px-3 py-2 text-center font-semibold text-emerald-700">Dialog opens<br><span class="font-normal text-slate-500">focus auto-captured</span></div><span class="text-slate-300">&rarr;</span><div class="bg-amber-50 border-2 border-amber-200 rounded-lg px-3 py-2 text-center font-semibold text-amber-700">Tab cycles<br><span class="font-normal text-slate-500">inside dialog only</span></div><span class="text-slate-300">&rarr;</span><div class="bg-rose-50 border-2 border-rose-200 rounded-lg px-3 py-2 text-center font-semibold text-rose-700">Dialog closes<br><span class="font-normal text-slate-500">focus returns to trigger</span></div></div></div>`
    },
    {
      "id": "angular-aria",
      "title": "Angular Aria - accessible headless components",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">The <strong>engine and chassis of a car sold without a body</strong>. Angular Aria gives you a fully working, correctly wired drivetrain &mdash; the keyboard navigation, focus management, and ARIA state logic all function correctly &mdash; but you bolt on whatever bodywork (markup and CSS) matches your brand. Compare that to buying a car off the lot fully assembled (Angular Material): faster to drive away in, but you're stuck with its styling.</p>
          </div>
        </div>
        <p><strong>Angular Aria</strong> is Angular 21's developer-preview package for building accessible, headless UI components. It provides directives for common WAI-ARIA patterns such as tabs, menus, listbox, select, toolbar, accordion, tree, and grid.</p>
        <h3>Why it matters</h3>
        <p>Custom interactive components are easy to make look right and hard to make behave correctly. Keyboard navigation, roving tabindex, focus management, selected/active state, ARIA attributes, and screen reader behavior all have to work together in lockstep. Angular Aria handles these interaction contracts for you while you keep full control over markup, styling, and business logic.</p>
        <h3>Angular Aria vs Angular Material</h3>
        <p>Reach for Angular Material when you want complete, pre-styled components out of the box. Reach for Angular Aria when your team already has a custom design system and needs correct accessible behavior underneath it, without inheriting Material's visual opinions.</p>
      `,
      "code": "import { Component } from '@angular/core';\nimport { TabList, Tab, TabPanel } from '@angular/aria/tabs';\n\n@Component({\n  selector: 'app-settings-tabs',\n  imports: [TabList, Tab, TabPanel],\n  template: `\n    <div ngTabList aria-label=\"Settings sections\" [(selectedTab)]=\"selectedTab\">\n      <button ngTab value=\"profile\">Profile</button>\n      <button ngTab value=\"security\">Security</button>\n      <button ngTab value=\"billing\">Billing</button>\n    </div>\n\n    <section ngTabPanel value=\"profile\">\n      <h2>Profile</h2>\n      <p>Profile settings go here.</p>\n    </section>\n\n    <section ngTabPanel value=\"security\">\n      <h2>Security</h2>\n      <p>Password and MFA settings go here.</p>\n    </section>\n\n    <section ngTabPanel value=\"billing\">\n      <h2>Billing</h2>\n      <p>Plan and payment settings go here.</p>\n    </section>\n  `\n})\nexport class SettingsTabsComponent {\n  selectedTab = 'profile';\n}\n\n// Install: npm install @angular/aria",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Headless vs Styled Components</p><div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs"><div class="bg-slate-50 border-2 border-dashed border-slate-300 rounded-xl p-3 text-center"><p class="font-bold text-slate-700 mb-2">Angular Aria</p><p class="text-slate-500">behavior + a11y only</p><p class="text-slate-500 mt-1">you supply markup &amp; CSS</p></div><div class="bg-slate-800 text-white rounded-xl p-3 text-center"><p class="font-bold mb-2">Angular Material</p><p class="text-slate-300">behavior + a11y + styling</p><p class="text-slate-300 mt-1">Material Design out of the box</p></div></div></div>`
    },
    {
      "id": "testing-accessibility",
      "title": "Testing for accessibility",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A <strong>building inspection with a checklist and a walk-through</strong>. The checklist (automated tools like axe-core) catches missing fire extinguishers and unlabeled exits fast &mdash; objective, mechanical, incomplete. It cannot tell you whether the hallway actually feels safe to walk at night. For that you send an inspector to physically walk it (manual keyboard and screen reader testing). Real accessibility testing needs both.</p>
          </div>
        </div>
        <p>Accessibility testing requires multiple strategies because no single tool catches everything. Automated tools catch roughly 30&ndash;40% of accessibility issues. The rest need human judgment &mdash; navigating with a keyboard, running a screen reader, or listening to how the app sounds to understand its flow.</p>
        <h3>Automated testing with axe-core</h3>
        <p><code>@axe-core/angular</code> integrates the industry-standard axe accessibility engine into your Angular app during development. Enabled, it automatically audits each rendered page and logs violations to the browser console &mdash; catching missing alt text, incorrect ARIA usage, insufficient color contrast, and missing form labels before you even open DevTools.</p>
        <h3>Unit testing ARIA attributes</h3>
        <p>Use Angular's <code>TestBed</code> and <code>ComponentFixture</code> to assert that dynamic ARIA attributes are set correctly when component state changes: that <code>aria-expanded</code> is <code>"true"</code> after toggling, that an error's <code>role="alert"</code> appears when a form field is invalid, or that <code>aria-disabled</code> is applied when expected.</p>
        <h3>Manual testing checklist</h3>
        <p>Unplug your mouse and complete key user journeys &mdash; login, add to cart, submit a form &mdash; using only Tab, Shift+Tab, Enter, Space, and arrow keys. Every interactive element should be reachable and operable. Then run VoiceOver (Mac/iOS), NVDA (Windows), or TalkBack (Android) and navigate your app to hear exactly what a screen reader user experiences.</p>
      `,
      "code": "// ---- Install axe-core for Angular ----\n// npm install --save-dev @axe-core/angular\n\n// main.ts — enable in development only\nimport { isDevMode } from '@angular/core';\n\nif (isDevMode()) {\n  import('@axe-core/angular').then(axe => {\n    axe.default(AppComponent, 1000); // logs violations to console\n  });\n}\n\n// ---- Unit testing ARIA attributes ----\nimport { ComponentFixture, TestBed } from '@angular/core/testing';\nimport { By } from '@angular/platform-browser';\nimport { AccordionComponent } from './accordion.component';\n\ndescribe('AccordionComponent — Accessibility', () => {\n  let fixture: ComponentFixture<AccordionComponent>;\n\n  beforeEach(async () => {\n    await TestBed.configureTestingModule({\n      imports: [AccordionComponent]\n    }).compileComponents();\n    fixture = TestBed.createComponent(AccordionComponent);\n    fixture.detectChanges();\n  });\n\n  it('should have aria-expanded=\"false\" initially', () => {\n    const btn = fixture.debugElement.query(By.css('button')).nativeElement;\n    expect(btn.getAttribute('aria-expanded')).toBe('false');\n  });\n\n  it('should set aria-expanded=\"true\" after click', () => {\n    const btn = fixture.debugElement.query(By.css('button')).nativeElement;\n    btn.click();\n    fixture.detectChanges();\n    expect(btn.getAttribute('aria-expanded')).toBe('true');\n  });\n\n  it('should have role=\"alert\" on error messages', () => {\n    // Trigger an error state...\n    fixture.componentInstance.showError = true;\n    fixture.detectChanges();\n    const alert = fixture.debugElement.query(By.css('[role=\"alert\"]'));\n    expect(alert).toBeTruthy();\n    expect(alert.nativeElement.textContent).toContain('required');\n  });\n\n  it('all images should have alt text', () => {\n    const images = fixture.debugElement.queryAll(By.css('img'));\n    images.forEach(img => {\n      // alt attribute must exist (empty string is valid for decorative images)\n      expect(img.nativeElement.hasAttribute('alt')).toBe(true);\n    });\n  });\n});",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Coverage — Automated vs Manual</p><div class="flex items-end justify-center gap-6 max-w-sm mx-auto text-xs"><div class="flex flex-col items-center"><div class="bg-indigo-100 border border-indigo-300 rounded-t-lg w-20" style="height:60px"></div><p class="font-bold text-indigo-700 mt-2 text-center">axe-core<br><span class="font-normal text-slate-500">~30-40%</span></p></div><div class="flex flex-col items-center"><div class="bg-emerald-100 border border-emerald-300 rounded-t-lg w-20" style="height:110px"></div><p class="font-bold text-emerald-700 mt-2 text-center">Manual keyboard + screen reader<br><span class="font-normal text-slate-500">the rest</span></p></div></div></div>`
    }
  ]
});
