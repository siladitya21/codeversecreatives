window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "data-binding",
  "title": "Data Binding",
  "icon": "bi bi-link-45deg",
  "questions": [
    {
      "id": "what-is-data-binding",
      "title": "What is data binding? Explain its types",
      "explanation": "<p><strong>Data binding</strong> is the mechanism that keeps the component class (TypeScript) and the template (HTML) in sync. Without it, you would have to manually query DOM elements and update them with JavaScript every time your data changes. Angular's binding syntax eliminates all of that.</p><h3>The four types</h3><ul><li><strong>Interpolation <code>{{ }}</code></strong> — embeds a component property as text in the template. One-way: class → template.</li><li><strong>Property binding <code>[prop]=\"value\"</code></strong> — sets a DOM property or component input from the class. One-way: class → template.</li><li><strong>Event binding <code>(event)=\"handler()\"</code></strong> — listens to a DOM event and calls a class method. One-way: template → class.</li><li><strong>Two-way binding <code>[(ngModel)]</code></strong> — combines property + event binding. Both directions at once.</li></ul><h3>Simple memory trick</h3><ul><li><code>{{ }}</code> — display text, class to template</li><li><code>[ ]</code> — square brackets, data flows INTO the element (class to template)</li><li><code>( )</code> — round brackets, events come OUT of the element (template to class)</li><li><code>[( )]</code> — both together, banana in a box</li></ul>",
      "code": "// Component class\n@Component({ selector: 'app-demo', templateUrl: './demo.component.html' })\nexport class DemoComponent {\n  title    = 'Angular Bindings';\n  isActive = true;\n  username = '';\n\n  handleClick(event: MouseEvent): void {\n    console.log('Clicked at:', event.clientX, event.clientY);\n  }\n}\n\n// demo.component.html\n\n// 1. Interpolation — class → template (text output)\n// <h1>{{ title }}</h1>\n\n// 2. Property binding — class → template (DOM property)\n// <button [disabled]=\"!isActive\">Submit</button>\n\n// 3. Event binding — template → class (user action)\n// <button (click)=\"handleClick($event)\">Click Me</button>\n\n// 4. Two-way binding — both directions simultaneously\n// <input [(ngModel)]=\"username\" />\n// <p>Hello, {{ username }}</p>",
      "language": "typescript"
    },
    {
      "id": "what-is-interpolation",
      "title": "What is interpolation?",
      "explanation": "<p><strong>Interpolation</strong> uses double curly braces <code>{{ expression }}</code> to embed a component value or expression as text directly into the HTML. It is one-way: data flows from the class to the template. Whenever the component property changes, Angular re-evaluates the expression and updates the displayed text automatically.</p><h3>What you can put inside {{ }}</h3><ul><li><strong>Properties</strong>: <code>{{ title }}</code>, <code>{{ user.name }}</code>, <code>{{ product.price }}</code></li><li><strong>Arithmetic expressions</strong>: <code>{{ price * quantity }}</code>, <code>{{ count + 1 }}</code></li><li><strong>Method calls</strong>: <code>{{ getFullName() }}</code>, <code>{{ formatDate(createdAt) }}</code></li><li><strong>Ternary operator</strong>: <code>{{ isLoggedIn ? 'Logout' : 'Login' }}</code></li><li><strong>Pipe transforms</strong>: <code>{{ name | uppercase }}</code>, <code>{{ price | currency }}</code></li></ul><h3>What you cannot do</h3><ul><li>Cannot assign: <code>{{ a = 5 }}</code> — not allowed</li><li>Cannot use <code>new</code>, <code>++</code>, <code>--</code></li><li>Cannot access global objects like <code>window</code> or <code>document</code> directly</li><li>Cannot use <code>console.log()</code></li></ul><h3>Important</h3><p>Interpolation always produces a <strong>string</strong>. For passing booleans or objects to DOM properties, use property binding <code>[ ]</code> instead.</p>",
      "code": "@Component({ selector: 'app-profile', templateUrl: './profile.component.html' })\nexport class ProfileComponent {\n  firstName  = 'Siladitya';\n  lastName   = 'Datta';\n  age        = 25;\n  price      = 49.99;\n  quantity   = 3;\n  isLoggedIn = true;\n  joinDate   = new Date('2024-01-15');\n\n  getFullName(): string {\n    return `${this.firstName} ${this.lastName}`;\n  }\n}\n\n// profile.component.html\n\n// Simple property\n// <h1>{{ firstName }}</h1>\n// → Siladitya\n\n// Arithmetic expression\n// <p>Total: {{ price * quantity | currency }}</p>\n// → Total: $149.97\n\n// Method call\n// <p>Welcome, {{ getFullName() }}!</p>\n// → Welcome, Siladitya Datta!\n\n// Ternary\n// <span>{{ isLoggedIn ? 'Logout' : 'Login' }}</span>\n// → Logout\n\n// Built-in pipes\n// <p>{{ firstName | uppercase }}</p>           → SILADITYA\n// <p>{{ joinDate | date:'mediumDate' }}</p>    → Jan 15, 2024\n// <p>{{ price | currency:'INR' }}</p>          → ₹49.99\n\n// Age calculation\n// <p>Age next year: {{ age + 1 }}</p>          → Age next year: 26",
      "language": "typescript"
    },
    {
      "id": "what-is-property-binding",
      "title": "What is property binding?",
      "explanation": "<p><strong>Property binding</strong> sets a DOM element's property (or a component/directive's <code>@Input()</code>) from a component class property. The syntax uses square brackets: <code>[domProperty]=\"classProperty\"</code>.</p><p>Data flows one-way — class → template. Angular watches the class property and updates the DOM property whenever it changes.</p><h3>Property binding vs interpolation</h3><p>This is a common interview question. The key difference:</p><ul><li>Interpolation (<code>{{ }}</code>) always produces a <strong>string</strong> and is used for text content between tags</li><li>Property binding sets the actual <strong>DOM property</strong> — it passes the real TypeScript value (boolean, number, object, array)</li></ul><p>The classic gotcha: <code>&lt;button disabled=\"{{ isDisabled }}\"&gt;</code> passes the <em>string</em> <code>\"false\"</code> — the attribute still exists, so the button stays disabled. <code>&lt;button [disabled]=\"isDisabled\"&gt;</code> passes the actual boolean <code>false</code> — correct behaviour.</p><h3>Common use cases</h3><ul><li>Enable/disable a button: <code>[disabled]=\"!form.valid\"</code></li><li>Set image source: <code>[src]=\"product.imageUrl\"</code></li><li>Toggle a CSS class: <code>[class.active]=\"isSelected\"</code></li><li>Set inline style: <code>[style.color]=\"status === 'error' ? 'red' : 'green'\"</code></li><li>Pass data to child component: <code>[user]=\"currentUser\"</code></li></ul>",
      "code": "@Component({ selector: 'app-demo', templateUrl: './demo.component.html' })\nexport class DemoComponent {\n  isFormValid  = false;\n  imageUrl     = 'https://example.com/photo.jpg';\n  isActive     = true;\n  isLoading    = false;\n  cardTitle    = 'Product Card';\n  statusColor  = 'green';\n}\n\n// demo.component.html\n\n// ✅ Correct: property binding passes the actual boolean\n// <button [disabled]=\"!isFormValid\">Submit</button>\n\n// ❌ Wrong: interpolation passes the string \"false\" — button stays disabled!\n// <button disabled=\"{{ isFormValid }}\">Submit</button>\n\n// Dynamic image src\n// <img [src]=\"imageUrl\" [alt]=\"cardTitle\" />\n\n// Toggle CSS class\n// <div [class.active]=\"isActive\">Dashboard</div>\n\n// Multiple classes with ngClass\n// <div [ngClass]=\"{ active: isActive, loading: isLoading }\">Content</div>\n\n// Inline style\n// <p [style.color]=\"statusColor\">Status</p>\n// <p [style.font-size.px]=\"16\">Fixed size</p>\n\n// Pass data to a child component input\n// <app-product-card [title]=\"cardTitle\"></app-product-card>",
      "language": "typescript"
    },
    {
      "id": "what-is-event-binding",
      "title": "What is event binding?",
      "explanation": "<p><strong>Event binding</strong> listens to DOM events and calls a method in the component class when they fire. The syntax uses parentheses: <code>(eventName)=\"handler()\"</code>. Data flows one-way — template → class.</p><p>This is how the user communicates with your component: a click, a keystroke, a form submission — all of these are events that you catch with event binding and respond to in your class.</p><h3>The $event object</h3><p>Angular provides a special variable <code>$event</code> inside event bindings. It holds the native browser event object — <code>MouseEvent</code>, <code>KeyboardEvent</code>, <code>InputEvent</code> — giving you access to coordinates, key codes, the input value, and more.</p><h3>Keyboard shortcut syntax</h3><p>Angular supports key combination shortcuts directly in the binding syntax: <code>(keyup.enter)</code> fires only when the Enter key is released. <code>(keyup.ctrl.s)</code> fires on Ctrl+S. No extra code needed.</p><h3>Common events</h3><ul><li><code>(click)</code>, <code>(dblclick)</code> — mouse clicks</li><li><code>(input)</code> — fires on every keystroke in an input</li><li><code>(change)</code> — fires when a select or checkbox value is committed</li><li><code>(submit)</code> — form submission</li><li><code>(keyup)</code>, <code>(keydown)</code>, <code>(keyup.enter)</code></li><li><code>(focus)</code>, <code>(blur)</code> — focus events</li></ul>",
      "code": "@Component({ selector: 'app-events', templateUrl: './events.component.html' })\nexport class EventsComponent {\n  counter    = 0;\n  searchTerm = '';\n  lastKey    = '';\n\n  increment(): void {\n    this.counter++;\n  }\n\n  // Full mouse event object via $event\n  onButtonClick(event: MouseEvent): void {\n    console.log('Clicked at X:', event.clientX, 'Y:', event.clientY);\n  }\n\n  // Read the typed value from $event.target\n  onSearch(event: Event): void {\n    this.searchTerm = (event.target as HTMLInputElement).value;\n  }\n\n  onKeyUp(event: KeyboardEvent): void {\n    this.lastKey = event.key;\n  }\n\n  onFormSubmit(event: Event): void {\n    event.preventDefault();  // prevent page reload\n    console.log('Submitted with search:', this.searchTerm);\n  }\n}\n\n// events.component.html\n\n// Simple click\n// <button (click)=\"increment()\">Count: {{ counter }}</button>\n\n// $event with mouse coordinates\n// <button (click)=\"onButtonClick($event)\">Where am I?</button>\n\n// Input — fires on every keystroke\n// <input (input)=\"onSearch($event)\" placeholder=\"Search...\" />\n// <p>You typed: {{ searchTerm }}</p>\n\n// Keyboard shortcut — fires ONLY on Enter key\n// <input (keyup.enter)=\"onFormSubmit($event)\" />\n\n// Form submit\n// <form (submit)=\"onFormSubmit($event)\">\n//   <button type=\"submit\">Submit</button>\n// </form>",
      "language": "typescript"
    },
    {
      "id": "what-is-two-way-binding",
      "title": "What is two-way data binding?",
      "explanation": "<p><strong>Two-way data binding</strong> keeps a component property and a template input perfectly in sync — automatically, in both directions simultaneously. When the user types in the input, the component property updates. When the component property changes in code, the input field updates. No manual event handler needed.</p><p>The syntax is <code>[(ngModel)]</code> — sometimes called the <strong>\"banana in a box\"</strong> because the parentheses <code>()</code> (banana) sit inside the square brackets <code>[]</code> (box).</p><h3>What [(ngModel)] actually is</h3><p>It is shorthand that Angular expands into:</p><ul><li><code>[ngModel]=\"name\"</code> — property binding: sets the input's value from the component</li><li><code>(ngModelChange)=\"name = $event\"</code> — event binding: updates the component when the input changes</li></ul><p>Both lines together — that's what <code>[(ngModel)]</code> does in one clean piece of syntax.</p><h3>Setup requirement</h3><p>You must import <code>FormsModule</code> (NgModule apps) or add it to the component's <code>imports</code> array (standalone). Without it, Angular throws a template error.</p><h3>When to use it</h3><p>Template-driven forms with simple inputs. For complex, multi-field forms with validation, prefer <strong>Reactive Forms</strong> (<code>FormGroup</code> / <code>FormControl</code>) — they give you much more control.</p>",
      "code": "// Standalone component setup\nimport { Component } from '@angular/core';\nimport { FormsModule } from '@angular/forms';\n\n@Component({\n  selector: 'app-profile-form',\n  standalone: true,\n  imports: [FormsModule],   // ← required for ngModel\n  templateUrl: './profile-form.component.html'\n})\nexport class ProfileFormComponent {\n  username   = 'Siladitya';  // initial value shown in input\n  email      = '';\n  country    = 'IN';\n  newsletter = false;\n\n  resetForm(): void {\n    this.username   = '';     // ← clearing the property also clears the input\n    this.email      = '';\n    this.newsletter = false;\n  }\n}\n\n// profile-form.component.html\n\n// Two-way: typing in input updates username; changing username in code updates input\n// <input [(ngModel)]=\"username\" placeholder=\"Username\" />\n// <p>Preview: {{ username }}</p>\n\n// Equivalent long form:\n// <input [ngModel]=\"username\" (ngModelChange)=\"username = $event\" />\n\n// Select dropdown\n// <select [(ngModel)]=\"country\">\n//   <option value=\"IN\">India</option>\n//   <option value=\"US\">United States</option>\n// </select>\n// <p>Selected: {{ country }}</p>\n\n// Checkbox\n// <input type=\"checkbox\" [(ngModel)]=\"newsletter\" />\n// <span>{{ newsletter ? 'Subscribed' : 'Not subscribed' }}</span>\n\n// Reset button clears ALL inputs instantly\n// <button (click)=\"resetForm()\">Reset</button>",
      "language": "typescript"
    },
    {
      "id": "one-way-vs-two-way",
      "title": "Difference between one-way and two-way binding",
      "explanation": "<p>The core difference is the <strong>direction data can flow</strong>.</p><h3>One-way binding</h3><p>Data moves in a single direction per binding. There are three forms:</p><ul><li><strong>Class → Template</strong>: interpolation <code>{{ }}</code> and property binding <code>[ ]</code>. Changing the class property updates the UI; changing the UI does <em>not</em> update the class.</li><li><strong>Template → Class</strong>: event binding <code>( )</code>. User actions call class methods; class properties are not automatically set.</li></ul><p>One-way binding is <strong>explicit</strong>. You know exactly where data comes from and where it goes. This makes the data flow easy to trace and debug.</p><h3>Two-way binding</h3><p>Data flows in both directions simultaneously via <code>[(ngModel)]</code>. Typing in the input updates the class property. Changing the class property (e.g. from a button click) updates the input value. Both sync automatically.</p><h3>When to use which</h3><ul><li>Use <strong>one-way binding</strong> for the vast majority of your app — displaying data, passing inputs to child components, and handling events separately</li><li>Use <strong>two-way binding</strong> for simple form inputs where you want zero boilerplate (template-driven forms)</li><li>For anything complex — multi-field forms with validation, async data, dynamic fields — use <strong>Reactive Forms</strong> instead</li></ul>",
      "code": "// ─── ONE-WAY: three separate bindings ─────────────────────────\n@Component({\n  selector: 'app-one-way',\n  template: `\n    <!-- Class → Template: displays message, does not update on user input -->\n    <p>{{ message }}</p>\n    <button [disabled]=\"isLoading\">Save</button>\n\n    <!-- Template → Class: user input calls method, but does not auto-update 'message' -->\n    <input (input)=\"onInput($event)\" />\n  `\n})\nexport class OneWayComponent {\n  message   = 'Hello World';\n  isLoading = false;\n\n  onInput(e: Event): void {\n    // Must manually update message to show what the user typed\n    this.message = (e.target as HTMLInputElement).value;\n  }\n}\n\n// ─── TWO-WAY: one [(ngModel)] does both ───────────────────────\n@Component({\n  selector: 'app-two-way',\n  imports: [FormsModule],\n  template: `\n    <!-- ngModel syncs BOTH ways automatically -->\n    <input [(ngModel)]=\"message\" />\n    <p>Live preview: {{ message }}</p>\n\n    <!-- If you programmatically change message, the input updates too -->\n    <button (click)=\"message = 'Reset!'\">Reset</button>\n  `\n})\nexport class TwoWayComponent {\n  message = 'Hello World';\n  // No onInput() handler needed — ngModel handles it\n}\n\n// ─── Quick comparison ─────────────────────────────────────────\n// Feature         One-way              Two-way\n// Syntax          {{ }} / [] / ()      [(ngModel)]\n// Direction       Single               Both\n// FormsModule     Not needed           Required\n// Best for        Display & events     Form inputs\n// Traceability    Easy to follow       Can be harder in large forms",
      "language": "typescript"
    }
  ]
});
