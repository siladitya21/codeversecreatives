window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "performance-optimization",
  "title": "Performance Optimization",
  "icon": "bi bi-speedometer2",
  "questions": [
    {
      "id": "how-to-optimize-angular",
      "title": "How to optimize Angular applications?",
      "explanation": "<p>Angular performance breaks into two distinct problems:</p><ul><li><strong>Load performance</strong> — how fast the app starts up (initial download, parse, render)</li><li><strong>Runtime performance</strong> — how smooth the app feels while running (how fast it reacts to user input)</li></ul><p>They require different solutions.</p><h3>Load performance strategies</h3><ul><li><strong>Lazy loading</strong> — only download code for the current route; other routes load on demand</li><li><strong>AOT compilation</strong> — compile templates at build time, not at runtime in the browser</li><li><strong>Tree shaking</strong> — the build process removes all unused code from the bundle</li><li><strong>Bundle analysis</strong> — find out what is actually in your bundle and cut unnecessary dependencies</li><li><strong>Preloading strategy</strong> — after the initial load, silently prefetch other routes in the background</li></ul><h3>Runtime performance strategies</h3><ul><li><strong>OnPush change detection</strong> — tell Angular to skip components whose inputs haven't changed</li><li><strong>trackBy in *ngFor</strong> — prevent DOM recreation for unchanged list items</li><li><strong>async pipe</strong> — let Angular manage subscriptions and only update the view when data actually changes</li><li><strong>runOutsideAngular</strong> — keep high-frequency operations (animations, scroll events) outside Angular's change detection zone</li></ul>",
      "code": "// ─── Quick checklist for optimizing a production Angular app ───\n\n// LOAD TIME\n// ✅ ng build --configuration production  (AOT + minification + tree shaking)\n// ✅ Lazy load every feature route with loadComponent() or loadChildren()\n// ✅ Analyze bundle: ng build --stats-json && npx webpack-bundle-analyzer\n// ✅ Avoid importing full libraries — import specific functions\n//    ❌  import * as _ from 'lodash';\n//    ✅  import { debounce } from 'lodash-es';\n\n// RUNTIME\n// ✅ OnPush on all display/list components\n// ✅ trackBy on every *ngFor with dynamic data\n// ✅ Use async pipe instead of manual subscribe()\n// ✅ Run heavy non-Angular operations outside NgZone\n// ✅ Use virtual scrolling (CDK) for lists > 100 items",
      "language": "typescript"
    },
    {
      "id": "what-is-lazy-loading",
      "title": "What is lazy loading?",
      "explanation": "<p><strong>Lazy loading</strong> means Angular only downloads the JavaScript code for a route when the user actually navigates to it. Without lazy loading, all feature code is bundled together and downloaded at startup — even code for screens the user may never visit.</p><h3>Why it matters</h3><p>Imagine an e-commerce app with: a product list, a product detail, a checkout flow, an admin panel, and a user profile. Without lazy loading, a customer browsing products downloads the admin panel code too — wasted bytes. With lazy loading, the admin code is only downloaded if someone visits <code>/admin</code>.</p><h3>loadComponent vs loadChildren</h3><ul><li><code>loadComponent</code> — lazily load a single standalone component (the modern approach)</li><li><code>loadChildren</code> — lazily load an entire feature's routes file (a group of routes and their components). Better when a feature has many sub-routes.</li></ul><h3>How to measure the improvement</h3><p>Run <code>ng build --stats-json</code> before and after adding lazy loading. Look at the initial chunk size — it should decrease significantly.</p>",
      "code": "// app.routes.ts\nimport { Routes } from '@angular/router';\n\nexport const routes: Routes = [\n  // ─── Eagerly loaded (always in the main bundle) ────────────\n  { path: 'home', loadComponent: () => import('./home/home.component').then(m => m.HomeComponent) },\n\n  // ─── Lazily loaded — downloaded only when user visits /products ─\n  {\n    path: 'products',\n    loadComponent: () =>\n      import('./products/products.component').then(m => m.ProductsComponent)\n  },\n\n  // ─── Lazy feature group — entire /admin section with sub-routes ─\n  {\n    path: 'admin',\n    canMatch: [adminGuard],   // don't even download the chunk if not admin\n    loadChildren: () =>\n      import('./admin/admin.routes').then(m => m.adminRoutes)\n    // admin.routes.ts defines /admin/users, /admin/reports, etc.\n  },\n\n  { path: '**', loadComponent: () => import('./not-found.component').then(m => m.NotFoundComponent) }\n];\n\n// ─── The result in Chrome DevTools Network tab ─────────────────\n// Initial load: main.js (small — only home + shell code)\n// When user visits /products: products-chunk.js downloads automatically\n// When user visits /admin (if they're admin): admin-chunk.js downloads",
      "language": "typescript"
    },
    {
      "id": "what-is-aot",
      "title": "What is ahead-of-time (AOT) compilation?",
      "explanation": "<p>Angular templates (<code>{{ name }}</code>, <code>*ngFor</code>, <code>[class.active]</code>, etc.) are not standard HTML — they need to be compiled into JavaScript that can manipulate the DOM. This compilation can happen at two different times.</p><h3>JIT (Just-in-Time) — compile in the browser at runtime</h3><p>The Angular compiler is included in the bundle and runs in the user's browser when the app starts. This is what happens during <code>ng serve</code> (development). Disadvantages: the compiler itself is large (~150KB), startup is slower, and template errors only surface at runtime.</p><h3>AOT (Ahead-of-Time) — compile at build time</h3><p>The Angular compiler runs during <code>ng build</code>, on your machine. The browser receives pre-compiled JavaScript — there is no compiler in the bundle and no compilation step at startup.</p><h3>Benefits of AOT</h3><ul><li><strong>Faster startup</strong> — no compilation in the browser</li><li><strong>Smaller bundle</strong> — the compiler (~150KB) is not shipped</li><li><strong>Catch errors early</strong> — template typos and type errors are caught at build time, not at runtime in a user's browser</li><li><strong>More secure</strong> — no dynamic template evaluation at runtime</li></ul><p>AOT is enabled automatically with <code>ng build</code> (production). JIT is only used internally by <code>ng serve</code> for fast rebuilds during development.</p>",
      "code": "// ─── Build commands ────────────────────────────────────────────\n// Development (JIT, with source maps for debugging):\n// ng serve\n\n// Production (AOT + minification + tree shaking + source map removal):\n// ng build --configuration production\n// OR with Angular CLI:\n// ng build  (production is the default for 'ng build' since Angular 12)\n\n// ─── What AOT catches that JIT misses ─────────────────────────\n\n// Template type error — caught by AOT at build time:\n@Component({\n  template: `<p>{{ user.nonExistentProperty }}</p>`  // ❌ Build error with AOT\n})\nexport class ExampleComponent {\n  user: { name: string } = { name: 'Alice' };\n  // 'nonExistentProperty' doesn't exist on the type — AOT flags this\n}\n\n// Angular.json — production configuration (AOT is on by default):\n// \"configurations\": {\n//   \"production\": {\n//     \"budgets\": [\n//       { \"type\": \"initial\", \"maximumWarning\": \"500kB\", \"maximumError\": \"1MB\" }\n//     ],\n//     \"outputHashing\": \"all\"\n//   }\n// }\n\n// The 'budgets' section warns you if your bundle exceeds a size limit.",
      "language": "typescript"
    },
    {
      "id": "what-is-tree-shaking",
      "title": "What is tree shaking?",
      "explanation": "<p><strong>Tree shaking</strong> is an automated build step that removes code you import but never actually use. The term comes from shaking a tree to make dead leaves fall out.</p><h3>How it works</h3><p>Modern JavaScript (ES Modules) uses static <code>import</code> and <code>export</code> statements. Because these are static (not dynamic), a build tool like Webpack or esbuild can analyse the import graph and determine exactly which exported functions are actually called somewhere in your code — and drop the ones that aren't.</p><h3>Example</h3><p>You import <code>map</code> from RxJS. Tree shaking ensures only the <code>map</code> operator's code is in your bundle — not the hundreds of other operators you didn't use.</p><h3>What breaks tree shaking</h3><ul><li>Side-effectful modules that run code on import — Angular marks these with <code>\"sideEffects\": false</code> in package.json to help the bundler</li><li>Dynamic imports like <code>require()</code> — the bundler can't statically analyse these</li><li>Importing entire namespaces: <code>import * as _ from 'lodash'</code> — the bundler can't tell which parts you use</li></ul>",
      "code": "// ─── Tree shaking in practice ──────────────────────────────────\n\n// ❌ Imports the ENTIRE lodash library (~70KB)\nimport * as _ from 'lodash';\nconst result = _.debounce(fn, 300);\n\n// ✅ Imports ONLY the debounce function — everything else is shaken out\nimport { debounce } from 'lodash-es';   // ES module version supports tree shaking\nconst result = debounce(fn, 300);\n\n// ─── RxJS — automatically tree-shaken ─────────────────────────\nimport { map, filter } from 'rxjs';\n// Only 'map' and 'filter' code is included in the bundle.\n// The 200+ other RxJS operators are shaken out automatically.\n\n// ─── Angular itself is fully tree-shakeable ────────────────────\n// If you never use FormBuilder, its code is not in your bundle.\n// If you never use the DatePipe, it's not in your bundle.\n// This is why Angular standalone components improve bundle size —\n// you import exactly what each component needs.\n\n// ─── Check what's in your bundle ──────────────────────────────\n// 1. ng build --stats-json\n// 2. npx webpack-bundle-analyzer dist/<app-name>/stats.json\n// This opens a visual treemap showing every dependency by size.",
      "language": "typescript"
    },
    {
      "id": "what-is-trackby",
      "title": "What is trackBy in *ngFor?",
      "explanation": "<p>By default, when the array bound to an <code>*ngFor</code> changes, Angular cannot tell which items are new and which already exist — so it <strong>destroys and recreates all DOM elements</strong> in the list. For a list of 100 items, that's 100 DOM operations just to update one item's name.</p><h3>How trackBy fixes this</h3><p><code>trackBy</code> gives Angular a way to identify each item uniquely (typically by ID). When the array changes, Angular compares identities: items with the same ID are reused (their DOM nodes are moved or updated in place), only new or removed items trigger DOM creation/deletion.</p><h3>When does it matter?</h3><p>If the list is static (never reordered, never items added/removed), trackBy makes no difference. The real gains are when:</p><ul><li>You refresh the list from an API call (a common pattern — the new array has the same items but new object references)</li><li>The list is long (50+ items)</li><li>List items contain complex child components or heavy DOM</li></ul>",
      "code": "// ─── Without trackBy — Angular destroys and recreates ALL 100 items\n// even if only one changed:\n// <li *ngFor=\"let user of users\">{{ user.name }}</li>\n\n// ─── With trackBy — Angular reuses DOM nodes for unchanged items ─\n// Template:\n// <ul>\n//   <li *ngFor=\"let user of users; trackBy: trackByUserId\">\n//     {{ user.name }}\n//   </li>\n// </ul>\n\n@Component({\n  selector: 'app-users-list',\n  template: `\n    <ul>\n      <li *ngFor=\"let user of users; trackBy: trackByUserId\"\n          [class.highlighted]=\"user.id === selectedId\">\n        {{ user.name }} — {{ user.email }}\n      </li>\n    </ul>\n  `\n})\nexport class UsersListComponent {\n  users: User[] = [];\n  selectedId?: number;\n\n  // trackBy function — receives the index and the item, returns a unique key\n  trackByUserId(index: number, user: User): number {\n    return user.id;   // Angular uses this to identify items across array changes\n  }\n}\n\n// ─── Practical example — refreshing a list from an API ─────────\n// Without trackBy: refreshing users calls API, returns same users with\n// new object references → Angular destroys ALL 100 <li> elements and recreates them\n//\n// With trackBy: same refresh → Angular sees same IDs → no DOM nodes destroyed\n// Only items whose data actually changed get their text updated in place.\n// For a list of 100 items, this is 100× less DOM work.",
      "language": "typescript"
    },
    {
      "id": "what-is-onpush",
      "title": "OnPush change detection strategy",
      "explanation": "<p>The <strong>OnPush</strong> strategy is the single most impactful runtime performance optimization in Angular. It tells Angular: \"Don't check this component on every cycle — only check it when its data might actually have changed.\"</p><h3>Default vs OnPush</h3><p>With Default, a single click on any button in the app triggers Angular to check <em>every component in the tree</em>. With OnPush on most components, Angular skips entire subtrees whose inputs haven't changed — reducing checks by 80–90% in a complex app.</p><h3>The four OnPush triggers</h3><ol><li>An <code>@Input()</code> property receives a new object reference</li><li>An event originating inside the component fires (click, input, etc.)</li><li>An Observable bound with <code>async</code> pipe emits</li><li><code>cdr.markForCheck()</code> is called manually</li></ol><h3>The golden rule for OnPush</h3><p>Never mutate objects in place. Always return new references: <code>this.user = { ...this.user, name: 'x' }</code> instead of <code>this.user.name = 'x'</code>. This is called <strong>immutability</strong> and it's what makes OnPush work reliably.</p>",
      "code": "import { Component, Input, ChangeDetectionStrategy } from '@angular/core';\n\n// ─── Good candidate for OnPush: a display-only component ───────\n@Component({\n  selector: 'app-product-card',\n  template: `\n    <div class=\"card\">\n      <h3>{{ product.name }}</h3>\n      <p>{{ product.price | currency }}</p>\n      <button (click)=\"onAddToCart()\">Add to Cart</button>\n    </div>\n  `,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class ProductCardComponent {\n  @Input() product!: Product;\n  // ✅ Angular skips this component unless 'product' reference changes\n  // ✅ Click event on the button still triggers CD for this component\n\n  onAddToCart() {\n    console.log('Added:', this.product.name);\n  }\n}\n\n// ─── Parent updates product the OnPush-safe way ────────────────\nexport class ProductListComponent {\n  products: Product[] = [];\n\n  updatePrice(id: number, newPrice: number) {\n    // ❌ WRONG — mutates in place, OnPush child won't re-render\n    // this.products.find(p => p.id === id)!.price = newPrice;\n\n    // ✅ CORRECT — replace the changed item with a new reference\n    this.products = this.products.map(p =>\n      p.id === id ? { ...p, price: newPrice } : p\n    );\n    // Now Angular sees a new 'product' input reference in the child → triggers check\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "how-to-reduce-bundle-size",
      "title": "How to reduce bundle size?",
      "explanation": "<p>Bundle size directly affects load time, especially on mobile networks. Here is a systematic approach to reducing it.</p><h3>Step 1: Analyze what's in your bundle</h3><p>You can't optimize what you can't see. Use <code>webpack-bundle-analyzer</code> to get a visual treemap of every package in your bundle, sorted by size. You will immediately see which dependencies are largest and whether any are accidentally included multiple times.</p><h3>Step 2: Lazy load features</h3><p>The single biggest win. Move every feature route to <code>loadComponent()</code> or <code>loadChildren()</code>. This splits your bundle into chunks that download on demand.</p><h3>Step 3: Audit your dependencies</h3><p>Common culprits: <code>moment.js</code> (~300KB), importing all of <code>lodash</code> (~70KB), icon libraries with thousands of icons when you use ten. Replace Moment with the built-in <code>DatePipe</code> or <code>date-fns</code>. Use named imports from <code>lodash-es</code>. Import only the specific icons you use.</p><h3>Step 4: Use production build</h3><p><code>ng build</code> (production config) enables: AOT compilation, minification, dead code elimination, and file hashing for optimal caching.</p>",
      "code": "// ─── Step 1: Analyze the bundle ───────────────────────────────\n// ng build --stats-json\n// npx webpack-bundle-analyzer dist/<app-name>/stats.json\n// This opens a visual page showing every module by size.\n\n// ─── Step 2: Check Angular's built-in budget warnings ─────────\n// angular.json — set budgets to get warnings/errors if you exceed limits\n// \"budgets\": [\n//   { \"type\": \"initial\",    \"maximumWarning\": \"500kB\", \"maximumError\": \"1MB\" },\n//   { \"type\": \"anyComponentStyle\", \"maximumWarning\": \"4kB\" }\n// ]\n\n// ─── Step 3: Replace heavy packages ──────────────────────────\n// ❌ moment.js — 300KB, affects every app that uses it\nimport * as moment from 'moment';\nconst formatted = moment().format('DD/MM/YYYY');\n\n// ✅ Angular's built-in DatePipe — zero extra bytes\n// {{ today | date:'dd/MM/yyyy' }}\n// Or in component class:\n// inject(DatePipe).transform(new Date(), 'dd/MM/yyyy')\n\n// ─── Step 4: Named imports from lodash-es (tree-shakeable) ─────\n// ❌ Pulls in the entire lodash library\nimport * as _ from 'lodash';\n\n// ✅ Only the specific function is bundled\nimport { debounce } from 'lodash-es';\n\n// ─── Step 5: Import only the icons you use ─────────────────────\n// ❌ Font Awesome full library\nimport { library } from '@fortawesome/fontawesome-svg-core';\nimport { fas } from '@fortawesome/free-solid-svg-icons';  // ALL solid icons — huge\n\n// ✅ Import only what you need\nimport { faUser, faCart } from '@fortawesome/free-solid-svg-icons';\nlibrary.add(faUser, faCart);",
      "language": "typescript"
    },
    {
      "id": "what-is-preloading",
      "title": "What is preloading strategy?",
      "explanation": "<p><strong>Preloading</strong> bridges the gap between eager loading (everything downloaded upfront, slow start) and lazy loading (instant start, but a noticeable delay the first time you visit a route).</p><h3>How it works</h3><p>After the initial bundle loads and the app is interactive, Angular silently downloads the other lazy chunks in the background while the user is reading the first page. By the time they click a navigation link, the code is already in the browser cache — so navigation feels instant.</p><h3>Built-in strategies</h3><ul><li><strong>NoPreloading</strong> (default) — no background prefetching; each lazy chunk downloads on first navigation</li><li><strong>PreloadAllModules</strong> — prefetch all lazy chunks after initial load. Simple, works well for small-to-medium apps.</li></ul><h3>Custom preloading strategy</h3><p>For large apps with many features, preloading everything wastes bandwidth (especially on mobile). A custom strategy lets you annotate specific routes with <code>data: { preload: true }</code> and only preload those.</p>",
      "code": "import { Routes, PreloadAllModules, RouterModule, PreloadingStrategy, Route } from '@angular/router';\nimport { Injectable } from '@angular/core';\nimport { Observable, of } from 'rxjs';\n\n// ─── Option 1: PreloadAllModules — prefetch everything ─────────\n// main.ts (standalone app)\nbootstrapApplication(AppComponent, {\n  providers: [\n    provideRouter(routes, withPreloading(PreloadAllModules))\n  ]\n});\n\n// ─── Option 2: Custom strategy — preload only flagged routes ───\n// Mark specific routes in app.routes.ts:\nconst routes: Routes = [\n  {\n    path: 'products',\n    data: { preload: true },   // ← flag this route for preloading\n    loadComponent: () => import('./products.component').then(m => m.ProductsComponent)\n  },\n  {\n    path: 'admin',\n    data: { preload: false },  // ← don't preload admin (most users won't visit)\n    loadChildren: () => import('./admin/admin.routes').then(m => m.adminRoutes)\n  }\n];\n\n// Custom preloading strategy service:\n@Injectable({ providedIn: 'root' })\nexport class SelectivePreloadingStrategy implements PreloadingStrategy {\n  preload(route: Route, load: () => Observable<any>): Observable<any> {\n    // Only preload routes marked with data.preload === true\n    return route.data?.['preload'] ? load() : of(null);\n  }\n}\n\n// Register:\n// provideRouter(routes, withPreloading(SelectivePreloadingStrategy))",
      "language": "typescript"
    }
  ]
});
