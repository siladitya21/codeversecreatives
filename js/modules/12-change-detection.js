window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "change-detection",
  "title": "Change Detection",
  "icon": "bi bi-lightning-charge",
  "questions": [
    {
      "id": "what-is-change-detection",
      "title": "What is change detection?",
      "explanation": "<p><strong>Change detection</strong> is Angular's process of keeping the DOM in sync with your component's data. Whenever data in a component changes, Angular needs to find out what changed and update the relevant parts of the HTML.</p><h3>The problem it solves</h3><p>In plain JavaScript, if you do <code>this.userName = 'Alice'</code>, the browser has no idea you changed a variable — you'd have to manually find the DOM element and update it. Angular's change detection does this automatically: you change the data, Angular figures out what the DOM should now look like, and updates it.</p><h3>How Angular triggers it</h3><p>Angular doesn't continuously watch your data (that would be too slow). Instead, it runs change detection at specific moments — whenever something asynchronous happens that <em>might</em> have changed state:</p><ul><li>A browser event fires (click, input, scroll, keypress)</li><li>An HTTP response arrives</li><li>A timer (<code>setTimeout</code> / <code>setInterval</code>) fires</li><li>A Promise or Observable resolves</li></ul><p>Zone.js (covered next) is the mechanism that detects all of these and tells Angular to run a check.</p><h3>What Angular checks</h3><p>Angular walks the component tree from the root downward, checks each component's template bindings against current values, and updates any DOM nodes whose value has changed.</p>",
      "code": "// When you update a property:\nthis.userName = 'Alice';\n\n// Angular detects this change (triggered by an event or async op)\n// and updates any template binding that uses userName:\n// <h1>{{ userName }}</h1>  ← Angular updates this DOM node\n\n// ─── Angular also checks object/array bindings ─────────────────\nthis.user = { ...this.user, name: 'Alice' };   // ✅ NEW reference — CD detects it\nthis.user.name = 'Alice';                       // ✅ CD detects mutations for Default strategy\n                                                // ❌ OnPush strategy misses this (same reference)\n\n// ─── Strings, numbers, booleans — always detected ─────────────\nthis.count = 42;      // ✅\nthis.loading = true;  // ✅\nthis.title = 'New';   // ✅",
      "language": "typescript"
    },
    {
      "id": "how-does-cd-work",
      "title": "How does change detection work?",
      "explanation": "<p>Angular maintains a <strong>component tree</strong>. Change detection always starts at the root component and travels downward through every child component, in order — this is called a change detection <strong>cycle</strong>.</p><h3>What happens in each cycle</h3><p>For each component, Angular compares the current value of every template expression (e.g. <code>{{ user.name }}</code>, <code>[disabled]=\"form.invalid\"</code>) against the value it recorded in the previous cycle. If they differ, Angular updates that DOM node. This comparison is called <strong>dirty checking</strong>.</p><h3>When does a cycle run?</h3><p>A cycle runs after every asynchronous event that Zone.js intercepts — a click, a timer callback, an HTTP response completing. After the event handler finishes, Zone.js signals Angular: \"Something might have changed, run a cycle.\"</p><h3>Unidirectional data flow</h3><p>Angular enforces a top-down flow: parent components pass data down to children. This means Angular only needs to walk the tree in one direction (top to bottom) per cycle, which is fast and predictable. Angular will throw an error in dev mode (<em>ExpressionChangedAfterItHasBeenChecked</em>) if a child component tries to change a parent's binding during the same cycle.</p>",
      "code": "// Conceptual mental model of one change detection cycle:\n\n// 1. AppComponent  — check bindings, update DOM if needed\n//    ↓\n// 2. NavbarComponent  — check bindings, update DOM if needed\n//    ↓\n// 3. DashboardComponent  — check bindings, update DOM if needed\n//    ↓\n// 4. UserCardComponent  — check bindings, update DOM if needed\n//    ↓\n// 5. LeafComponent  — check bindings, update DOM if needed\n\n// Angular walks ALL components each cycle by default.\n// In a large app with 200 components, this runs 200 checks per click.\n// This is why OnPush strategy exists — to skip unchanged subtrees.\n\n// ─── The ExpressionChangedAfterItHasBeenChecked error ──────────\n// This error means something is changing data AFTER Angular already\n// checked and rendered it in the same cycle:\n\nngAfterViewInit() {\n  // ❌ This changes a template-bound property after the view was rendered\n  // this.title = 'Changed';  ← causes the error in dev mode\n\n  // ✅ Use setTimeout to push the change to the NEXT cycle\n  setTimeout(() => this.title = 'Changed');\n}",
      "language": "typescript"
    },
    {
      "id": "what-is-zone-js",
      "title": "What is Zone.js?",
      "explanation": "<p><strong>Zone.js</strong> is a library that <em>monkey-patches</em> (wraps) all asynchronous browser APIs — <code>setTimeout</code>, <code>setInterval</code>, <code>addEventListener</code>, <code>fetch</code>, Promise callbacks, and more.</p><h3>What monkey-patching means</h3><p>When Zone.js loads, it replaces the native <code>setTimeout</code> with its own version. Your code calls <code>setTimeout</code>, but you're actually calling Zone.js's wrapper. The wrapper does two things: calls the real <code>setTimeout</code>, and also hooks in to know when the callback fires.</p><h3>Why Angular needs this</h3><p>Angular has no way of knowing when your data changes inside a <code>setTimeout</code> or an event listener. Zone.js solves this: when any async callback finishes executing, Zone.js notifies Angular's root zone, which triggers a change detection cycle. This is how Angular \"just works\" — you don't call any update function; Zone.js does it for you.</p><h3>The future without Zone.js</h3><p>Angular Signals (introduced in v16) allow Angular to know exactly which component needs re-rendering, without needing Zone.js to notify it. Future Angular versions aim to make Zone.js optional.</p>",
      "code": "// ─── Zone.js intercepts all async operations ──────────────────\n\n// setTimeout — Zone.js wraps this\nsetTimeout(() => {\n  this.message = 'Timer fired!';  // Zone.js detects this callback finished\n                                  // → triggers Angular change detection\n                                  // → DOM updates to show new message\n}, 2000);\n\n// Event listener — also intercepted\ndocument.querySelector('button')?.addEventListener('click', () => {\n  this.count++;  // Zone.js intercepts this event → triggers CD\n});\n\n// ─── Running outside NgZone (performance optimization) ─────────\n// If you have a high-frequency operation (mouse move, requestAnimationFrame)\n// that does NOT change Angular bindings, running it inside Angular's zone\n// triggers needless CD on every event. Run it OUTSIDE the zone:\n\nimport { NgZone } from '@angular/core';\n\nconstructor(private ngZone: NgZone) {}\n\nsetupChart() {\n  // This runs hundreds of times per second — keep CD out of it\n  this.ngZone.runOutsideAngular(() => {\n    requestAnimationFrame(this.drawFrame.bind(this));\n  });\n}\n\nupdateAngularData(value: number) {\n  // When you DO need Angular to update, re-enter the zone:\n  this.ngZone.run(() => {\n    this.chartValue = value;  // now CD will pick this up\n  });\n}",
      "language": "typescript"
    },
    {
      "id": "cd-strategies",
      "title": "Change detection strategies (Default vs OnPush)",
      "explanation": "<p>Angular provides two strategies that control how frequently a component is checked during change detection cycles.</p><h3>Default (CheckAlways)</h3><p>Angular checks the component on <em>every</em> change detection cycle, regardless of what triggered it. Safe and simple, but in a large app with hundreds of components, every user click runs hundreds of checks — most of them unnecessary.</p><h3>OnPush</h3><p>Angular checks the component <em>only</em> when specific conditions are met — essentially only when its data might actually have changed. Angular skips the entire component subtree otherwise. This can dramatically reduce the number of checks per cycle.</p><h3>OnPush triggers a check when</h3><ol><li>An <code>@Input()</code> property receives a <strong>new object reference</strong></li><li>An <strong>event handler</strong> inside the component fires (click, input, etc.)</li><li>An Observable bound with the <strong>async pipe</strong> emits a new value</li><li>You call <code>markForCheck()</code> or <code>detectChanges()</code> manually on <code>ChangeDetectorRef</code></li></ol><h3>OnPush and immutability</h3><p>OnPush only detects <em>reference changes</em>. Mutating an object (<code>this.user.name = 'x'</code>) while keeping the same reference will <em>not</em> trigger a check. You must replace the reference: <code>this.user = { ...this.user, name: 'x' }</code>.</p>",
      "code": "import { Component, Input, ChangeDetectionStrategy } from '@angular/core';\n\n// ─── Default strategy ──────────────────────────────────────────\n// Checked on EVERY cycle — safe, zero extra effort\n@Component({\n  selector: 'app-header',\n  template: `<h1>{{ title }}</h1>`,\n  changeDetection: ChangeDetectionStrategy.Default   // implicit if omitted\n})\nexport class HeaderComponent {\n  title = 'Dashboard';\n}\n\n// ─── OnPush strategy ───────────────────────────────────────────\n// Checked only when inputs change reference, events fire, or async pipe emits\n@Component({\n  selector: 'app-user-card',\n  template: `\n    <div>{{ user.name }}</div>\n    <div>{{ user.email }}</div>\n    <button (click)=\"onSelect()\">Select</button>\n  `,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class UserCardComponent {\n  @Input() user!: User;   // CD runs only when a NEW User object is passed\n\n  onSelect() {\n    // Events inside OnPush components DO trigger CD for this component\n    console.log('Selected:', this.user.name);\n  }\n}\n\n// ─── Parent must replace the reference for OnPush to detect the change ──\n// ❌ This mutates in place — OnPush child won't re-render\n// this.currentUser.name = 'Alice';\n\n// ✅ Replace the reference — OnPush child WILL re-render\n// this.currentUser = { ...this.currentUser, name: 'Alice' };",
      "language": "typescript"
    },
    {
      "id": "default-strategy",
      "title": "When to use Default strategy?",
      "explanation": "<p>The <strong>Default</strong> strategy (also called <code>CheckAlways</code>) checks the component on every change detection cycle, no matter what. Angular always walks through it.</p><h3>When to use it</h3><ul><li><strong>Small applications</strong> where performance isn't a concern yet</li><li>Components that <strong>mutate objects in place</strong> and can't easily switch to immutable patterns</li><li>Components that <strong>receive data from imperative code</strong> (third-party libraries, WebSockets that push mutations) where you don't control how state is updated</li><li>During prototyping — use Default to get things working, then optimize with OnPush later</li></ul><h3>The trade-off</h3><p>Default is convenient but scales poorly. In an app with 300 components and Default everywhere, a single click triggers 300 template checks. Most checks find nothing has changed — they're wasted. This is why large, production Angular apps use OnPush on most components.</p>",
      "code": "// Default strategy — no extra code needed, it's the implicit behaviour\n@Component({\n  selector: 'app-notification-banner',\n  template: `<div *ngIf=\"show\">{{ message }}</div>`,\n  // changeDetection: ChangeDetectionStrategy.Default  // ← same as not writing it at all\n})\nexport class NotificationBannerComponent {\n  show = false;\n  message = '';\n\n  // Even mutations are picked up:\n  push(msg: string) {\n    this.message = msg;   // Angular will detect this on the next cycle\n    this.show = true;\n  }\n}\n\n// ─── Real-world: component fed by a third-party WebSocket library\nexport class StockTickerComponent implements OnInit {\n  price = 0;\n\n  ngOnInit() {\n    // A third-party lib calls a callback with new data\n    // We have no control over reference replacement here\n    thirdPartyStockFeed.onUpdate((newPrice: number) => {\n      this.price = newPrice;   // Default strategy picks this up automatically\n    });\n    // With OnPush, we'd need markForCheck() here — Default just works\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "onpush-strategy",
      "title": "OnPush strategy — real-world usage",
      "explanation": "<p><strong>OnPush</strong> is the performance-critical strategy. Angular skips the component and all its children unless one of the four trigger conditions is met. In a large app, this can cut the number of template checks by 80–90% per cycle.</p><h3>Best for</h3><ul><li><strong>Pure presentational (dumb) components</strong> — components that only display data they receive via <code>@Input()</code> and don't manage their own state</li><li><strong>List items</strong> — e.g. <code>*ngFor</code> renders 100 UserCardComponents; with OnPush, Angular only re-checks the cards whose input reference changed</li><li><strong>Components that use the async pipe</strong> — the async pipe calls <code>markForCheck()</code> automatically when the Observable emits</li></ul><h3>Using OnPush with services</h3><p>When an OnPush component needs to react to data from a service (not via <code>@Input()</code>), use an Observable with the async pipe — this is the cleanest pattern. Alternatively, inject <code>ChangeDetectorRef</code> and call <code>markForCheck()</code> after updating data.</p>",
      "code": "import { Component, Input, ChangeDetectionStrategy, ChangeDetectorRef, OnInit } from '@angular/core';\nimport { Observable } from 'rxjs';\nimport { NotificationService } from './notification.service';\n\n// ─── Pattern 1: Pure presentational component with @Input ───────\n@Component({\n  selector: 'app-user-card',\n  template: `<p>{{ user.name }} — {{ user.role }}</p>`,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class UserCardComponent {\n  @Input() user!: User;\n  // Angular re-checks ONLY when a new User object reference is passed.\n  // Perfect for large *ngFor lists.\n}\n\n// ─── Pattern 2: OnPush + async pipe (self-managing) ─────────────\n@Component({\n  selector: 'app-notification-panel',\n  template: `\n    <div *ngFor=\"let n of notifications$ | async\">{{ n.text }}</div>\n  `,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class NotificationPanelComponent {\n  notifications$: Observable<Notification[]>;\n\n  constructor(private notifService: NotificationService) {\n    this.notifications$ = notifService.getAll();  // async pipe handles subscribe + markForCheck\n  }\n}\n\n// ─── Pattern 3: OnPush + manual markForCheck ────────────────────\n@Component({\n  selector: 'app-clock',\n  template: `<p>{{ time }}</p>`,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class ClockComponent implements OnInit {\n  time = '';\n  constructor(private cdr: ChangeDetectorRef) {}\n\n  ngOnInit() {\n    setInterval(() => {\n      this.time = new Date().toLocaleTimeString();\n      this.cdr.markForCheck();  // tell Angular: this OnPush component has new data\n    }, 1000);\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "manual-cd-trigger",
      "title": "How to trigger change detection manually (ChangeDetectorRef)?",
      "explanation": "<p>Sometimes you need to take direct control of change detection. Angular provides <code>ChangeDetectorRef</code> for this purpose — inject it into any component to control when and how it is checked.</p><h3>markForCheck()</h3><p>Marks the component (and all its ancestors) as dirty — to be checked in the <em>next</em> change detection cycle. Use this inside OnPush components when you update data that Angular wouldn't otherwise detect (e.g. data from a service callback, a setTimeout, or a third-party library).</p><h3>detectChanges()</h3><p>Forces an <em>immediate</em> synchronous check of this component and its children. Useful when you need the DOM to update right now (e.g. after programmatic focus changes, or inside a non-Angular callback).</p><h3>detach() / reattach()</h3><p>Completely removes a component from the change detection tree (<code>detach()</code>) or adds it back (<code>reattach()</code>). Advanced pattern for components that update very infrequently — you manage entirely when they are checked.</p><h3>detach + manual poll</h3><p>A powerful pattern for dashboards: detach the component from automatic CD, and only call <code>detectChanges()</code> when fresh data arrives from the API.</p>",
      "code": "import { Component, OnInit, ChangeDetectorRef, ChangeDetectionStrategy } from '@angular/core';\nimport { ExternalWidgetService } from './external-widget.service';\n\n@Component({\n  selector: 'app-live-price',\n  template: `<span>{{ price | currency }}</span>`,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class LivePriceComponent implements OnInit {\n  price = 0;\n\n  constructor(\n    private widgetService: ExternalWidgetService,\n    private cdr: ChangeDetectorRef\n  ) {}\n\n  ngOnInit() {\n    // This callback is from a third-party library — Zone.js doesn't always\n    // intercept these, so Angular might not know the value changed.\n    this.widgetService.onPriceChange((newPrice: number) => {\n      this.price = newPrice;\n      this.cdr.markForCheck();   // schedule a check on this OnPush component\n    });\n  }\n}\n\n// ─── detach / detectChanges pattern for high-performance panels ─\n@Component({\n  selector: 'app-metrics-panel',\n  template: `<div *ngFor=\"let m of metrics\">{{ m.label }}: {{ m.value }}</div>`,\n  changeDetection: ChangeDetectionStrategy.OnPush\n})\nexport class MetricsPanelComponent implements OnInit {\n  metrics: Metric[] = [];\n\n  constructor(private cdr: ChangeDetectorRef, private api: MetricsService) {}\n\n  ngOnInit() {\n    this.cdr.detach();   // completely remove from automatic CD tree\n\n    // Poll every 5 seconds manually\n    setInterval(() => {\n      this.api.getMetrics().subscribe(data => {\n        this.metrics = data;\n        this.cdr.detectChanges();   // only update when new data actually arrives\n      });\n    }, 5000);\n  }\n}",
      "language": "typescript"
    }
  ]
});
