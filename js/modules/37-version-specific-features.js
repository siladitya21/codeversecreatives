window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "version-specific-features",
  "title": "Angular Version History",
  "icon": "bi bi-arrow-repeat",
  "questions": [
    {
      id: "angular-22-standard-version-upgrade",
      title: "Angular 22 standard version baseline",
      explanation: `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A relay race where every runner has now crossed the same finish line. Signals, standalone components, zoneless change detection, and typed forms each started as the "new runner" in a different Angular release &mdash; some as an experiment, some as a preview. By Angular 22, released stable on <strong>June 3, 2026</strong> (now on patch <strong>22.0.7</strong>), every one of them has crossed into "default and stable." There's no more "coming soon" language left to hedge with.</p>
          </div>
        </div>
        <p>For Angular 22-ready interview prep, the baseline is no longer NgModule-first Angular. The modern baseline is standalone components, functional providers, signals, block template syntax, typed forms, functional router APIs, functional HTTP interceptors, SSR/hydration awareness, and modern testing &mdash; and as of Angular 22, two long-running defaults finally flipped.</p>
        <h3>What to treat as current standard</h3>
        <ul>
          <li><code>bootstrapApplication()</code> instead of a root <code>AppModule</code> for new apps.</li>
          <li>Standalone components, directives, and pipes &mdash; the only style the CLI generates.</li>
          <li><code>signal()</code>, <code>computed()</code>, <code>effect()</code>, and signal-based component APIs.</li>
          <li><code>@if</code>, <code>@for</code>, and <code>@switch</code> in new templates.</li>
          <li><code>provideRouter()</code>, <code>provideHttpClient()</code>, and functional interceptors.</li>
          <li><strong>OnPush is the default change detection strategy</strong> in Angular 22 for any component that doesn't explicitly set <code>changeDetection</code>.</li>
          <li><strong>Signal Forms</strong> (<code>@angular/forms/signals</code>) and <strong><code>resource()</code>/<code>rxResource()</code>/<code>httpResource()</code></strong> are all stable, production-ready APIs.</li>
        </ul>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text">Zoneless being "the default" describes <strong>new</strong> apps generated by the CLI, not every app that happens to run on Angular 22. An existing project upgraded to v22 keeps Zone.js until a developer deliberately removes it &mdash; the package upgrade alone changes nothing about whether Zone.js is loaded.</p>
          </div>
        </div>
      `,
      code: `// Modern Angular 22 baseline
bootstrapApplication(AppComponent, {
  providers: [
    provideRouter(routes),
    provideHttpClient(withInterceptors([authInterceptor]))
    // Zoneless by default for new CLI apps — no provider needed
  ]
});

@Component({
  selector: 'app-user-card',
  imports: [CurrencyPipe],
  // changeDetection: ChangeDetectionStrategy.OnPush  ← default in v22, no longer needed
  template: '<h3>{{ user().name }}</h3>'
})
export class UserCardComponent {
  readonly user = input.required<User>();
  readonly selected = output<User>();
}`,
      language: "typescript",
      diagram: `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Angular 14 &rarr; 22 at a Glance</p><div class="grid grid-cols-3 md:grid-cols-9 gap-1.5 text-[0.65rem]"><div class="bg-slate-50 border border-slate-200 rounded-lg p-1.5 text-center"><p class="font-bold text-slate-700">14</p><p class="text-slate-500 mt-0.5">inject()</p></div><div class="bg-slate-50 border border-slate-200 rounded-lg p-1.5 text-center"><p class="font-bold text-slate-700">15</p><p class="text-slate-500 mt-0.5">standalone stable</p></div><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-1.5 text-center"><p class="font-bold text-indigo-700">16</p><p class="text-slate-500 mt-0.5">signals preview</p></div><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-1.5 text-center"><p class="font-bold text-indigo-700">17</p><p class="text-slate-500 mt-0.5">@if/@for, @defer</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-1.5 text-center"><p class="font-bold text-emerald-700">18</p><p class="text-slate-500 mt-0.5">zoneless experimental</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-1.5 text-center"><p class="font-bold text-emerald-700">19</p><p class="text-slate-500 mt-0.5">resource(), incr. hydration</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-1.5 text-center"><p class="font-bold text-amber-700">20</p><p class="text-slate-500 mt-0.5">signals stable</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-1.5 text-center"><p class="font-bold text-amber-700">21</p><p class="text-slate-500 mt-0.5">zoneless default, Signal Forms preview</p></div><div class="bg-rose-100 border-2 border-rose-300 rounded-lg p-1.5 text-center"><p class="font-bold text-rose-700">22</p><p class="text-slate-600 mt-0.5">OnPush default, Signal Forms stable</p></div></div></div>`
    },
    {
      "id": "angular-14-15-features",
      "title": "Angular 14 & 15 — inject(), standalone components, typed forms",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">Moving out of a dorm and into your own studio apartment. Before, everything you needed had to be requested at the front desk (the constructor) and cleared through building management (an <code>NgModule</code>) before you could use it. <code>inject()</code> lets you reach for what you need directly, right where you're standing. Standalone components mean you don't need management's sign-off to exist at all &mdash; you just need your own front door and a lease (an <code>imports</code> array).</p>
          </div>
        </div>
        <p>Angular 14 and 15 (released mid-2022 and late-2022) introduced three changes that fundamentally shaped how modern Angular code is written.</p>
        <h3>inject() — Angular 14</h3>
        <p>The <code>inject()</code> function lets you access the DI system without a constructor. Instead of declaring all dependencies as constructor parameters, you call <code>inject(HttpClient)</code> directly in a class field initializer. This eliminates verbose constructors, works in functional contexts (guards, resolvers, interceptors), and integrates cleanly with signals because the injected value is available immediately as a class property rather than requiring assignment in the constructor body.</p>
        <h3>Standalone components — Angular 14 (preview), 15 (stable)</h3>
        <p>Standalone components (<code>standalone: true</code>) made NgModules optional. A component declares its own template dependencies in its <code>imports</code> array, eliminating the module file entirely. Angular 15 made standalone stable and extended it to directives and pipes. The Angular CLI started generating standalone by default. This is now the universal recommended approach for new Angular code.</p>
        <h3>Strictly typed reactive forms — Angular 14</h3>
        <p>Before Angular 14, all <code>FormControl</code>, <code>FormGroup</code>, and <code>FormArray</code> values were typed as <code>any</code>. Angular 14 introduced full generic typing: <code>FormControl&lt;string&gt;</code> knows its value is a <code>string</code>. <code>FormGroup</code> keys are checked against the defined shape. This catches an entire class of form bugs at compile time &mdash; accessing a form control that does not exist, or assigning a value of the wrong type.</p>
      `,
      "code": "// ---- Angular 14: inject() function ----\nimport { Component, inject, OnInit } from '@angular/core';\nimport { HttpClient } from '@angular/common/http';\nimport { Router } from '@angular/router';\n\n@Component({ selector: 'app-example', standalone: true, template: '...' })\nexport class ExampleComponent {\n  // inject() in field initializers — no constructor boilerplate\n  private http   = inject(HttpClient);\n  private router = inject(Router);\n}\n\n// ---- Angular 14: Functional guards (uses inject()) ----\nexport const authGuard = () => {\n  const auth   = inject(AuthService);\n  const router = inject(Router);\n  return auth.isLoggedIn() ? true : router.createUrlTree(['/login']);\n};\n\n// Routes use it directly:\n// { path: 'dashboard', canActivate: [authGuard] }\n\n// ---- Angular 14: Strictly typed reactive forms ----\nimport { FormControl, FormGroup, Validators } from '@angular/forms';\n\n// Every form value is now statically typed\nconst loginForm = new FormGroup({\n  email:    new FormControl('', { nonNullable: true, validators: Validators.required }),\n  password: new FormControl('', { nonNullable: true, validators: Validators.minLength(8) })\n});\n\n// TypeScript knows loginForm.value.email is string | undefined\n// loginForm.value.nonExistentField → compile error\nconst email: string | undefined = loginForm.value.email;\n\n// ---- Angular 15: Directive Composition API ----\n// Apply multiple directives to a component's host element without wrapping\n@Component({\n  selector: 'app-tooltip-button',\n  standalone: true,\n  hostDirectives: [\n    // Apply MatTooltip's behavior directly to this component\n    { directive: MatTooltip, inputs: ['matTooltip: tooltip'] }\n  ],\n  template: `<button><ng-content /></button>`\n})\nexport class TooltipButtonComponent {}\n// Usage: <app-tooltip-button tooltip=\"Save changes\">Save</app-tooltip-button>",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Angular 14 &amp; 15 — Three Foundational Shifts</p><div class="grid grid-cols-1 md:grid-cols-3 gap-3 text-xs"><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3 text-center"><p class="font-bold text-indigo-700">inject()</p><p class="text-slate-500 mt-1">DI without a constructor</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3 text-center"><p class="font-bold text-emerald-700">Standalone components</p><p class="text-slate-500 mt-1">NgModules become optional</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3 text-center"><p class="font-bold text-amber-700">Typed reactive forms</p><p class="text-slate-500 mt-1">no more FormControl&lt;any&gt;</p></div></div></div>`
    },
    {
      "id": "angular-16-17-features",
      "title": "Angular 16 & 17 — Signals, @defer, new control flow, esbuild",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">Replacing a security guard who walks the entire building every few minutes checking every door (Zone.js patching every async API to trigger change detection) with individual smart sensors on each door that report the instant something actually changes (a <code>signal()</code>). The guard still works, but the sensors are faster, cheaper to run, and only ever bother you about the door that actually opened.</p>
          </div>
        </div>
        <p>Angular 16 and 17 (May 2023 and November 2023) were the most transformative releases since Angular 2, introducing the signals reactivity model and a completely new template syntax.</p>
        <h3>Signals — Angular 16 (developer preview), 17 (stable)</h3>
        <p>Signals are a new reactive primitive: a <code>signal()</code> holds a value and tracks who reads it. <code>computed()</code> derives values from signals and recomputes only when dependencies change. <code>effect()</code> runs side effects when signals change. Unlike RxJS observables, signals are synchronous, always have a current value (no "not yet emitted" state), and do not require subscription management. Angular 16 introduced <code>toSignal()</code> and <code>toObservable()</code> for gradual adoption alongside existing RxJS code.</p>
        <h3>New control flow — Angular 17</h3>
        <p>The <code>@if</code>, <code>@for</code>, <code>@switch</code> block syntax replaced structural directives. The new syntax is built into the compiler &mdash; no <code>CommonModule</code> import needed, better TypeScript type narrowing inside blocks (TypeScript knows the type is non-null inside <code>@if (user)</code>), and mandatory <code>track</code> in <code>@for</code> for efficient list reconciliation.</p>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text"><code>@for</code> will not compile without a <code>track</code> expression &mdash; unlike the old <code>*ngFor</code>, where omitting <code>trackBy</code> just silently degraded performance. Reaching for <code>track $index</code> out of habit works, but defeats the point: use a stable identity like <code>track product.id</code> so Angular can reuse DOM nodes instead of re-rendering the whole list on every change.</p>
          </div>
        </div>
        <h3>@defer — Angular 17</h3>
        <p>Deferrable views let you lazy-load components directly in templates without route-level code splitting. A component inside <code>@defer (on viewport)</code> is not included in the initial bundle &mdash; it's downloaded only when the browser's Intersection Observer fires. Combined with <code>@placeholder</code> and <code>@loading</code> blocks, this gives you fine-grained control over what loads and when, improving Time to Interactive without architectural changes.</p>
        <h3>New Application Builder — Angular 17</h3>
        <p>Angular 17 replaced the Webpack-based builder with an esbuild-based "Application Builder" as the default. Cold build times dropped by roughly 70% (30-second builds become under 10 seconds). Hot reload became near-instantaneous. The new builder also enabled Vite as the dev server, giving HMR (Hot Module Replacement) with sub-100ms rebuild times during development.</p>
      `,
      "code": "// ---- Angular 16: Signals ----\nimport { Component, signal, computed, effect, input, output } from '@angular/core';\n\n@Component({\n  selector: 'app-cart-summary',\n  standalone: true,\n  template: `\n    <p>{{ itemCount() }} items — {{ total() | currency }}</p>\n  `\n})\nexport class CartSummaryComponent {\n  // Signal inputs (Angular 17.1+) — replaces @Input()\n  items = input<CartItem[]>([]);\n\n  // Computed signals — auto-update when items() changes\n  itemCount = computed(() => this.items().length);\n  total     = computed(() =>\n    this.items().reduce((sum, item) => sum + item.price * item.quantity, 0)\n  );\n}\n\n// ---- Angular 17: @for with track (required) ----\n// <ul>\n//   @for (product of products(); track product.id) {\n//     <li [class.out-of-stock]=\"product.stock === 0\">\n//       {{ product.name }} — {{ product.price | currency }}\n//       @if (product.stock === 0) {\n//         <span class=\"badge\">Out of Stock</span>\n//       }\n//     </li>\n//   } @empty {\n//     <li>No products found.</li>\n//   }\n// </ul>\n\n// ---- Angular 17: @defer ----\n// @defer (on viewport; prefetch on idle) {\n//   <!-- Only downloaded when visible in viewport -->\n//   <app-reviews-section [productId]=\"id\" />\n// } @placeholder {\n//   <div class=\"reviews-skeleton\">Loading reviews...</div>\n// } @loading (minimum 500ms) {\n//   <mat-spinner />\n// } @error {\n//   <p>Failed to load reviews.</p>\n// }\n\n// ---- angular.json: use new Application builder ----\n// \"builder\": \"@angular-devkit/build-angular:application\"  ← esbuild-based\n// Previously: \"@angular-devkit/build-angular:browser\"     ← webpack-based",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Angular 16 &amp; 17 — The Reactivity Overhaul</p><div class="grid grid-cols-2 md:grid-cols-4 gap-3 text-xs"><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3 text-center"><p class="font-bold text-indigo-700">Signals</p><p class="text-slate-500 mt-1">preview in 16, stable in 17</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3 text-center"><p class="font-bold text-emerald-700">@if / @for / @switch</p><p class="text-slate-500 mt-1">built into the compiler</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3 text-center"><p class="font-bold text-amber-700">@defer</p><p class="text-slate-500 mt-1">template-level lazy loading</p></div><div class="bg-purple-50 border border-purple-200 rounded-lg p-3 text-center"><p class="font-bold text-purple-700">esbuild + Vite</p><p class="text-slate-500 mt-1">~70% faster cold builds</p></div></div></div>`
    },
    {
      "id": "angular-18-19-features",
      "title": "Angular 18 & 19 — zoneless, incremental hydration, resource()",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A moving crew that only unpacks the boxes you actually open. Old SSR would truck in the entire shipment and unload everything onto the driveway whether you needed it that day or not. Incremental hydration leaves boxes sealed on the truck (still visible as fast, static HTML) until you actually walk over and reach for one &mdash; only then does the crew (Angular's JS) unpack and wire it up.</p>
          </div>
        </div>
        <p>Angular 18 and 19 (May 2024 and November 2024) focused on completing the signals story and advancing server-side rendering.</p>
        <h3>Zoneless change detection — Angular 18 (experimental)</h3>
        <p>Angular 18 introduced <strong>experimental zoneless change detection</strong> via <code>provideZonelessChangeDetection()</code>. When enabled, Zone.js is no longer required. Angular relies entirely on signals (and explicit <code>ChangeDetectorRef</code> calls) to know when to update the DOM. Removing Zone.js reduces bundle size by roughly 36KB and eliminates the performance overhead of Zone.js patching every async API. Zoneless requires that your application's state changes go through signals or explicit marking &mdash; passive property mutations no longer trigger change detection.</p>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text">Zoneless has four distinct milestones across four releases, and interviewers love mixing them up: <strong>experimental</strong> and opt-in (Angular 18), <strong>stable</strong> and still opt-in (Angular 20.2), <strong>default</strong> for new apps (Angular 21), and unchanged in that respect through Angular 22 &mdash; existing apps stay on Zone.js unless a developer removes it. "Zoneless" and "default" are not the same milestone.</p>
          </div>
        </div>
        <h3>Signal-based component APIs — Angular 17.1–18</h3>
        <p><code>input()</code>, <code>output()</code>, and <code>model()</code> completed the signal-based component API surface. <code>input()</code> replaces <code>@Input()</code> with a signal that reads the parent-bound value reactively. <code>output()</code> replaces <code>@Output()</code> with a cleaner emitter API. <code>model()</code> provides a two-way binding signal &mdash; equivalent to an input paired with an output named <code>valueChange</code>.</p>
        <h3>Incremental hydration — Angular 19</h3>
        <p>Angular 19 extended <code>@defer</code> to work with SSR hydration. With incremental hydration, the server renders the full page HTML (including deferred components), but the client only hydrates (downloads JavaScript and attaches event listeners) for the parts that the user actually interacts with. Sections below the fold or behind user interaction remain as static HTML until needed. This dramatically reduces the JavaScript that must be parsed on initial load.</p>
        <h3>resource() API — Angular 19</h3>
        <p>The <code>resource()</code> API provides a signal-based abstraction for async data loading. A <code>resource</code> has <code>value()</code>, <code>isLoading()</code>, <code>error()</code>, and <code>status()</code> signals, eliminating the boilerplate of manually managing loading and error state with separate signals. When the <code>request</code> signal changes, the resource automatically re-fetches.</p>
      `,
      "code": "// ---- Angular 18: Zoneless change detection ----\nimport { bootstrapApplication, provideZonelessChangeDetection } from '@angular/platform-browser';\n\nbootstrapApplication(AppComponent, {\n  providers: [\n    // Replaces Zone.js — requires signals for reactivity\n    provideZonelessChangeDetection()\n    // Remove 'zone.js' import from polyfills.ts as well\n  ]\n});\n\n// ---- Angular 17.1+: signal-based component APIs ----\nimport { Component, input, output, model } from '@angular/core';\n\n@Component({\n  selector: 'app-quantity-picker',\n  standalone: true,\n  template: `\n    <button (click)=\"decrement()\">-</button>\n    {{ quantity() }}\n    <button (click)=\"increment()\">+</button>\n  `\n})\nexport class QuantityPickerComponent {\n  // input() — replaces @Input()\n  min = input(1);\n  max = input(99);\n\n  // model() — two-way binding: replaces @Input() + @Output() valueChange\n  quantity = model(1);\n\n  // output() — replaces @Output() + EventEmitter\n  outOfRange = output<void>();\n\n  increment(): void {\n    const next = this.quantity() + 1;\n    if (next <= this.max()) this.quantity.set(next);\n    else this.outOfRange.emit();\n  }\n  decrement(): void {\n    const next = this.quantity() - 1;\n    if (next >= this.min()) this.quantity.set(next);\n  }\n}\n// Parent: <app-quantity-picker [(quantity)]=\"cartItem.qty\" [max]=\"stock\" />\n\n// ---- Angular 19: resource() API ----\nimport { Component, signal, resource } from '@angular/core';\n\n@Component({\n  selector: 'app-product-detail',\n  standalone: true,\n  template: `\n    @if (productResource.isLoading()) {\n      <mat-spinner />\n    } @else if (productResource.error()) {\n      <p>Error: {{ productResource.error() }}</p>\n    } @else {\n      <h1>{{ productResource.value()?.name }}</h1>\n    }\n  `\n})\nexport class ProductDetailComponent {\n  productId = signal<number | null>(null);\n\n  // resource() re-fetches whenever productId() changes\n  productResource = resource({\n    request: this.productId,\n    loader: async ({ request: id }) => {\n      if (!id) return null;\n      const res = await fetch(`/api/products/${id}`);\n      return res.json() as Promise<Product>;\n    }\n  });\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Zoneless Journey — Four Different Milestones</p><div class="flex flex-wrap items-center justify-center gap-2 text-xs"><div class="bg-slate-50 border-2 border-slate-200 rounded-lg px-3 py-2 text-center font-semibold text-slate-600">v18 — experimental, opt-in</div><span class="text-slate-300">&rarr;</span><div class="bg-indigo-50 border-2 border-indigo-200 rounded-lg px-3 py-2 text-center font-semibold text-indigo-700">v20.2 — stable, opt-in</div><span class="text-slate-300">&rarr;</span><div class="bg-emerald-50 border-2 border-emerald-200 rounded-lg px-3 py-2 text-center font-semibold text-emerald-700">v21 — default for new apps</div><span class="text-slate-300">&rarr;</span><div class="bg-amber-50 border-2 border-amber-200 rounded-lg px-3 py-2 text-center font-semibold text-amber-700">v22 — unchanged, still opt-out</div></div></div>`
    },
    {
      "id": "angular-20-21-features",
      "title": "Angular 20 & 21 - stable signals, zoneless default, Signal Forms, Angular Aria",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A young apprentice finishing their certification. Signals spent Angular 16 through 19 as an apprentice &mdash; useful, promising, but still supervised ("developer preview"). Angular 20 hands over the certificate: signals, incremental hydration, and the core reactivity model are now fully licensed and stable. Angular 21 goes a step further and puts the apprentice in charge of the whole shop by default (zoneless), while a new class of trainees &mdash; Signal Forms and Angular Aria &mdash; starts its own apprenticeship in experimental/preview mode.</p>
          </div>
        </div>
        <p>Angular 20 and 21 complete a major transition that started in Angular 16: Angular is now signals-first, standalone-first, and increasingly zoneless by default. For interview prep and production work, this means modern Angular knowledge should not stop at components, services, RxJS, and NgModules. You should also understand signal-based component APIs, fine-grained change notifications, server rendering improvements, and the newer form and accessibility packages.</p>
        <h3>Angular 20 - stabilization release</h3>
        <p>Angular 20 stabilized the core reactivity story. The fundamental signal primitives graduated to stable in v20, including <code>signal</code>, <code>effect</code>, <code>linkedSignal</code>, signal-based queries, and signal-based inputs. Angular 20 also graduated incremental hydration to stable, so server-rendered apps can hydrate deferred parts of the page only when they are needed.</p>
        <h3>Angular 20.2 - zoneless stable</h3>
        <p>Zoneless Angular became stable in Angular 20.2. In v20 projects, you opt in with <code>provideZonelessChangeDetection()</code>. Once zoneless is enabled, Angular no longer depends on Zone.js monkey-patching every async browser API. Instead, Angular updates when known notifications happen: a template-read signal changes, <code>AsyncPipe</code> marks a view, an event listener runs, <code>markForCheck()</code> is called, or an input is set.</p>
        <h3>Angular 21 - zoneless by default</h3>
        <p>Angular 21 makes zoneless the default for new Angular apps. This changes the mental model: passive class-field mutation is no longer the thing to rely on. Prefer signals, immutable updates, <code>AsyncPipe</code>, and explicit change notifications. Existing Zone.js apps can still be migrated gradually, but new Angular 21 code should be written as if signals are the normal state primitive.</p>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text">Angular 21's Signal Forms and Angular Aria shipped as <strong>experimental</strong> and <strong>developer preview</strong> respectively &mdash; not stable. If asked in an interview "are Signal Forms production-ready in Angular 21?", the accurate answer is no; that graduation happens in Angular 22 (Signal Forms stable) while Angular Aria's status should be checked against the release you're targeting rather than assumed.</p>
          </div>
        </div>
        <h3>Angular 21 - Signal Forms and Angular Aria</h3>
        <p>Angular 21 introduced experimental Signal Forms in <code>@angular/forms/signals</code>. Signal Forms use a writable signal model as the source of truth, generate a type-safe field tree with <code>form()</code>, and support schema-based validation. Angular 21 also launched Angular Aria in developer preview: headless accessible directives for patterns like tabs, menus, listbox, toolbar, grid, tree, and accordion. You bring the HTML and CSS; Angular Aria handles keyboard interaction, focus, ARIA attributes, and screen-reader behavior.</p>
      `,
      "code": "// ---- Angular 20.2: opt in to stable zoneless change detection ----\nimport { bootstrapApplication, provideZonelessChangeDetection } from '@angular/platform-browser';\n\nbootstrapApplication(AppComponent, {\n  providers: [provideZonelessChangeDetection()]\n});\n\n// Remove Zone.js from angular.json polyfills or from polyfills.ts:\n// import 'zone.js';          // remove\n// import 'zone.js/testing';  // remove from test setup\n\n// ---- Angular 21: zoneless is the default for new apps ----\n// No provider is needed unless you intentionally override the default.\n// Write updates using signals or APIs that notify Angular.\nimport { Component, computed, signal } from '@angular/core';\n\n@Component({\n  selector: 'app-cart',\n  standalone: true,\n  template: `\n    <button (click)=\"add({ id: crypto.randomUUID(), name: 'Book', price: 20 })\">\n      Add\n    </button>\n    <p>Total: {{ total() }}</p>\n  `\n})\nexport class CartComponent {\n  items = signal<CartItem[]>([]);\n  total = computed(() => this.items().reduce((sum, item) => sum + item.price, 0));\n\n  add(item: CartItem): void {\n    // Immutable update: creates a new array reference and notifies signal readers.\n    this.items.update(items => [...items, item]);\n  }\n}\n\n// ---- Angular 21: experimental Signal Forms ----\nimport { form, FormField, required, email } from '@angular/forms/signals';\n\ninterface LoginModel { email: string; password: string; }\n\n@Component({\n  selector: 'app-login',\n  standalone: true,\n  imports: [FormField],\n  template: `\n    <input type=\"email\" [formField]=\"loginForm.email\" />\n    <input type=\"password\" [formField]=\"loginForm.password\" />\n\n    @if (loginForm.email().invalid()) {\n      <p>Please enter a valid email.</p>\n    }\n  `\n})\nexport class LoginComponent {\n  loginModel = signal<LoginModel>({ email: '', password: '' });\n\n  loginForm = form(this.loginModel, path => {\n    required(path.email);\n    email(path.email);\n    required(path.password);\n  });\n}\n\n// ---- Angular 21: Angular Aria headless tabs (developer preview) ----\nimport { TabList, Tab, TabPanel } from '@angular/aria/tabs';\n\n@Component({\n  selector: 'app-settings-tabs',\n  standalone: true,\n  imports: [TabList, Tab, TabPanel],\n  template: `\n    <div ngTabList aria-label=\"Settings\" [(selectedTab)]=\"selectedTab\">\n      <button ngTab value=\"profile\">Profile</button>\n      <button ngTab value=\"security\">Security</button>\n    </div>\n\n    <section ngTabPanel value=\"profile\">Profile settings...</section>\n    <section ngTabPanel value=\"security\">Security settings...</section>\n  `\n})\nexport class SettingsTabsComponent {\n  selectedTab = 'profile';\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Angular 20 &amp; 21 — Graduation Timeline</p><div class="grid grid-cols-1 md:grid-cols-2 gap-3 text-xs"><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3"><p class="font-bold text-emerald-700 text-center">Angular 20</p><p class="text-slate-500 mt-1 text-center">signals stable &middot; incremental hydration stable</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3"><p class="font-bold text-amber-700 text-center">Angular 20.2</p><p class="text-slate-500 mt-1 text-center">zoneless stable, still opt-in</p></div><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3"><p class="font-bold text-indigo-700 text-center">Angular 21</p><p class="text-slate-500 mt-1 text-center">zoneless default &middot; Signal Forms &amp; Aria preview</p></div><div class="bg-rose-50 border border-rose-200 rounded-lg p-3"><p class="font-bold text-rose-700 text-center">Angular 22</p><p class="text-slate-500 mt-1 text-center">Signal Forms &amp; resource() family stable</p></div></div></div>`
    },
    {
      "id": "angular-22-stable-release",
      "title": "Angular 22 — the stable release, in full",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A ribbon-cutting ceremony for a building that's been quietly under construction for years. Every floor &mdash; signals, standalone components, zoneless rendering &mdash; was built and inspected release by release. Angular 22 is the day the "under construction" sign finally comes down and the whole building opens with its new default floor plan already in place, not a beta wing anyone has to opt into.</p>
          </div>
        </div>
        <p>Angular 22 released stable on <strong>June 3, 2026</strong>, and is currently on patch <strong>22.0.7</strong>. It's the capstone of the signals-and-standalone transition that began in Angular 14 &mdash; the release where the last "experimental" and "developer preview" labels from Angular 20 and 21 come off, and one long-standing default finally flips.</p>
        <h3>OnPush becomes the default change detection strategy</h3>
        <p>Any component that doesn't explicitly set <code>changeDetection</code> now runs under <code>ChangeDetectionStrategy.OnPush</code> instead of <code>Default</code>. Previously you had to opt in to OnPush; in Angular 22 you'd have to opt out (back to <code>Default</code>) to get the old behavior. This aligns the framework default with what signals, <code>AsyncPipe</code>, and zoneless rendering already expected of well-written components.</p>
        <h3>Signal Forms graduate to stable</h3>
        <p><code>@angular/forms/signals</code> &mdash; experimental as of Angular 21 &mdash; is stable in Angular 22. No more experimental warnings when you call <code>form()</code>, build a field tree, or wire up schema-based validation. Reactive Forms and Template-Driven Forms remain fully supported; Signal Forms is now a third, fully production-ready option rather than a preview.</p>
        <h3>resource(), rxResource(), and httpResource() graduate to stable</h3>
        <p>The whole signal-based async data loading family &mdash; introduced piecemeal starting in Angular 19 &mdash; is production-ready in Angular 22. <code>resource()</code> for general async loaders, <code>rxResource()</code> for wrapping an Observable-returning source, and <code>httpResource()</code> for a signal-driven HTTP call, all ship without experimental status.</p>
        <h3>Zoneless stays the default for new apps</h3>
        <p>Nothing changes here from Angular 21's default &mdash; the CLI still scaffolds new apps without Zone.js. What's worth remembering for Angular 22 specifically: <strong>upgrading</strong> an existing app to v22 does not remove Zone.js for you. It's still an explicit, deliberate migration step.</p>
      `,
      "code": "// main.ts — Angular 22 stable, zoneless-by-default bootstrap\nimport { bootstrapApplication } from '@angular/platform-browser';\nimport { provideHttpClient, withInterceptors } from '@angular/common/http';\nimport { provideRouter } from '@angular/router';\nimport { AppComponent } from './app/app.component';\nimport { routes } from './app/app.routes';\nimport { authInterceptor } from './app/core/auth.interceptor';\n\nbootstrapApplication(AppComponent, {\n  providers: [\n    provideRouter(routes),\n    provideHttpClient(withInterceptors([authInterceptor]))\n    // No provideZonelessChangeDetection() call needed —\n    // CLI-generated Angular 22 apps are zoneless by default.\n  ]\n});\n\n// ---- Signal Forms, stable in v22 ----\nimport { form, FormField, required, email } from '@angular/forms/signals';\nimport { signal } from '@angular/core';\n\ninterface SignupModel { email: string; password: string; }\n\nconst model = signal<SignupModel>({ email: '', password: '' });\nconst signupForm = form(model, path => {\n  required(path.email);\n  email(path.email);\n  required(path.password);\n});\n// No experimental warning in the console — production ready\n\n// ---- httpResource(), stable in v22 ----\nimport { httpResource } from '@angular/common/http';\nimport { computed, signal } from '@angular/core';\n\nconst productId = signal(42);\nconst product = httpResource<Product>(() => `/api/products/${productId()}`);\n\n// product.value(), product.isLoading(), product.error() all just work\n// re-fetches automatically whenever productId() changes",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Angular 22 — What Graduated to Stable</p><div class="grid grid-cols-2 md:grid-cols-4 gap-3 text-xs"><div class="bg-rose-50 border border-rose-200 rounded-lg p-3 text-center"><p class="font-bold text-rose-700">OnPush default</p><p class="text-slate-500 mt-1">flipped, no longer opt-in</p></div><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3 text-center"><p class="font-bold text-indigo-700">Signal Forms</p><p class="text-slate-500 mt-1">@angular/forms/signals stable</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3 text-center"><p class="font-bold text-emerald-700">resource() family</p><p class="text-slate-500 mt-1">resource / rxResource / httpResource</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3 text-center"><p class="font-bold text-amber-700">Zoneless default</p><p class="text-slate-500 mt-1">unchanged from v21, new apps only</p></div></div></div>`
    },
    {
      "id": "deprecated-features",
      "title": "Deprecated and discouraged patterns — what to migrate away from",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A grandfather clause in a city's zoning law. Buildings constructed under the old code are still legal to live in and don't have to be torn down &mdash; but if you're pulling a permit for new construction, the inspector expects you to build to current code. NgModules and decorator <code>@Input()</code>/<code>@Output()</code> are exactly that: perfectly legal in existing buildings, just not what you'd be approved to build new.</p>
          </div>
        </div>
        <p>Angular's evolution toward signals and standalone components means some older patterns are now actively discouraged in new code, even though they still work. Understanding what is deprecated and why helps you write idiomatic modern Angular and plan migration work for existing codebases.</p>
        <h3>NgModules</h3>
        <p>NgModules are not deprecated and will not be removed &mdash; Angular maintains strong backwards compatibility. However, they are no longer recommended for new code. Every new feature introduced since Angular 14 is designed for standalone components. The Angular CLI generates standalone by default. The main motivation to keep NgModules in existing code is avoiding large-scale refactoring; new features should be written standalone.</p>
        <h3>@Input / @Output decorators</h3>
        <p>The decorator-based <code>@Input()</code> and <code>@Output()</code> still work and will continue to work. The signal-based <code>input()</code> and <code>output()</code> are preferred for new code because they integrate with zoneless change detection, work naturally with <code>computed()</code>, and require no decorator metadata. Migrating existing components is low-priority unless they are being actively developed.</p>
        <div class="gotcha-box">
          <svg class="gotcha-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
          <div class="gotcha-body">
            <p class="gotcha-label">Common trap</p>
            <p class="gotcha-text">"Discouraged" and "deprecated" are not the same word, and interviewers notice the difference. NgModules, <code>@Input()</code>/<code>@Output()</code>, and <code>*ngIf</code>/<code>*ngFor</code> are all still fully supported APIs with no removal date &mdash; they are legacy-style, not deprecated. Saying "NgModules are deprecated" in an interview is itself a small red flag.</p>
          </div>
        </div>
        <h3>Zone.js reliance</h3>
        <p>In a zoneless Angular app, Zone.js can be removed from <code>polyfills.ts</code> entirely. However, many third-party libraries still trigger change detection via Zone.js patching. Removing Zone.js may break libraries that are not yet zoneless-compatible. Check your dependencies before removing it in production.</p>
        <h3>Karma test runner</h3>
        <p>Karma was deprecated in Angular 16. The Angular team recommends migrating to the Web Test Runner (with <code>@web/test-runner</code>) or to Jest for unit tests. New projects generated with the CLI use the Web Test Runner by default.</p>
      `,
      "code": "// ----  Pattern comparison: old vs modern Angular ----\n\n// ❌ Old: NgModule-based component (Angular < 14 style)\n// @NgModule({ declarations: [ProductListComponent], imports: [CommonModule] })\n// export class ProductsModule {}\n//\n// @Component({ selector: 'app-product-list', templateUrl: '...' })\n// export class ProductListComponent implements OnInit {\n//   @Input() category: string = '';\n//   @Output() productSelected = new EventEmitter<Product>();\n//   constructor(private service: ProductService) {}\n//   ngOnInit() { this.service.getAll().subscribe(p => this.products = p); }\n// }\n\n// ✅ Modern: Standalone + signals + inject() (Angular 22 style)\nimport { Component, inject, signal, input, output, computed } from '@angular/core';\nimport { takeUntilDestroyed } from '@angular/core/rxjs-interop';\nimport { ProductService } from './product.service';\n\n@Component({\n  selector: 'app-product-list',\n  template: `\n    @for (product of filtered(); track product.id) {\n      <app-product-card [product]=\"product\"\n        (add)=\"productSelected.emit(product)\" />\n    } @empty {\n      <p>No products in {{ category() }}</p>\n    }\n  `\n})\nexport class ProductListComponent {\n  // Signal-based input\n  category = input<string>('');\n\n  // Signal-based output\n  productSelected = output<Product>();\n\n  // inject() — no constructor\n  private service = inject(ProductService);\n\n  // Signal state\n  products = signal<Product[]>([]);\n\n  // Computed — auto-updates when category or products change\n  filtered = computed(() =>\n    this.products().filter(p => !this.category() || p.category === this.category())\n  );\n\n  constructor() {\n    // takeUntilDestroyed — no ngOnDestroy needed\n    this.service.getAll()\n      .pipe(takeUntilDestroyed())\n      .subscribe(p => this.products.set(p));\n  }\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">Legacy-Style vs Deprecated</p><div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-xs"><div class="bg-amber-50 border-2 border-amber-200 rounded-xl p-3"><p class="font-bold text-amber-700 text-center mb-2">Legacy-style, still supported</p><div class="flex flex-col items-center gap-1"><div class="bg-white border border-amber-200 rounded px-2 py-1 w-full text-center">NgModules</div><div class="bg-white border border-amber-200 rounded px-2 py-1 w-full text-center">@Input() / @Output()</div><div class="bg-white border border-amber-200 rounded px-2 py-1 w-full text-center">*ngIf / *ngFor</div></div></div><div class="bg-rose-50 border-2 border-rose-200 rounded-xl p-3"><p class="font-bold text-rose-700 text-center mb-2">Actually deprecated</p><div class="flex flex-col items-center gap-1"><div class="bg-white border border-rose-200 rounded px-2 py-1 w-full text-center">Karma test runner (since v16)</div></div></div></div></div>`
    },
    {
      "id": "deferrable-views-defer",
      "title": "@defer — template-level lazy loading with trigger conditions",
      "explanation": `
        <div class="analogy-box">
          <svg class="analogy-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"/></svg>
          <div class="analogy-body">
            <p class="analogy-label">Think of it like</p>
            <p class="analogy-text">A restaurant that fires a dish only once someone actually orders it, instead of pre-cooking the entire menu at 6am and hoping people show up. <code>@defer (on interaction)</code> is "cook it when they order." <code>@defer (on viewport)</code> is "start cooking when they walk up and read the menu on the wall." Nothing is wasted on dishes nobody was going to order in the first place.</p>
          </div>
        </div>
        <p><code>@defer</code> is one of the most practically impactful features introduced in Angular 17. Before it, the only way to lazy-load a component was through route-level code splitting (<code>loadComponent</code>). If you wanted a heavy component (a rich text editor, a data visualization, a comments section) to not be part of the initial bundle, you had to restructure your routing to put it on a separate route &mdash; often an awkward architectural compromise.</p>
        <p><code>@defer</code> solves this at the template level. Any component inside a <code>@defer</code> block is automatically split into a separate chunk by the build system. The chunk is downloaded only when the trigger condition is met. The component's module, its dependencies, and its styles all stay out of the initial bundle.</p>
        <h3>Trigger conditions</h3>
        <p><code>on viewport</code> &mdash; uses IntersectionObserver to load when the placeholder enters the visible area. Best for below-the-fold content. <code>on interaction</code> &mdash; loads on click, hover, or focus on the placeholder. Best for expandable panels and on-demand widgets. <code>on idle</code> &mdash; loads during browser idle time, after the main content is interactive. Best for lower-priority enhancements. <code>when condition</code> &mdash; loads when a boolean expression becomes true. Best for user-triggered states (e.g., user scrolls to a section, or a feature flag is enabled).</p>
        <h3>Prefetch</h3>
        <p>Prefetch lets you separate the download trigger from the render trigger. <code>@defer (on interaction; prefetch on idle)</code> starts downloading the chunk during idle time, but renders the component only when the user interacts. This hides the download latency while still deferring the render until needed.</p>
      `,
      "code": "// ---- Full @defer example with all blocks ----\nimport { Component, signal } from '@angular/core';\n\n@Component({\n  selector: 'app-product-page',\n  standalone: true,\n  template: `\n    <!-- Always in the main bundle: fast initial paint -->\n    <app-product-header [product]=\"product()\" />\n    <app-product-gallery [images]=\"product().images\" />\n\n    <!-- Rich text editor: only needed if user clicks Edit -->\n    <!-- Prefetch during idle so it downloads without waiting for click -->\n    @defer (on interaction; prefetch on idle) {\n      <app-rich-editor [content]=\"product().description\"\n                       (saved)=\"saveDescription($event)\" />\n    } @placeholder {\n      <div class=\"description-preview\">{{ product().description }}</div>\n      <button>Edit Description</button>\n    } @loading (minimum 300ms) {\n      <mat-spinner diameter=\"24\" />\n    } @error {\n      <p class=\"text-red-500\">Editor failed to load. Please refresh.</p>\n    }\n\n    <!-- Reviews section: below fold — load when visible -->\n    @defer (on viewport) {\n      <app-reviews [productId]=\"product().id\" />\n    } @placeholder {\n      <div class=\"reviews-skeleton\" aria-hidden=\"true\">\n        @for (_ of [1,2,3]; track $index) {\n          <div class=\"skeleton-line\"></div>\n        }\n      </div>\n    }\n\n    <!-- AI recommendations: low priority — load on idle -->\n    @defer (on idle) {\n      <app-ai-recommendations [productId]=\"product().id\" />\n    }\n\n    <!-- Related products: load when user reaches this section -->\n    @defer (when showRelated()) {\n      <app-related-products [category]=\"product().category\" />\n    }\n  `\n})\nexport class ProductPageComponent {\n  product = signal<Product>(null!);\n  showRelated = signal(false);\n\n  saveDescription(content: string): void {\n    // ...\n  }\n}",
      "language": "typescript",
      "diagram": `<div class="diagram-wrap"><p class="text-center text-xs font-bold text-slate-400 uppercase tracking-widest mb-5">@defer Triggers, Best Fit</p><div class="grid grid-cols-2 md:grid-cols-4 gap-3 text-xs"><div class="bg-indigo-50 border border-indigo-200 rounded-lg p-3 text-center"><p class="font-bold text-indigo-700">on viewport</p><p class="text-slate-500 mt-1">below-the-fold sections</p></div><div class="bg-emerald-50 border border-emerald-200 rounded-lg p-3 text-center"><p class="font-bold text-emerald-700">on interaction</p><p class="text-slate-500 mt-1">expandable panels, editors</p></div><div class="bg-amber-50 border border-amber-200 rounded-lg p-3 text-center"><p class="font-bold text-amber-700">on idle</p><p class="text-slate-500 mt-1">low-priority enhancements</p></div><div class="bg-purple-50 border border-purple-200 rounded-lg p-3 text-center"><p class="font-bold text-purple-700">when condition</p><p class="text-slate-500 mt-1">explicit app state / flags</p></div></div></div>`
    }
  ]
});
