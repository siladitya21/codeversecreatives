window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "http-api",
  "title": "HTTP & API",
  "icon": "bi bi-cloud-arrow-down",
  "questions": [
    {
      "id": "what-is-httpclient",
      "title": "What is HttpClient?",
      "explanation": "<p><strong>HttpClient</strong> is Angular's built-in service for making HTTP requests to backend APIs. It wraps the browser's <code>XMLHttpRequest</code> (or <code>fetch</code> in newer Angular versions) and gives you a clean, Observable-based API with TypeScript support.</p><h3>Why not use fetch() directly?</h3><p>You could, but HttpClient gives you several things for free:</p><ul><li><strong>Observable-based</strong> — every request returns an Observable, which integrates naturally with RxJS operators like <code>switchMap</code>, <code>catchError</code>, <code>retry</code></li><li><strong>Type safety</strong> — you can pass a generic type parameter and get a typed response</li><li><strong>Interceptors</strong> — middleware to add auth headers, log requests, handle errors globally</li><li><strong>Automatic JSON parsing</strong> — response bodies are parsed as JSON by default</li><li><strong>Cancellable</strong> — unsubscribing cancels the pending HTTP request</li><li><strong>Testable</strong> — Angular provides <code>HttpClientTestingModule</code> to mock requests</li></ul><h3>Setup</h3><p>In modern Angular (17+) with standalone components, register HttpClient globally by calling <code>provideHttpClient()</code> in <code>main.ts</code>. Then inject <code>HttpClient</code> into any service.</p>",
      "code": "// main.ts — register once for the whole app\nimport { bootstrapApplication } from '@angular/platform-browser';\nimport { provideHttpClient, withInterceptors } from '@angular/common/http';\nimport { AppComponent } from './app/app.component';\n\nbootstrapApplication(AppComponent, {\n  providers: [\n    provideHttpClient()   // makes HttpClient injectable everywhere\n    // provideHttpClient(withInterceptors([authInterceptor]))  ← with interceptors\n  ]\n});\n\n// users.service.ts — inject and use\nimport { Injectable } from '@angular/core';\nimport { HttpClient } from '@angular/common/http';\nimport { Observable } from 'rxjs';\n\nexport interface User { id: number; name: string; email: string; }\n\n@Injectable({ providedIn: 'root' })\nexport class UsersService {\n  private apiUrl = 'https://api.example.com/users';\n\n  constructor(private http: HttpClient) {}\n\n  // Generic type parameter <User[]> tells TypeScript what the response looks like\n  getAll(): Observable<User[]> {\n    return this.http.get<User[]>(this.apiUrl);\n  }\n\n  getById(id: number): Observable<User> {\n    return this.http.get<User>(`${this.apiUrl}/${id}`);\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "how-to-make-http-requests",
      "title": "How to make HTTP requests (GET, POST, PUT, DELETE)?",
      "explanation": "<p>HttpClient exposes one method per HTTP verb. Each method returns a cold Observable — nothing happens until you call <code>.subscribe()</code> (or use the <code>async</code> pipe in a template).</p><h3>GET — fetch data</h3><p>Use for reading data. Pass a generic type to get a typed response. You can also pass query parameters via the <code>params</code> option.</p><h3>POST — create new data</h3><p>Use for creating a new resource. The second argument is the request body — Angular serializes it to JSON automatically.</p><h3>PUT / PATCH — update data</h3><p><code>PUT</code> replaces the entire resource. <code>PATCH</code> applies a partial update. Use whichever your API expects.</p><h3>DELETE — remove data</h3><p>Sends a DELETE request. Many APIs return the deleted item or just a 204 No Content — adjust the type accordingly.</p>",
      "code": "import { HttpClient, HttpParams } from '@angular/common/http';\nimport { Observable } from 'rxjs';\n\n@Injectable({ providedIn: 'root' })\nexport class ProductsService {\n  private url = 'https://api.example.com/products';\n  constructor(private http: HttpClient) {}\n\n  // ─── GET with query parameters ──────────────────────────────\n  getAll(category?: string, page = 1): Observable<Product[]> {\n    let params = new HttpParams().set('page', page);\n    if (category) params = params.set('category', category);\n    return this.http.get<Product[]>(this.url, { params });\n    // Sends: GET /products?page=1&category=electronics\n  }\n\n  getById(id: number): Observable<Product> {\n    return this.http.get<Product>(`${this.url}/${id}`);\n  }\n\n  // ─── POST — create ───────────────────────────────────────────\n  create(product: Omit<Product, 'id'>): Observable<Product> {\n    return this.http.post<Product>(this.url, product);\n    // Body is automatically serialized to JSON\n    // Content-Type: application/json is set automatically\n  }\n\n  // ─── PUT — full update ───────────────────────────────────────\n  update(id: number, product: Product): Observable<Product> {\n    return this.http.put<Product>(`${this.url}/${id}`, product);\n  }\n\n  // ─── PATCH — partial update ──────────────────────────────────\n  patch(id: number, changes: Partial<Product>): Observable<Product> {\n    return this.http.patch<Product>(`${this.url}/${id}`, changes);\n  }\n\n  // ─── DELETE ──────────────────────────────────────────────────\n  delete(id: number): Observable<void> {\n    return this.http.delete<void>(`${this.url}/${id}`);\n  }\n}\n\n// Component usage:\nthis.productsService.create({ name: 'Laptop', price: 999 }).subscribe({\n  next:  (created) => console.log('Created:', created),\n  error: (err)     => console.error('Failed:', err)\n});",
      "language": "typescript"
    },
    {
      "id": "what-is-httpclientmodule",
      "title": "How to set up HttpClient (Module vs Standalone)?",
      "explanation": "<p>How you register HttpClient depends on whether your app uses NgModules or standalone components.</p><h3>Modern approach — Standalone (Angular 17+)</h3><p>Call <code>provideHttpClient()</code> in the <code>providers</code> array of <code>bootstrapApplication()</code>. This is the recommended approach for new Angular apps. You can pass optional features like interceptors or XSRF protection here.</p><h3>Legacy approach — NgModule</h3><p>Import <code>HttpClientModule</code> in your root <code>AppModule</code>'s <code>imports</code> array. This is still valid for apps that have not migrated to standalone.</p><h3>Important</h3><p>Register HttpClient only <em>once</em>, at the root level. If you import it in feature modules as well, you can end up with multiple instances, which breaks interceptors.</p>",
      "code": "// ─── Modern standalone setup (recommended) ────────────────────\n// main.ts\nimport { bootstrapApplication } from '@angular/platform-browser';\nimport { provideHttpClient, withFetch, withInterceptorsFromDi } from '@angular/common/http';\n\nbootstrapApplication(AppComponent, {\n  providers: [\n    provideHttpClient(\n      withFetch()              // use native fetch() instead of XHR (Angular 18+)\n      // withInterceptors([authInterceptor, loggingInterceptor])\n    )\n  ]\n});\n\n// ─── Legacy NgModule setup ─────────────────────────────────────\n// app.module.ts\nimport { NgModule } from '@angular/core';\nimport { BrowserModule } from '@angular/platform-browser';\nimport { HttpClientModule } from '@angular/common/http';\n\n@NgModule({\n  imports: [\n    BrowserModule,\n    HttpClientModule   // ← registers HttpClient for the whole app\n  ],\n  bootstrap: [AppComponent]\n})\nexport class AppModule {}",
      "language": "typescript"
    },
    {
      "id": "how-to-handle-http-errors",
      "title": "How to handle HTTP errors?",
      "explanation": "<p>HTTP errors fall into two categories:</p><ul><li><strong>Client errors (4xx)</strong> — bad request, unauthorized, not found. The server responded but with an error status.</li><li><strong>Network errors</strong> — no response at all (server down, no internet). The <code>status</code> will be 0.</li></ul><p>Angular delivers both as an <code>HttpErrorResponse</code> object to the <code>error</code> callback of your subscription.</p><h3>Where to handle errors</h3><p>There are two levels:</p><ul><li><strong>Per-request</strong> — use <code>catchError</code> in the service method's pipe for request-specific recovery (show a fallback, retry, etc.)</li><li><strong>Global</strong> — use an interceptor to handle errors centrally (e.g. redirect to login on 401, show a toast on 500)</li></ul><h3>Best practice</h3><p>Keep error-handling logic in the <strong>service</strong>, not the component. Components should only receive clean data or a clear error state.</p>",
      "code": "import { Injectable } from '@angular/core';\nimport { HttpClient, HttpErrorResponse } from '@angular/common/http';\nimport { Observable, throwError, of } from 'rxjs';\nimport { catchError, retry } from 'rxjs/operators';\n\n@Injectable({ providedIn: 'root' })\nexport class DataService {\n  constructor(private http: HttpClient) {}\n\n  getUsers(): Observable<User[]> {\n    return this.http.get<User[]>('/api/users').pipe(\n      retry(2),   // retry up to 2 times before giving up (good for transient network errors)\n      catchError(this.handleError)\n    );\n  }\n\n  private handleError(error: HttpErrorResponse): Observable<never> {\n    let userMessage = 'Something went wrong. Please try again.';\n\n    if (error.status === 0) {\n      // Network error — no response received\n      console.error('Network error:', error.error);\n      userMessage = 'Network error. Check your connection.';\n    } else if (error.status === 401) {\n      userMessage = 'You are not authorised. Please log in.';\n    } else if (error.status === 404) {\n      userMessage = 'Resource not found.';\n    } else if (error.status >= 500) {\n      userMessage = 'Server error. Please try again later.';\n    } else {\n      // API returned an error body\n      console.error(`Backend error ${error.status}:`, error.error);\n    }\n\n    return throwError(() => new Error(userMessage));\n  }\n}\n\n// Component — clean error handling:\nthis.dataService.getUsers().subscribe({\n  next:  users => { this.users = users; this.loading = false; },\n  error: err   => { this.errorMessage = err.message; this.loading = false; }\n});",
      "language": "typescript"
    },
    {
      "id": "what-are-interceptors",
      "title": "What are interceptors?",
      "explanation": "<p><strong>Interceptors</strong> are middleware for HTTP requests and responses. Every request passes through all registered interceptors before reaching the server, and every response passes back through them before reaching your code.</p><h3>What interceptors are used for</h3><ul><li><strong>Attaching auth tokens</strong> — add an <code>Authorization: Bearer &lt;token&gt;</code> header to every request automatically</li><li><strong>Global error handling</strong> — catch 401 errors and redirect to login; show toast notifications for 500 errors</li><li><strong>Logging</strong> — log request URLs and response times to the console or an analytics service</li><li><strong>Loading indicators</strong> — show/hide a global spinner while any request is in progress</li><li><strong>Request transformation</strong> — add a base URL prefix, set content-type headers, append correlation IDs</li></ul><h3>Two styles</h3><p>Modern Angular (15+) uses <strong>functional interceptors</strong> — just a plain function, no class needed. Older codebases use class-based interceptors implementing <code>HttpInterceptor</code>.</p>",
      "code": "// ─── Functional interceptor (modern, recommended) ─────────────\nimport { HttpInterceptorFn, HttpRequest, HttpHandlerFn } from '@angular/common/http';\nimport { inject } from '@angular/core';\nimport { AuthService } from './auth.service';\n\nexport const authInterceptor: HttpInterceptorFn = (req: HttpRequest<unknown>, next: HttpHandlerFn) => {\n  const auth = inject(AuthService);\n  const token = auth.getToken();\n\n  if (!token) return next(req);  // no token — pass through unchanged\n\n  // Clone the request (requests are immutable) and add the header\n  const authReq = req.clone({\n    setHeaders: { Authorization: `Bearer ${token}` }\n  });\n\n  return next(authReq);\n};\n\n// Register in main.ts:\n// provideHttpClient(withInterceptors([authInterceptor]))\n\n// ─── Class-based interceptor (legacy) ─────────────────────────\nimport { Injectable } from '@angular/core';\nimport { HttpInterceptor, HttpRequest, HttpHandler, HttpEvent } from '@angular/common/http';\nimport { Observable } from 'rxjs';\n\n@Injectable()\nexport class AuthInterceptorClass implements HttpInterceptor {\n  constructor(private auth: AuthService) {}\n\n  intercept(req: HttpRequest<any>, next: HttpHandler): Observable<HttpEvent<any>> {\n    const token = this.auth.getToken();\n    if (!token) return next.handle(req);\n\n    const authReq = req.clone({\n      setHeaders: { Authorization: `Bearer ${token}` }\n    });\n    return next.handle(authReq);\n  }\n}\n\n// Register in AppModule:\n// { provide: HTTP_INTERCEPTORS, useClass: AuthInterceptorClass, multi: true }",
      "language": "typescript"
    },
    {
      "id": "how-to-implement-http-interceptors",
      "title": "Real-world interceptor examples",
      "explanation": "<p>Here are two interceptors you'll build in almost every production Angular app.</p><h3>1. Loading interceptor</h3><p>Show a spinner whenever any HTTP request is in flight. Track the count of active requests — show spinner when count > 0, hide when it drops back to 0.</p><h3>2. Error interceptor</h3><p>Catch specific HTTP error codes globally. Redirect to login on 401. Show a notification on server errors. This removes repeated error-handling code from every service.</p><h3>Multiple interceptors</h3><p>Interceptors are applied in the order they are registered. For functional interceptors, pass them as an array to <code>withInterceptors()</code>.</p>",
      "code": "import { HttpInterceptorFn, HttpErrorResponse } from '@angular/common/http';\nimport { inject } from '@angular/core';\nimport { Router } from '@angular/router';\nimport { catchError, throwError } from 'rxjs';\nimport { tap, finalize } from 'rxjs/operators';\nimport { LoadingService } from './loading.service';\nimport { ToastService } from './toast.service';\n\n// ─── 1. Loading spinner interceptor ───────────────────────────\nexport const loadingInterceptor: HttpInterceptorFn = (req, next) => {\n  const loading = inject(LoadingService);\n  loading.show();   // increment active request count, show spinner\n\n  return next(req).pipe(\n    finalize(() => loading.hide())  // always hide, even on error\n  );\n};\n\n// ─── 2. Global error interceptor ──────────────────────────────\nexport const errorInterceptor: HttpInterceptorFn = (req, next) => {\n  const router = inject(Router);\n  const toast  = inject(ToastService);\n\n  return next(req).pipe(\n    catchError((err: HttpErrorResponse) => {\n      if (err.status === 401) {\n        // Not authenticated — clear session and send to login\n        router.navigate(['/login']);\n      } else if (err.status === 403) {\n        toast.error('You do not have permission to do this.');\n      } else if (err.status >= 500) {\n        toast.error('A server error occurred. Please try again later.');\n      }\n      return throwError(() => err);  // still propagate to the calling service\n    })\n  );\n};\n\n// main.ts — register both interceptors\n// provideHttpClient(withInterceptors([authInterceptor, loadingInterceptor, errorInterceptor]))\n// They run left to right on the request, right to left on the response.",
      "language": "typescript"
    },
    {
      "id": "what-is-cors",
      "title": "What is CORS and how to fix it?",
      "explanation": "<p><strong>CORS (Cross-Origin Resource Sharing)</strong> is a browser security policy. A browser blocks JavaScript from making HTTP requests to a different origin (different domain, port, or protocol) unless the server explicitly allows it.</p><h3>Example</h3><p>Your Angular app runs at <code>http://localhost:4200</code>. It makes a request to <code>http://localhost:3000/api/users</code>. The browser detects these are different origins (different port) and sends a preflight <code>OPTIONS</code> request. If the server doesn't respond with the right CORS headers, the browser blocks the response.</p><h3>CORS is a SERVER-side fix</h3><p>Angular (the client) cannot fix CORS. You must configure the server to send the correct headers. Angular's dev proxy is just a workaround for local development — it bypasses the browser's same-origin check by proxying through Node.js, which isn't subject to CORS.</p><h3>Production fix</h3><p>Configure your backend to send <code>Access-Control-Allow-Origin</code> with the correct domain. If you serve the Angular app and the API from the same origin (e.g. same domain, different paths), CORS is not an issue at all.</p>",
      "code": "// ─── SERVER response headers (what the server must send) ───────\n// Access-Control-Allow-Origin: https://yourapp.com\n// Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS\n// Access-Control-Allow-Headers: Content-Type, Authorization\n\n// ─── Express.js example (Node backend) ────────────────────────\n// const cors = require('cors');\n// app.use(cors({ origin: 'https://yourapp.com' }));\n\n// ─── Angular proxy for LOCAL DEVELOPMENT only ─────────────────\n// proxy.conf.json — routes /api/* through a local Node proxy\n// to avoid CORS during development\n{\n  \"/api\": {\n    \"target\": \"http://localhost:3000\",\n    \"changeOrigin\": true,\n    \"secure\": false\n  }\n}\n\n// angular.json (under serve > options):\n// \"proxyConfig\": \"proxy.conf.json\"\n\n// After this, requests to /api/users in your Angular code\n// are automatically proxied to http://localhost:3000/api/users\n// — the browser never sees the different origin\n\n// ⚠️ The proxy ONLY works in 'ng serve' (local dev).\n// In production, fix CORS on the server.",
      "language": "json"
    }
  ]
});
