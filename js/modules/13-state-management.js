window.MODULES = window.MODULES || [];
window.MODULES.push({
  "id": "state-management",
  "title": "State Management",
  "icon": "bi bi-diagram-3",
  "questions": [
    {
      "id": "what-is-state-management",
      "title": "What is state management?",
      "explanation": "<p><strong>State</strong> is any data your application needs to remember and display — the logged-in user, a shopping cart, a list of products, whether a sidebar is open. <strong>State management</strong> is how you store, update, and share that data across components.</p><h3>Why it becomes hard</h3><p>In a small app with 3–4 components, you can pass data around with <code>@Input()</code> and <code>@Output()</code>. But as an app grows to 50 components across many routes, this becomes unmanageable:</p><ul><li><strong>Prop drilling</strong> — passing data through 4–5 levels of components just to reach the one that needs it</li><li><strong>Out-of-sync views</strong> — two unrelated components both display the cart count, but they each maintain their own copy</li><li><strong>Unpredictable mutations</strong> — multiple components modify the same data in different ways, making bugs hard to trace</li></ul><h3>The solution: a single source of truth</h3><p>Store shared state in one central place (a service, a store library, or Signals). Components read from this central state and dispatch updates through it. Any component that subscribes to the state automatically re-renders when it changes.</p>",
      "code": "// ─── Problem: prop drilling ────────────────────────────────────\n// AppComponent → LayoutComponent → SidebarComponent → CartBadgeComponent\n// CartBadgeComponent needs the cart count — it travels through 3 parents.\n// If any intermediate component doesn't use it, it still has to pass it down.\n\n// ─── Solution 1: Service with BehaviorSubject (lightweight) ────\n@Injectable({ providedIn: 'root' })\nexport class CartService {\n  private _items$ = new BehaviorSubject<CartItem[]>([]);\n  readonly items$ = this._items$.asObservable();\n  readonly count$ = this.items$.pipe(map(items => items.length));\n\n  add(item: CartItem) {\n    this._items$.next([...this._items$.getValue(), item]);\n  }\n\n  remove(id: string) {\n    this._items$.next(this._items$.getValue().filter(i => i.id !== id));\n  }\n}\n\n// ANY component anywhere in the tree can inject CartService and:\n// — read: this.cartService.count$ | async\n// — update: this.cartService.add(item)\n// No prop drilling needed.",
      "language": "typescript"
    },
    {
      "id": "what-is-ngrx",
      "title": "What is NgRx?",
      "explanation": "<p><strong>NgRx</strong> is the most widely used state management library for Angular. It implements the <strong>Redux pattern</strong> using RxJS Observables.</p><h3>Core idea</h3><p>All application state lives in a single immutable object called the <strong>Store</strong>. State can only be changed by dispatching an <strong>Action</strong>. A <strong>Reducer</strong> is a pure function that receives the current state and an action, and returns a new state. This makes state changes predictable, traceable, and testable.</p><h3>The four building blocks</h3><ul><li><strong>Store</strong> — the single object holding all state; an Observable you can subscribe to</li><li><strong>Actions</strong> — plain objects describing what happened: <code>{ type: '[Cart] Add Item', item: {...} }</code></li><li><strong>Reducers</strong> — pure functions: <code>(currentState, action) => newState</code></li><li><strong>Effects</strong> — handle side effects (API calls, localStorage, routing) triggered by actions</li></ul><h3>When to use NgRx</h3><p>NgRx adds boilerplate. Use it when your app has complex shared state that is accessed by many unrelated components, has many async operations that interact with each other, or when your team needs strong debugging tools (time-travel with Redux DevTools). For simpler apps, a service with BehaviorSubject is often enough.</p>",
      "code": "// NgRx in 30 seconds — a mental model\n\n// 1. State lives in one place — the Store:\n//    { users: [], loading: false, error: null }\n\n// 2. To change state, you dispatch an Action (just a plain event):\n//    store.dispatch(loadUsers());\n\n// 3. A Reducer responds to the action and returns NEW state (never mutates):\n//    on(loadUsers, state => ({ ...state, loading: true }))\n\n// 4. An Effect handles the API call and dispatches a result action:\n//    loadUsers$ → http.get('/api/users') → dispatch(loadUsersSuccess({ users }))\n\n// 5. Components SELECT data from the store (read-only):\n//    this.users$ = store.select(selectAllUsers);\n\n// Everything flows in ONE direction:\n// Component → Action → Reducer → Store → Selector → Component",
      "language": "typescript"
    },
    {
      "id": "actions-reducers-effects",
      "title": "What are actions, reducers, and effects?",
      "explanation": "<h3>Actions — what happened</h3><p>An action is a plain object with a <code>type</code> string and optional data (<code>props</code>). It describes an event that occurred in the app. Actions are like notifications: \"Hey, the user clicked Load Users\", or \"The API returned these results.\" They never contain logic — they just report facts.</p><h3>Reducers — how state changes</h3><p>A reducer is a pure function that takes the current state and an action, and returns the next state. It must never mutate the existing state — always return a new object. Reducers contain no async code, no API calls, no side effects — only plain data transformations.</p><h3>Effects — side effects triggered by actions</h3><p>Effects are where async work happens. An Effect listens for a specific action (e.g. <code>loadUsers</code>), calls an API, and then dispatches a new action with the result (<code>loadUsersSuccess</code> or <code>loadUsersFailure</code>). Effects are the only place in NgRx where you should put HTTP calls, routing, localStorage, etc.</p>",
      "code": "import { createAction, createReducer, on, props, createEffect, Actions, ofType } from '@ngrx/store';\nimport { switchMap, map, catchError } from 'rxjs/operators';\nimport { of } from 'rxjs';\nimport { UserService } from './user.service';\n\n// ─── Actions ────────────────────────────────────────────────────\n// Naming convention: '[Feature/Source] Event Description'\nexport const loadUsers        = createAction('[Users Page] Load Users');\nexport const loadUsersSuccess = createAction('[Users API] Load Users Success', props<{ users: User[] }>());\nexport const loadUsersFailure = createAction('[Users API] Load Users Failure', props<{ error: string }>());\nexport const deleteUser       = createAction('[Users Page] Delete User', props<{ id: number }>());\n\n// ─── Reducer ────────────────────────────────────────────────────\nexport interface UsersState { users: User[]; loading: boolean; error: string | null; }\nconst initialState: UsersState = { users: [], loading: false, error: null };\n\nexport const usersReducer = createReducer(\n  initialState,\n  // Each 'on' handles one action — returns a new state object (never mutates)\n  on(loadUsers,        state              => ({ ...state, loading: true,  error: null })),\n  on(loadUsersSuccess, (state, { users }) => ({ ...state, loading: false, users })),\n  on(loadUsersFailure, (state, { error }) => ({ ...state, loading: false, error })),\n  on(deleteUser,       (state, { id })    => ({ ...state, users: state.users.filter(u => u.id !== id) }))\n);\n\n// ─── Effect ─────────────────────────────────────────────────────\nimport { Injectable } from '@angular/core';\n@Injectable()\nexport class UsersEffects {\n  loadUsers$ = createEffect(() =>\n    this.actions$.pipe(\n      ofType(loadUsers),        // listen only for this action\n      switchMap(() =>           // cancel prev request if new one arrives\n        this.userService.getAll().pipe(\n          map(users  => loadUsersSuccess({ users })),     // on success\n          catchError(err => of(loadUsersFailure({ error: err.message })))  // on failure\n        )\n      )\n    )\n  );\n\n  constructor(private actions$: Actions, private userService: UserService) {}\n}",
      "language": "typescript"
    },
    {
      "id": "what-is-the-store",
      "title": "What is the store in NgRx?",
      "explanation": "<p>The <strong>Store</strong> is the central state container. It is a single object (a plain JavaScript object) that holds <em>all</em> shared application state. In Angular, it is an Observable — meaning you can subscribe to it (or use <code>select()</code> to subscribe to a slice of it) and automatically receive updates whenever the state changes.</p><h3>Two operations</h3><ul><li><strong>dispatch(action)</strong> — tell the store something happened; the store passes the action to the reducer to produce new state</li><li><strong>select(selector)</strong> — subscribe to a slice of state; returns an Observable that emits the selected value whenever it changes</li></ul><h3>Immutability</h3><p>The store's state is never mutated directly. Every time state changes, the reducer returns a <em>completely new object</em>. This is what makes time-travel debugging possible — NgRx keeps a snapshot of every state in history.</p><h3>Feature states</h3><p>Large apps split state into feature slices (e.g. <code>state.users</code>, <code>state.products</code>, <code>state.cart</code>). Each feature registers its own reducer. <code>createFeatureSelector</code> makes it easy to select a feature slice.</p>",
      "code": "import { Store } from '@ngrx/store';\nimport { Component, OnInit } from '@angular/core';\nimport { Observable } from 'rxjs';\nimport { loadUsers, deleteUser } from './users.actions';\nimport { selectAllUsers, selectLoading, selectError } from './users.selectors';\n\n@Component({\n  selector: 'app-users',\n  template: `\n    <div *ngIf=\"loading$ | async\">Loading...</div>\n    <div *ngIf=\"error$ | async as error\" class=\"error\">{{ error }}</div>\n    <ul>\n      <li *ngFor=\"let user of users$ | async\">\n        {{ user.name }}\n        <button (click)=\"onDelete(user.id)\">Delete</button>\n      </li>\n    </ul>\n  `\n})\nexport class UsersComponent implements OnInit {\n  // Select slices of state as Observables\n  users$:   Observable<User[]>         = this.store.select(selectAllUsers);\n  loading$: Observable<boolean>        = this.store.select(selectLoading);\n  error$:   Observable<string | null>  = this.store.select(selectError);\n\n  constructor(private store: Store) {}\n\n  ngOnInit(): void {\n    // Dispatch an action — triggers the effect which calls the API\n    this.store.dispatch(loadUsers());\n  }\n\n  onDelete(id: number): void {\n    // Dispatch with payload\n    this.store.dispatch(deleteUser({ id }));\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "what-are-selectors",
      "title": "What are selectors?",
      "explanation": "<p><strong>Selectors</strong> are pure functions that extract and transform specific pieces of data from the store. They are NgRx's query layer — instead of every component knowing the exact shape of the store, components use selectors to ask for the data they need.</p><h3>Why use selectors instead of reading state directly?</h3><ul><li><strong>Memoized</strong> — a selector only recomputes its result when its input state actually changes. If the same state is passed again, it returns the cached result. This prevents unnecessary re-renders.</li><li><strong>Reusable</strong> — define the transformation once, use it in any component</li><li><strong>Composable</strong> — build complex selectors by composing simpler ones using <code>createSelector()</code></li><li><strong>Easy to test</strong> — selectors are pure functions; pass a mock state and assert the result</li></ul><h3>createFeatureSelector vs createSelector</h3><p><code>createFeatureSelector('users')</code> selects the top-level <code>state.users</code> slice. <code>createSelector()</code> composes that with a transformation to select specific data within that slice.</p>",
      "code": "import { createFeatureSelector, createSelector } from '@ngrx/store';\nimport { UsersState } from './users.reducer';\n\n// ─── Base selector — select the feature slice ──────────────────\nexport const selectUsersState = createFeatureSelector<UsersState>('users');\n\n// ─── Derived selectors — composed from the base ────────────────\nexport const selectAllUsers = createSelector(\n  selectUsersState,\n  (state) => state.users\n);\n\nexport const selectLoading = createSelector(\n  selectUsersState,\n  (state) => state.loading\n);\n\nexport const selectError = createSelector(\n  selectUsersState,\n  (state) => state.error\n);\n\n// ─── Computed selectors — expensive logic, run only when inputs change ─\nexport const selectAdminUsers = createSelector(\n  selectAllUsers,\n  (users) => users.filter(u => u.role === 'admin')   // memoized\n);\n\nexport const selectUserCount = createSelector(\n  selectAllUsers,\n  (users) => users.length   // recomputes only when users array changes\n);\n\n// ─── Selector with props (parameterised) ──────────────────────\nexport const selectUserById = (id: number) => createSelector(\n  selectAllUsers,\n  (users) => users.find(u => u.id === id)\n);\n\n// Component usage:\n// this.user$ = this.store.select(selectUserById(42));\n\n// ─── Testing a selector ────────────────────────────────────────\n// Selectors are just pure functions — test them directly:\nconst mockState = { users: { users: [{ id: 1, name: 'Alice', role: 'admin' }], loading: false, error: null } };\nconsole.log(selectAdminUsers(mockState));   // [{ id: 1, name: 'Alice', role: 'admin' }]",
      "language": "typescript"
    },
    {
      "id": "ngrx-alternatives",
      "title": "Alternatives to NgRx",
      "explanation": "<p>NgRx is powerful but adds significant boilerplate. Several alternatives exist depending on how much complexity your app actually needs.</p><h3>1. Service + BehaviorSubject (no library needed)</h3><p>For many apps, a shared service using <code>BehaviorSubject</code> is all you need. Simple to understand, zero dependencies, works great for apps with 5–20 shared state values.</p><h3>2. Angular Signals (built-in, Angular 16+)</h3><p>Signals are Angular's native reactive primitive. A <code>signal()</code> holds a value and Angular knows exactly which templates read it — so only those components re-render when it changes. No Zone.js, no RxJS operators needed. For new apps, Signals are often the first choice.</p><h3>3. NGXS</h3><p>NGXS uses decorators and classes instead of pure functions. It feels more Angular-like, has less boilerplate than NgRx, but follows the same Action → State flow. Good choice if you like the Redux pattern but find NgRx too verbose.</p><h3>4. Akita</h3><p>Entity-store based, similar to a repository pattern. Very intuitive for CRUD-heavy apps. Less opinionated than NgRx — no strict action/reducer split required.</p><h3>How to choose</h3><p>Small app → service + BehaviorSubject. Medium app → Signals. Large app with complex async flows and a team needing debugging tools → NgRx.</p>",
      "code": "// ─── Option 1: Service + BehaviorSubject ──────────────────────\n@Injectable({ providedIn: 'root' })\nexport class UserStore {\n  private _users$ = new BehaviorSubject<User[]>([]);\n  users$ = this._users$.asObservable();\n\n  setUsers(users: User[]) { this._users$.next(users); }\n  addUser(user: User)     { this._users$.next([...this._users$.getValue(), user]); }\n}\n\n// ─── Option 2: Angular Signals (Angular 16+) ──────────────────\nimport { signal, computed, effect } from '@angular/core';\n\nexport class CartStore {\n  items = signal<CartItem[]>([]);\n\n  // Computed values auto-update when items changes\n  total = computed(() => this.items().reduce((sum, i) => sum + i.price, 0));\n  count = computed(() => this.items().length);\n\n  add(item: CartItem)  { this.items.update(prev => [...prev, item]); }\n  remove(id: string)   { this.items.update(prev => prev.filter(i => i.id !== id)); }\n  clear()              { this.items.set([]); }\n}\n\n// ─── Option 3: NGXS ────────────────────────────────────────────\nimport { State, Action, StateContext, Selector } from '@ngxs/store';\n\nexport class LoadUsers { static readonly type = '[Users] Load'; }\n\n@State<User[]>({ name: 'users', defaults: [] })\n@Injectable()\nexport class UsersState {\n  @Selector() static allUsers(state: User[]) { return state; }\n\n  @Action(LoadUsers)\n  load(ctx: StateContext<User[]>) {\n    // call API and set state\n  }\n}",
      "language": "typescript"
    },
    {
      "id": "complete-ngrx-flow",
      "title": "Complete NgRx flow: action → effect → reducer → component",
      "explanation": "<p>Here is a complete, real-world NgRx feature for loading and displaying a list of users. All four layers are shown together so you can see how they connect.</p><ol><li>The <strong>component</strong> dispatches <code>loadUsers()</code> on init</li><li>The <strong>effect</strong> intercepts the action, calls the HTTP API, and dispatches <code>loadUsersSuccess</code> or <code>loadUsersFailure</code></li><li>The <strong>reducer</strong> handles those result actions and returns a new immutable state</li><li>The <strong>selectors</strong> extract the data from state</li><li>The <strong>component</strong> subscribes via selectors and renders the data</li></ol><p>The key insight is that the component never calls the API directly and never mutates state — it only dispatches actions and reads from selectors. All the complexity is handled in the NgRx layer.</p>",
      "code": "// ─── 1. users.actions.ts ──────────────────────────────────────\nexport const loadUsers        = createAction('[Users Page] Load Users');\nexport const loadUsersSuccess = createAction('[Users API] Success', props<{ users: User[] }>());\nexport const loadUsersFailure = createAction('[Users API] Failure', props<{ error: string }>());\n\n// ─── 2. users.reducer.ts ──────────────────────────────────────\nexport interface UsersState { users: User[]; loading: boolean; error: string | null; }\nconst initialState: UsersState = { users: [], loading: false, error: null };\n\nexport const usersReducer = createReducer(\n  initialState,\n  on(loadUsers,        state              => ({ ...state, loading: true,  error: null })),\n  on(loadUsersSuccess, (state, { users }) => ({ ...state, loading: false, users })),\n  on(loadUsersFailure, (state, { error }) => ({ ...state, loading: false, error }))\n);\n\n// ─── 3. users.effects.ts ──────────────────────────────────────\n@Injectable()\nexport class UsersEffects {\n  loadUsers$ = createEffect(() =>\n    this.actions$.pipe(\n      ofType(loadUsers),\n      switchMap(() =>\n        this.api.getAll().pipe(\n          map(users  => loadUsersSuccess({ users })),\n          catchError(err => of(loadUsersFailure({ error: err.message })))\n        )\n      )\n    )\n  );\n  constructor(private actions$: Actions, private api: UserService) {}\n}\n\n// ─── 4. users.selectors.ts ────────────────────────────────────\nexport const selectUsersState = createFeatureSelector<UsersState>('users');\nexport const selectAllUsers   = createSelector(selectUsersState, s => s.users);\nexport const selectLoading    = createSelector(selectUsersState, s => s.loading);\nexport const selectError      = createSelector(selectUsersState, s => s.error);\n\n// ─── 5. users.component.ts ────────────────────────────────────\n@Component({\n  template: `\n    <p *ngIf=\"loading$ | async\">Loading...</p>\n    <p *ngIf=\"error$ | async as e\" class=\"error\">{{ e }}</p>\n    <li *ngFor=\"let u of users$ | async\">{{ u.name }}</li>\n  `\n})\nexport class UsersComponent implements OnInit {\n  users$   = this.store.select(selectAllUsers);\n  loading$ = this.store.select(selectLoading);\n  error$   = this.store.select(selectError);\n  constructor(private store: Store) {}\n  ngOnInit() { this.store.dispatch(loadUsers()); }\n}",
      "language": "typescript"
    },
    {
      "id": "ngrx-entity",
      "title": "NgRx Entity adapter",
      "explanation": "<p><strong>NgRx Entity</strong> eliminates boilerplate for managing collections (arrays of records) in the store. Without it, every CRUD operation requires you to manually write array filter/map logic inside reducers. With it, you get pre-built, optimised mutation methods.</p><h3>EntityState — normalized storage</h3><p>Instead of storing records as a plain array (which requires O(n) lookup by id), EntityState stores them as two things: an <code>ids</code> array (ordered list of IDs) and an <code>entities</code> dictionary (object keyed by ID). This gives O(1) access to any record by ID.</p><h3>EntityAdapter — pre-built methods</h3><ul><li><code>addOne(entity, state)</code> — add a single record</li><li><code>addMany(entities, state)</code> — add multiple records</li><li><code>setAll(entities, state)</code> — replace all records</li><li><code>updateOne({ id, changes }, state)</code> — partial update</li><li><code>removeOne(id, state)</code> — delete by id</li><li><code>removeAll(state)</code> — clear the collection</li></ul><h3>getSelectors() — auto-generated selectors</h3><p>Returns <code>selectAll</code> (as array), <code>selectEntities</code> (as dict), <code>selectIds</code>, and <code>selectTotal</code>.</p>",
      "code": "import { createEntityAdapter, EntityAdapter, EntityState } from '@ngrx/entity';\nimport { createReducer, on, createFeatureSelector, createSelector } from '@ngrx/store';\n\nexport interface User { id: number; name: string; role: string; }\n\n// EntityState adds 'ids' and 'entities' to your own extra fields\nexport interface UsersState extends EntityState<User> {\n  loading: boolean;\n  selectedId: number | null;\n}\n\n// The adapter handles all collection mutations for you\nconst adapter: EntityAdapter<User> = createEntityAdapter<User>();\n\nconst initialState: UsersState = adapter.getInitialState({\n  loading: false,\n  selectedId: null\n});\n\nexport const usersReducer = createReducer(\n  initialState,\n  on(loadUsersSuccess,  (state, { users }) => adapter.setAll(users,  { ...state, loading: false })),\n  on(addUserSuccess,    (state, { user  }) => adapter.addOne(user,   state)),\n  on(updateUserSuccess, (state, { user  }) => adapter.updateOne({ id: user.id, changes: user }, state)),\n  on(deleteUserSuccess, (state, { id    }) => adapter.removeOne(id,  state))\n);\n\n// ─── Auto-generated selectors ─────────────────────────────────\nconst { selectAll, selectEntities, selectTotal, selectIds } = adapter.getSelectors();\n\nexport const selectUsersState   = createFeatureSelector<UsersState>('users');\nexport const selectAllUsers     = createSelector(selectUsersState, selectAll);       // User[]\nexport const selectUserEntities = createSelector(selectUsersState, selectEntities);  // { [id]: User }\nexport const selectUsersTotal   = createSelector(selectUsersState, selectTotal);     // number\n\nexport const selectUserById = (id: number) => createSelector(\n  selectUserEntities,\n  entities => entities[id]   // O(1) lookup\n);",
      "language": "typescript"
    },
    {
      "id": "ngrx-devtools-and-testing",
      "title": "NgRx DevTools and testing",
      "explanation": "<p><strong>Redux DevTools</strong> is a browser extension (Chrome/Firefox) that gives you a live inspector for your NgRx store. Every dispatched action is listed in a timeline. Click any action to see the exact state before and after. You can jump back in time to any previous state, replay actions from any point, and even export and import state snapshots.</p><h3>Setup</h3><p>Install <code>@ngrx/store-devtools</code> and add <code>StoreDevtoolsModule.instrument()</code> to AppModule, or <code>provideStoreDevtools()</code> in the standalone providers. Limit to dev mode so state isn't exposed in production.</p><h3>Testing NgRx — the good news</h3><p>Because NgRx enforces pure functions, testing each layer is straightforward:</p><ul><li><strong>Reducers</strong> — call the function directly with initial state + action, assert returned state</li><li><strong>Selectors</strong> — call with a mock state object, assert the selected value</li><li><strong>Effects</strong> — use <code>provideMockActions()</code> from <code>@ngrx/effects/testing</code> to control the action stream</li></ul>",
      "code": "// ─── DevTools setup (standalone) ──────────────────────────────\nimport { provideStoreDevtools } from '@ngrx/store-devtools';\nimport { isDevMode } from '@angular/core';\n\nbootstrapApplication(AppComponent, {\n  providers: [\n    provideStore({ users: usersReducer }),\n    provideEffects(UsersEffects),\n    provideStoreDevtools({\n      maxAge: 25,           // keep last 25 state snapshots\n      logOnly: !isDevMode() // disable time-travel in production\n    })\n  ]\n});\n\n// ─── Testing a reducer — pure function, no setup needed ────────\ndescribe('usersReducer', () => {\n  it('sets loading:true on loadUsers', () => {\n    const state = usersReducer(initialState, loadUsers());\n    expect(state.loading).toBe(true);\n    expect(state.error).toBeNull();\n  });\n\n  it('sets users and clears loading on success', () => {\n    const users = [{ id: 1, name: 'Alice', role: 'user' }];\n    const state = usersReducer(initialState, loadUsersSuccess({ users }));\n    expect(state.users).toEqual(users);\n    expect(state.loading).toBe(false);\n  });\n});\n\n// ─── Testing a selector — pure function, pass mock state ───────\ndescribe('selectAllUsers', () => {\n  it('returns all users from state', () => {\n    const mockState = {\n      users: { users: [{ id: 1, name: 'Alice', role: 'admin' }], loading: false, error: null }\n    };\n    expect(selectAllUsers(mockState)).toEqual([{ id: 1, name: 'Alice', role: 'admin' }]);\n  });\n});\n\n// ─── Testing an effect ─────────────────────────────────────────\ndescribe('UsersEffects', () => {\n  let effects: UsersEffects;\n  let actions$: Observable<Action>;\n  let apiSpy: jasmine.SpyObj<UserService>;\n\n  beforeEach(() => {\n    actions$ = new ReplaySubject(1);\n    apiSpy = jasmine.createSpyObj('UserService', ['getAll']);\n    effects = new UsersEffects(actions$ as Actions, apiSpy);\n  });\n\n  it('dispatches loadUsersSuccess on successful API call', (done) => {\n    const users = [{ id: 1, name: 'Alice', role: 'user' }];\n    apiSpy.getAll.and.returnValue(of(users));\n    actions$.next(loadUsers());\n\n    effects.loadUsers$.subscribe(action => {\n      expect(action).toEqual(loadUsersSuccess({ users }));\n      done();\n    });\n  });\n});",
      "language": "typescript"
    }
  ]
});
